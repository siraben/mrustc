# Task ledger

States are `TODO`, `ACTIVE`, `BLOCKED`, and `DONE`. A task is `DONE` only when
its listed evidence is committed or reproducibly generated by a committed
command. Task IDs are stable.

## M0: Repository and proof design

| ID | State | Task | Acceptance |
|---|---|---|---|
| M0-01 | DONE | Pin oracle, stable target, and version ladder | Revisions and hashes documented |
| M0-02 | DONE | Define architecture and compatibility policy | Architecture review finds no layer cycles |
| M0-03 | DONE | Identify live-bootstrap insertion point | Exact manifest stage and proof command recorded |
| M0-04 | DONE | Fast-forward oracle baseline and record delta | Branch contains upstream remote HEAD or explicit pin rationale |
| M0-05 | DONE | Establish build/test entry points | One command runs development and TCC gates |

## M1: Conservative-C nucleus

| ID | State | Task | Acceptance |
|---|---|---|---|
| M1-01 | ACTIVE | Portability/config header | TCC and strict compiler probes pass on i386/x86_64 |
| M1-02 | DONE | Checked allocator and pass arenas | Alignment, growth, OOM, reset tests pass |
| M1-03 | DONE | Strings, byte buffers, typed vectors | Unit and fault-injection tests pass |
| M1-04 | DONE | String interner and hash map | Stable IDs and collision tests pass |
| M1-05 | DONE | Source files, spans, diagnostics | Multi-file span snapshot tests pass |
| M1-06 | ACTIVE | CLI, target descriptor, process runner | No-core driver smoke runs under TCC-built binary |
| M1-07 | ACTIVE | TCC ABI/runtime probe suite | Required probes classified pass/emulated/blocked |

## M2: Syntax and macros

| ID | State | Task | Acceptance |
|---|---|---|---|
| M2-01 | DONE | Token schema and lexer | Canonical token dumps match oracle corpus |
| M2-02 | DONE | Token trees and editions | Nested delimiters and 2015-2024 keyword tests pass |
| M2-03 | DONE | Item/type/pattern/expression parser | Normalized AST matches oracle local tests |
| M2-04 | ACTIVE | cfg, attributes, builtin macros | Rust core cfg expansion matches oracle |
| M2-05 | ACTIVE | `macro_rules!` parser and matcher | Macro corpus and Rust core expansion pass |
| M2-06 | TODO | Proc-macro IPC/client | Existing `libproc_macro` round-trip tests pass |

## M3: Resolution, HIR, and metadata

| ID | State | Task | Acceptance |
|---|---|---|---|
| M3-01 | ACTIVE | Crate/module graph and namespaces | Import/reexport corpus matches oracle |
| M3-02 | ACTIVE | AST-to-HIR lowering | Canonical HIR matches oracle |
| M3-03 | ACTIVE | Type aliases, UFCS, bindings, markings | Rust core HIR resolves without guesses |
| M3-04 | ACTIVE | Uncompressed metadata writer/reader | C compiler reads its own dependent crate |
| M3-05 | TODO | Oracle metadata bridge | Bidirectional minimal-crate read succeeds |

### Semantic barrier checkpoints

- The complete cfg-active body manifest now advances atomically through
  `STRUCTURAL -> TYPED -> MARKED -> REGIONS`. MARKED computes the bounded C
  expression slice's value usage with its builtin-Copy subset and writes a
  NOT_PROMOTED sentinel because that slice admits no promotion-ready borrow
  form. REGIONS is a read-only structural closure proof over represented
  parameter/result/value types, body locals, expression types, direct and
  selected-call arguments, substitutions, generic arguments/defaults,
  predicate subjects/bounds/equality terms, outlives terms, const-argument
  types, and their conservatively bounded nested type forms. It accepts
  static/erased regions, exact early-bound lifetime parameters owned by the
  body item or its authenticated enclosing trait/impl, and exact late-bound
  indices owned by the active predicate binder. It rejects inference/error
  regions, inference/error types and consts, wrong parameter or binder
  ownership, cycles, shared DAGs, wrong body ownership, malformed slices, and
  inconsistent MARKED evidence. Success changes no HIR generation and mints a
  fresh REGIONS capability; every failure preserves phase, generation, and
  capability.
- This first REGIONS checkpoint is deliberately narrower than original
  mrustc's lifetime solver. Predicate traversal proves only structural closure
  and exact scoping; it does not solve traits, equality, or outlives facts. It
  does not yet traverse supertraits, associated bounds, ADT field declarations,
  enum discriminants, or type-position expressions. It does not claim lifetime
  inference, outlives/equality solving, promotion eligibility, or borrow
  semantics.
  Enum discriminants and unevaluated array lengths remain outside the body
  manifest until they have stable type-position atom identities. The next
  checkpoint begins the ordered closure/static-promotion/vtable/UFCS/reborrow/
  erased-type rewrites before independent rewritten-HIR validation.

### Active front-end evidence

The active states above are intentional: the committed foundations do not yet
meet the corpus-wide acceptance gates.

- M2-04 has structural `cfg`/`cfg_attr` views, builtin expansion and reparsing,
  cfg-active declaration lowering, and source-qualified cfg-active module item
  views (`141e7397`, `105c10fd`, `1f97e5b5`, `bdb42acc`, `784ee526`). The
  graph now borrows one explicit cfg set as the authority for every source
  unit and reapplies cfg/cfg_attr to every generated item sequence before
  recursive item-macro planning. Effective attribute snapshots store metadata
  bodies without source wrappers. This is focused evidence only; the
  Rust-core cfg acceptance corpus remains.
  The compiler driver now derives intrinsic cfg facts from the exact canonical
  target descriptor, including architecture, OS, environment, ABI, vendor,
  family, pointer width, endianness, baseline target features, thread-local
  availability, and target atomic classes. A caller-built descriptor must be
  byte-for-byte equivalent in semantic fields to the registered descriptor for
  its triple. Rust does not expose edition cfg facts, so the former invented
  `edition = "..."` and `rust_20xx` predicates were removed. Crate features,
  command-line cfg values, panic mode, sanitizers, and codegen options remain
  unowned and are not fabricated.
- M2-05 has bounded parsing/matching/transcription, explicit macro AST nodes,
  wrapper-free output reparsing, and deterministic lexical item-macro planning
  (`b8c61414`, `49c8de1d`, `9e5b5cef`, `a51e8b99`, `5ab1212f`, `ba6f4990`,
  `537a9733`). Planner nodes now own effective attributes, the outermost source
  invocation anchor, and source-qualified immediate generated-item invocation,
  definition, and depth provenance. Every parsed graph source unit owns one
  contextual plan; external source files inherit the parent textual
  `macro_rules!` scope captured at their declaring `mod`, while preserving
  declaration order, cfg filtering, nested propagation, and local shadowing.
  Generated declarations enter namespaces and generated inline modules recurse
  through the same graph builder. The planner now accepts bounded exact
  source-qualified invocation-to-declaration records, validates each target
  against its declaration AST, and consumes every record exactly once. Those
  records can authorize aliased, imported, and qualified paths without
  deriving identity from spelling; duplicate and inactive/stale records reject
  atomically. An explicit skeleton mode records
  unresolved source invocations as exact pending identities while omitting
  them from semantic roots; local forward references and unresolved generated
  invocations still reject. The production graph now uses that mode to build
  cfg-active modules and complete declaration histories, including inherited
  textual scope; resolves each unqualified pending call at its exact lexical
  position; replans with the exact declaration identities; discards the
  skeleton's derived storage; and publishes only the rebuilt graph. A
  cross-source fixture proves local-to-`macro_use`-to-local shadow ordering and
  exact expansion provenance. Staging now repeats transactionally when a full
  rebuild discovers pending calls in newly loaded source modules. Source calls
  inside traits and impls can be retained during discovery with their nearest
  module container, but final expansion there remains unsupported. Public
  source-qualified local-crate paths now resolve through the same fixed-point
  module/import model and pass one exact declaration identity back to the
  planner. Private, ambiguous, unresolved, extern-prelude, and generated
  qualified paths remain fail-closed; unqualified `use`-imported invocation
  lookup is still unsupported. Hygiene,
  expression-position expansion,
  proc macros, and the Rust-core macro acceptance corpus remain unsupported or
  unproven as applicable.
  The syntax layer now distinguishes invocations, `macro_rules!` definitions,
  and declarative `pub macro` definitions. Both Rust 1.90 declaration forms
  preserve visibility, attributes, name, optional parameter token tree, body,
  delimiter, and span. All 19 declarative macros in the pinned
  `core/src/macros/mod.rs` parse. Rule-bodied definitions without a parameter
  list now use the bounded `macro_rules!` matcher/transcriber, while
  parameter-style declarations still reject explicitly by default. The one
  admitted exception is the exact resolver-certified compiler builtin
  `cfg_select`: its cfg-active declaration must have exactly one bare
  `rustc_builtin_macro` attribute, source calls select the first true predicate
  or final `_` fallback, and only the selected item group is reparsed.
  Shadowed, similarly named, malformed, cfg-disabled, and generated calls fail
  closed. Item-macro plans separately retain
  every cfg-active named macro declaration, its effective attributes,
  immediate inline-module container, form, span, and generated provenance.
  Cfg-disabled declarations do not enter that list. This preserves the exact
  `rustc_builtin_macro` and `macro_export` metadata needed by the next resolver
  slice without exposing definitions as ordinary semantic roots.
  Function-item qualification now recognizes `const` followed by the remaining
  `async`, `unsafe`, and `extern` qualifiers before `fn`, rather than
  misclassifying forms such as `pub const unsafe fn` as const items. Array
  expressions also preserve the distinct `[value; length]` repeat form and
  traverse both expressions in syntax feature counts. Exact dump fixtures and
  the real Rust 1.90 `core/src/num/mod.rs` pass under strict GCC 15.3, TinyCC
  0.9.27, and GCC ASan/UBSan/leak builds. These are parser prerequisites for
  resolving the earlier `#[macro_use]` modules before authenticating the root
  `include!`; they do not advance the production module-graph frontier alone.
  The impl parser also distinguishes the rustdoc primitive placeholder
  `impl ! {}` as an inherent impl for the never type from a negative trait impl
  such as `impl !Trait for Type {}`. Canonical AST fixtures preserve both
  interpretations. Where predicates now distinguish type and lifetime
  subjects, and their ordered bounds distinguish trait types from interned
  lifetime constraints. This parses the real `C: Fn(&Ret) -> bool + Copy +
  'static` contract bound without pretending that `'static` is a type path;
  HIR lowering still rejects lifetime constraints explicitly until it can
  preserve their semantics.
- M3-01 has module graphs, three namespaces, fixed-point imports/reexports,
  graph-owned syntax views, source-qualified module declaration identities,
  and cfg-active module item views (`73e6e589`, `077785ab`, `ec41f1fc`,
  `33f250e9`, `784ee526`, `ba6f4990`, `537a9733`, `73882937`). Graph builds now
  publish monotonically revisioned, graph-owned effective source/generated item
  and attribute snapshots, and no namespace, import, or active-item semantic
  payload is exposed after a failed build. Source imports and reexports bind
  generated declarations through the same namespace tables while preserving
  their source-qualified declaration identity. Effective roots and recursively
  recorded trait/impl children have nonzero opaque IDs local to one graph
  revision and module. Revision-checked root, direct-child, and attribute
  lookup traverses that graph-owned effective tree; disabled children are
  omitted after cfg/cfg_attr processing rather than published as inactive.
  Module contents remain roots of their own module and are not duplicated as
  recursive children. Revision-checked namespace lookup now
  requires the caller's explicit graph revision, distinguishes stale and
  failed graph builds, supports named crate-root aliases, and resolves `as _`
  without publishing a fictitious `_` binding (`3aa917d1`). Resolver snapshots
  also retain declaration-ordered results for each source-qualified `use`,
  including anonymous leaves, glob expansions, and destination-shadowed
  results. Anonymous-import trait scope remains deferred to M3-03.
  Macro-generated imports and extern crates still hard-error, and the
  corpus-wide import/reexport oracle gate remains open.
  Cfg-active retained macro declarations now enter the macro namespace of
  their exact source root or immediate inline-module container. Their
  source-qualified declaration identity survives, while they remain absent
  from active/effective semantic item lists. Cfg-disabled declarations are not
  registered. Exactly one bare effective `#[macro_export]` on a `macro_rules!`
  declaration now copies that same source-qualified identity into the crate
  root macro namespace with public visibility. Root-local exports deduplicate
  by declaration identity; malformed/duplicate attributes, declarative macro
  exports, missing/duplicate owners, and distinct same-name exports reject the
  graph atomically. Cfg-disabled exports remain absent and non-exported nested
  macros remain local. A revision-checked lookup now maps each cfg-active
  source-qualified macro declaration to its exact original owner module, form,
  visibility, span, generated provenance, and effective attributes. Missing,
  stale, and failed-build lookups fail distinctly without exposing payload.
  A separate declaration-ordered legacy macro scope now applies one bare
  effective `#[macro_use]` on source modules from child to parent. It carries
  exact declaration and introducing-module identities, composes nested imports,
  filters declarative macros, honors cfg omission, and applies later-binding
  shadowing without confusing textual scope with the path namespace. Attribute
  arguments/duplicates, non-module placement, and generated macro-use modules
  reject atomically. A core-shaped fixed-point fixture proves that the
  `macro_export` root binding, `#[macro_use]` textual binding,
  `prelude::v1` reexport, edition-prelude glob, and crate-root glob all preserve
  one exact declaration identity, from which the exact builtin attributes can
  be recovered. Those declaration-ordered scopes now feed the exact planner
  boundary through a transactional skeleton/replan graph pass. Unqualified
  lookup uses declaration-ordered textual history first, then an unambiguous
  exact macro-namespace binding; this reaches nested `macro_export` builtins
  without treating invocation spelling as declaration identity.
  A test-only source adapter can splice exact unqualified item-position
  `include!("relative.rs");` fragments with lexical order and per-item source
  provenance. Loading is bounded to depth 32, 256 files, and 16 MiB total,
  with the byte cap enforced in the read loop. Cycles, absolute or escaped
  paths, unsupported forms/items, parse/cfg errors, source-array relocation,
  and exact-limit/one-byte-over loading have focused coverage. This adapter is
  remains explicitly separate from production macro resolution.
  Production now defers non-`macro_use` external modules while exact pending
  targets are discovered, honors one simple relative `#[path = "..."]` on the
  external macro-use modules needed to complete scope, and intercepts include
  only after its exact cfg-active declaration is a source `macro_rules!`
  declaration with exactly one bare `rustc_builtin_macro` attribute. The
  supported invocation is then spliced under the same depth-32, file-256, and
  16 MiB bounds; derived graph state and all contextual plans are discarded
  and rebuilt. Missing, malformed, duplicate, shadowed, disabled, qualified,
  nonliteral, generated, and repeated-source cases reject without opening an
  unauthenticated path. The authenticated Rust 1.90 `core` graph currently
  loads 293 source files and 377 modules with zero parser failures. Its one
  graph error is `zip_impl_general_defaults! {}` inside an impl in
  `core/src/iter/adapters/zip.rs`: transcription succeeds, then the root item
  fragment parser rejects the associated `default fn` output. The frontier
  now passes all item-position `cfg_select!` calls reached in `ffi/va_list.rs`
  and `ffi/primitives.rs`, as well as the former
  `core/src/lib.rs:421` gate, parses the `impl ! {}` placeholder in
  `primitive_docs.rs`, retains the lifetime bound in `contracts.rs`, and owns
  block-local const declarations as item statements without publishing them
  in module namespaces or effective-item views. Const blocks remain distinct
  expressions, and HIR body lowering rejects both forms transactionally until
  local definitions and const evaluation exist. Apostrophe disambiguation now
  retains all three lifetimes and delimiters in the lifetime-bearing method
  signature inside `define_bignum!`. Outer block-expression attributes now
  retain their attribute IDs and spans on the complete expression chain, are
  visible in AST dumps, and reject transactionally in HIR body lowering until
  their semantics are supported; inner attributes remain distinct and reject
  outside crate/module starts. Match-arm `if let` guards now retain a real
  pattern, initializer expression, and full guard span without changing
  ordinary expression guards; dumps expose both forms and HIR body lowering
  rejects let guards transactionally until match semantics exist. The
  method-call turbofish now retains ordered generic arguments and its exact
  `::<...>` span, appears in dumps, and rejects transactionally during HIR
  body lowering until method substitution exists. Opaque `impl Trait` types
  now own ordered, spanned trait bounds, including structured callable inputs
  and `Output`; HIR lowering rejects them explicitly until an existential type
  model exists. Postfix `?` now has a distinct operand-owning expression node,
  appears in dumps, and rejects transactionally during HIR body lowering until
  residual control flow exists. Block lookahead now recognizes local const
  items through balanced outer attributes, preserving those attributes on the
  item without publishing it in module namespaces. Tuple-index projection now
  has a distinct base-owning expression node with a parsed ordinal and exact
  selector span; HIR body preflight rejects it transactionally until tuple
  layout semantics exist. Match-arm body parsing now applies a statement-
  expression restriction after block-like expressions, allowing Rust's omitted
  arm comma without misclassifying a following tuple or slice pattern as a call
  or index postfix. Qualified expression paths now retain a self type, optional
  trait path, associated value path, and exact qualifier span; HIR body
  preflight rejects direct and call-shaped uses without falling back to an
  unqualified local or free function. Block statement lookahead also recognizes
  ordinary, attributed, `const`, `async`, and `unsafe` local functions; normal
  item parsing owns them while module namespaces exclude them. The next exact
  parser frontier is const trait impl `impl const From<Infallible>` at
  `core/src/num/error.rs:30:12`, reported as `expected impl body`.
  The narrower source-fixture include adapter parses `primitive_docs.rs` and
  then rejects its unsupported item set without publishing a partial splice.
- M3-02 has explicit typed identities/types, cfg-active declaration lowering,
  hard resolution failures, and explicit syntax-module-to-HIR-module mapping
  (`1fa285db`, `cd1b62ee`, `bdb42acc`, `76d142b9`, `36cde34c`, `5bd2087a`,
  `f7472bc9`).
  Graph lowering and module maps bind a specific graph identity/revision,
  enumerate only its effective snapshot, and reject failed graph revisions
  before exposing stale mappings. Generated body-free structs, enums, and type
  aliases now lower with validated source-qualified macro provenance.
  Generated trait/impl roots and associated methods also lower through the
  recursive effective-item path; other body-bearing generated declaration
  shapes still hard-error before publication. Failed graph lowering uses
  stack-disciplined, lifetime-checked
  arena/interner checkpoints to restore every HIR vector, arena, interner,
  result ID, and module map, and successful lowering explicitly commits those
  checkpoints. Graph type and restricted-visibility paths now use the import
  resolver as the authority for direct declarations, cross-file imports,
  aliases, module aliases, globs, chained reexports, generated targets, and
  crate-root aliases. Exact source/item identity preserves the original DefId,
  and commit-time validation rejects incomplete or permuted module maps.
  The HIR now also represents trait and positive trait-impl methods, receiver
  classification, symbolic owner-qualified `Self`, trait-method identity,
  structural effective item attributes, and source-qualified unlowered bodies.
  Method lowering is ordered after its containing trait/impl header and
  associated types, so `Self`, `Self::Assoc`, explicit projections, and parent
  generics resolve without guessed identities. Required/default method
  completeness, receiver/local mutability, cross-file body provenance, and
  graph rollback after staged bodies have focused coverage. Graph validation
  recursively checks effective identity, child topology, source/generated
  provenance, and structural attributes before reservation and lowering.
  Generated children have no source item identity, retain their immediate
  producing invocation/definition and expansion depth, and use the outermost
  source-backed invocation as a coarse span anchor. HIR copies effective
  attributes structurally for item-owning roots and children. Module
  declarations store effective outer attributes on the real child module,
  separately from its inner attributes and without allocating a synthetic item
  or DefId. Source-qualified graph syntax access authenticates each effective
  declaration against its publishing syntax unit's per-item source map;
  `include!`-spliced items and inline-module children therefore retain their
  included-file provenance through HIR. The canonical whole-core HIR probe now
  passes `primitive_docs.rs`. Item-owned outlives predicates separately retain
  ordered `Type: 'a` and `'a: 'b` constraints as HIR regions, and free-function
  trait predicates are admitted without weakening higher-ranked or malformed
  generic rejection. This clears the `C: Fn(&Ret) -> bool + Copy + 'static`
  clause in `contracts.rs`. Source-written and authenticated macro-generated
  root consts now retain their real item DefId, effective attributes, declared
  type, and an unlowered body pointing at the exact graph-owned initializer
  expression. Immutable, explicitly typed, initializer-bearing source statics
  use the same retained body identity; mutable and generated statics and
  generated functions whose producing invocation and declaration share the
  owning module source now retain the same real DefId, effective attributes,
  substituted signature, parameter locals, and exact graph-owned unlowered
  body identity. Include-origin generated bodies remain closed until syntax
  and diagnostic sources can be represented separately. Array lengths that
  are simple resolved const names retain the referenced item DefId as
  unevaluated HIR consts rather than synthesizing an integer value.
  Inherent impl headers now retain their real self type with no synthetic trait
  identity, and their non-default, immutable, explicitly typed,
  initializer-bearing associated consts retain a real child DefId and
  graph-owned unlowered initializer body. Immutable, explicitly typed,
  targetless trait associated const declarations retain their child DefId and
  trait-owned `Self` type without a body; defaults and impl definitions remain
  closed pending an explicit declaration link in the value-item schema. This
  clears `f128::RADIX`, `f128::consts::PI`, `POWER_OF_FIVE_128`, and
  `Integer::{ZERO, ONE}`. This also clears every same-root function emitted by
  `signed_fn!` and `unsigned_fn!` in `num/int_sqrt.rs`. The target-configured
  canonical probe also retains the cfg-active children of a source-written
  `unsafe extern "C"` block as ordinary bodyless root HIR functions. The
  block itself has no synthetic item or DefId; the exact lint-only block
  attribute `allow(improper_ctypes)` is accepted and ignored because it has no
  checking or code-generation effect. Each child keeps its real definition,
  visibility, effective structural attributes, parameter/return types, exact
  span, and inherited `C` or `unadjusted` ABI. A declaration without an
  explicit safety qualifier is unsafe, while an explicit `safe fn` remains
  safe. Other block attributes and ABIs, generated blocks, foreign statics,
  and generic or body-bearing children remain closed. This clears the four
  libm declarations in `num/libm.rs`, the `link_name` declarations in
  `fxsr.rs`, and the cfg-active unadjusted intrinsic blocks. The
  target-configured canonical
  probe still has zero graph/import errors over 363 sources and 451 modules;
  generated inline modules now use their authenticated graph child and mapped
  HIR module definition, invocation span, effective outer/inner attributes,
  and generated child declarations without inventing a module item or second
  DefId. This clears the `mod private` emitted by
  `impl_zeroable_primitive!`. Root `const _` declarations now follow the
  graph's namespace rule: every item retains a distinct DefId, declared type,
  effective attributes, span, and unlowered initializer body, but introduces
  no value name and therefore does not collide with another anonymous const.
  This clears `define_valid_range_type!` in `num/niche_types.rs`.
  Source-written targetless foreign types in those authenticated extern-C
  blocks now retain a real extern-type DefId, declared visibility, effective
  attributes, span, import identity, and `CM_HIR_TYPE_FOREIGN_KIND` path
  classification. This clears `type VTable;` in `ptr/metadata.rs:165`. The
  graph-authenticated local macro provenance now recognizes named declarative
  `macro` definitions as well as `macro_rules!`, clearing the recursive
  `marker_impls!` expansions in `marker.rs`. Bound-level higher-ranked trait
  bounds now retain an ordered, named, exactly spanned predicate binder;
  binder-local regions lower as `late(index)` while enclosing item lifetimes
  remain early-bound. Generic inherent methods retain their parent impl,
  predicates, receiver, generics, signature, and body without inventing a
  trait declaration link. Model validation confines bound-level late regions
  to the bound side of their owning predicate. Predicate-prefix HRTBs use one
  item-owned scope shared by the subject and every atomic trait or outlives
  constraint expanded from the source predicate; scope counts and references
  are validated together. Canonical dumps are `hir-v37`. This clears both
  `VaListImpl::with_copy` in `core/src/ffi/va_list.rs:246` and
  `for<'a> F: FnMut(GenericShunt<'a, I, R>) -> U` in
  `core/src/iter/adapters/mod.rs:155`. Graph-authenticated inherent methods may
  now carry explicit visibility and `const`; they retain their parent, effective
  attributes, Rust ABI, receiver/signature, const flag, and graph-owned
  unlowered body while trait and trait-impl const methods remain closed. This
  clears `pub const fn is_nan` in `core/src/num/f128.rs:293`.
  Macro-generated trait-impl associated const definitions now retain a
  schema-level link to the exact targetless trait declaration, with the impl
  parent, `Self`-owned type, effective provenance, and graph-owned initializer
  body validated together. Impl completeness requires exactly one definition
  for every required const. This clears `Integer::{ZERO, ONE}` from
  `int!(u16, u32, u64)` at `core/src/num/dec2flt/float.rs:48`. Trait associated
  const defaults retain the declaration-owned type and graph-owned unlowered
  body; impl completeness permits zero or one override for a default while
  still requiring exactly one definition for a targetless declaration. This
  clears `SIG_BITS` and the following `RawFloat` defaults. The frontier is now
  crossed by allowing method-owned generic parameters and where predicates on
  positive trait impl methods, with the impl method's declaration link and
  generic arity validated together. This clears `Hash::hash<H>` in
  `core/src/num/nonzero.rs:291`. The frontier is now the `extern "rust-call"`
  trait method `AsyncFn::async_call`, whose ABI is retained and checked against
  linked impl definitions. This clears line 14 in
  `core/src/ops/async_function.rs`. Trait declarations and positive trait impls
  now retain generic associated type parameters and where predicates. The
  `AsyncFnMut::CallRefFuture<'a>` fixture preserves its item-owned lifetime,
  `Future<Output = Self::Output>` bound/equality, inherited `AsyncFn::Output`
  identity, and `Self: 'a` outlives predicate; linked impl definitions must
  have the declaration's generic arity. This clears line 28 in
  `core/src/ops/async_function.rs`. Attributed async trait methods now retain
  their effective attributes, async signature flag, custom receiver, and
  bodyless declaration; linked impl methods must agree on asyncness. This
  clears `AsyncDrop::drop` at `core/src/future/async_drop.rs:40`. Enum variants
  now own distinct canonical DefIds, pre-reserved from their source enum/index
  identity and bound atomically to the final enum item and variant index.
  Structural explicit, alias, and glob imports retain those DefIds; named-field
  variants remain type-only while unit and tuple variants also admit value
  bindings. Existing imports are revalidated when the enum binds. This clears
  the enum-variant bindings in the prelude glob at `core/src/lib.rs:214`
  (`item=2`, span `7763..7789`). Named macro definitions consumed before the
  effective-item pass now retain canonical definition-only DefIds on first
  structural import. The authenticated graph declaration supplies the exact
  source identity, owner module, declared name, and `macro_rules!` or
  declarative form; aliases and repeated imports share one DefId, and only
  macro-namespace bindings may target it. This clears the `Copy` macro binding
  in that same prelude glob. Const generic parameters now retain their parsed
  structural declared type in the AST and lower through the existing HIR
  const-parameter kind, declared-type identity, source order, and owner range.
  Const defaults retain both exact source text and a structural expression;
  the first fail-closed HIR slice accepts a prior const parameter, a unique
  root const, or a unique inherent associated const path, optionally wrapped
  in a plain block. The resulting const argument keeps the declared type and
  referenced canonical DefId. The HIR setter structurally authenticates the
  default value type against the destination const parameter and, for a
  parameter reference, against the referenced parameter too; mismatches reject
  without publishing the default. Generic-parameter `where T: ?Sized` is
  normalized into the same relaxed-sized parameter property as inline
  `T: ?Sized`, without inventing a trait predicate. This clears
  `atomic_cxchg` at `core/src/intrinsics/mod.rs:96`, including both
  `AtomicOrdering` parameters. Inline lifetime bounds on type and lifetime
  generic parameters now lower as ordered item outlives predicates, retaining
  exact bound spans and early-bound or `'static` region identity. This clears
  `T: ?Sized + 'static` in `type_id` at
  `core/src/intrinsics/mod.rs:2757`. The const-default slice clears
  `{ Assume::NOTHING }` on `ASSUME: Assume` in `TransmuteFrom` at
  `core/src/mem/transmutability.rs:90`. The canonical 363-source/451-module
  probe still has zero graph/import errors. Lifetime-generic trait methods now
  retain method-owned early-bound regions in receivers, arguments, and default
  bodies, clearing `Error::provide<'a>` at `core/src/error.rs:204`.
  Trait-owned lifetime parameters and lifetime supertraits lower structurally
  as early-bound generics plus `Self: 'a` outlives predicates rather than
  nominal supertrait edges, clearing `unsafe trait Erased<'a>: 'a` at
  `core/src/error.rs:971`. Trailing type defaults are now retained on structs,
  unions, and enums as well as aliases and traits. ADT applications fill
  omitted trailing type arguments from primitive defaults or from a prior
  type parameter after substituting the argument already supplied for that
  parameter; wrong arity/kind, const-generic ADT applications, and defaults
  requiring broader structural substitution remain fail-closed. This clears
  `pub enum ControlFlow<B, C = ()>` at
  `core/src/ops/control_flow.rs:87`. The canonical GCC- and TinyCC-built probes
  still report zero graph/import errors over 363 sources and 451 modules. Both
  now retain method-owned `const N: usize` on
  `SpecArrayClone::clone` in `core/src/array/mod.rs:454`. Array types such as
  `[Self; N]` retain `N` as a typed `CM_HIR_CONST_PARAMETER`; non-const or
  non-`usize` array-length parameters and function generic defaults remain
  fail-closed. Boundless, nongeneric targetless associated-type declarations
  now enter a transaction-marked registry before trait defaults lower, without
  publishing an item or changing canonical item/definition/type order. This
  admits the source-order-independent default
  `FromResidual<R = <Self as Try>::Residual>` while retaining wrong-kind,
  arity, duplicate, bounded/GAT, and rollback rejection. Defaulted supertraits
  continue to see their completed defaults before trait headers bind. The
  canonical GCC- and TinyCC-built probes still report zero graph/import errors
  over 363 sources and 451 modules. The resolver now authenticates the single
  cfg-active private root glob carrying the exact effective
  `#[prelude_import]` attribute and retains its resolved public bindings as a
  lowest-priority fallback for unqualified lookup in every module. Direct and
  imported bindings shadow that fallback; qualified, private, malformed,
  duplicate, nested, public, ambiguous, and non-glob cases remain fail-closed.
  This clears the `Sized` supertrait on `FullOps` at
  `core/src/num/bignum.rs:23`. Ordered positional supertrait arguments and
  associated equalities now retain their resolved trait/associated DefIds,
  RHS types, source order, exact spans, and deep ownership, clearing
  `ops::Shr<u32, Output = Self>` at
  `core/src/num/dec2flt/float.rs:20`. Literal `'static` supertraits lower as
  structural `Self: 'static` predicates, while free, inferred, and wrong-kind
  lifetime bounds reject. Trait aliases now retain ordered trait and lifetime
  RHS bounds, generic arguments, associated equalities, modifiers, exact
  spans, and alias-owned generic defaults. Alias references are admitted in
  predicates and supertraits, mixed trait/alias cycles reject iteratively, and
  impl headers cannot name an alias. This clears `Thin` in
  `core/src/ptr/metadata.rs:84`. Auto traits now retain authenticated identity,
  and safe itemless negative impls retain polarity, complete
  generics/predicates/types, identities, and spans; duplicates and
  positive/negative overlap reject. Structural trait type defaults recursively
  substitute `Self` plus prior lifetime/type arguments with DAG memoization,
  while explicit static, early-bound, and predicate-binder lifetime arguments
  retain their authenticated region identity. Const trait arguments remain
  unsupported and no source-reachable const-default claim is made.
  Associated-type trait bounds now retain positional lifetime/type arguments
  and Self-free trailing defaults separately from associated equalities.
  Lifetime bounds lower as exact projection-subject outlives predicates with
  authenticated static or enclosing early-bound regions; inferred/free,
  wrong-owner, binderless-late, and GAT cases fail closed. Trait defaults such
  as `PartialEq<Rhs = Self>` now substitute the bounded associated projection
  as their authenticated `Self`, clearing the `marker.rs` frontier. Both GCC-
  and TinyCC-built probes now stop at `core/src/ops/async_function.rs:28`
  (`item=0`, span `1247..1259`), where inherited `Self::Output` must retain the
  defining generic supertrait's arguments.
  Each source
  `use` is retained
  in its module as a structural record containing the raw
  tree, declared visibility, effective attributes, and resolved namespace
  bindings; it is not a definition or synthetic HIR item. Import retention
  alone does not implement downstream lookup,
  visibility enforcement, or anonymous-trait scope. Value/body path
  resolution, extern-prelude resolution, expression lowering, and canonical
  HIR oracle parity remain. Before Rust-core corpus scaling,
  per-path segment allocation and the linear source/item declaration lookup
  should be replaced by reusable scratch storage and a keyed index.
- M3-03 has a first bounded type-alias substitution slice. Definitions and all
  lifetime/type generic parameters are predeclared before targets lower; alias
  applications remain explicit transient HIR nodes until every declaration is
  bound, then structural normalization substitutes parameters through
  references, pointers, tuples, arrays, slices, function pointers, and nominal
  arguments without changing nominal DefIds. Forward chains, imports, chained
  reexports, macro-generated generic aliases, inferred lifetimes, distinct
  concrete instantiations, and nested non-cycles have focused coverage. Wrong
  arity/kind and direct, structural, generic, or cross-module cycles hard-error,
  and both the normalizer and graph transaction rewind their allocations on
  failure. Trailing type-parameter defaults on free aliases are now structural
  from parsing through HIR: defaults can refer to earlier lifetime/type
  parameters, instantiate sequentially after explicit arguments, and remain
  overrideable. Omitted and partial defaults, forward/default alias chains,
  imported/chained reexports, generated aliases, explicit or inferred leading
  lifetimes, unused malformed signatures, cycles through defaults, direct API
  rollback, and graph transaction rollback have focused coverage. The public
  HIR model assigns a default only while its item definition is reserved,
  rejects self/later/foreign parameter dependencies, and prevents bypassing the
  setter. Lifetime defaults and type or const defaults on functions/impls are
  parser errors; otherwise-valid type defaults on structs and enums, plus
  literal, compound, and non-path const defaults, remain explicit hard errors
  in HIR. A first explicit projection slice now preserves
  definition-backed `<T as Trait>::Assoc` types from parsing through HIR and
  alias substitution, including forward declarations, use in trailing alias
  defaults, local module paths, imports, and chained reexports. Traits enter the
  type namespace and own bare, targetless associated type declarations without
  publishing those children in their module namespace. The ordered nominal
  generic impl checkpoint lowers local, positive impls of local, nongeneric
  traits for a scalar or local struct/enum self type. Its generic shape is
  exactly `impl<T, U, ...> Trait for LocalNominal<T, U, ...>`: impl parameters
  are type-only and have no defaults, their count equals the nominal arity, and
  the self arguments use each parameter exactly once in declaration order.
  Monomorphic scalar and zero-argument local nominal impls remain supported.
  Impl children may contain bare associated type definitions and ordinary
  non-generic Rust-ABI methods. They must define every required associated
  type and method exactly once unless a trait method has a default body, and
  must match the trait's safety. Impl methods record the corresponding trait
  method DefId; trait defaults may be omitted or overridden. Receivers remain
  parameter zero and are classified as value, shared reference, mutable
  reference, custom typed, or absent. `mut self` produces a mutable local,
  while `&mut self` produces an immutable binding whose reference pointee is
  mutable. Typed receivers require a plain `self` binding. Symbolic `Self` is
  owned by the containing trait/impl and survives alias normalization and
  generic instantiation; projection selection conservatively returns
  `DEFERRED_SELF` when symbolic `Self` is queried. Allocation-free
  `cm_hir_match_projection` reports the matching template and identities;
  mutable `cm_hir_select_projection` instantiates it.
  A direct target such as `type Assoc = T` reuses the query argument TypeId
  without allocation, while structural substitution appends replacement types
  without mutating existing HIR and rewinds those additions on failure.
  Unsupported candidates that may apply conservatively return
  `DEFERRED_ARGUMENTS` and are not skipped in favor of another candidate;
  multiple exact candidates return `AMBIGUOUS`, and substitution failure never
  publishes a partial target or identities. Both APIs take the requesting
  crate explicitly: foreign traits or nominal self types and potentially
  matching foreign impls return `DEFERRED_CRATE`, while unrelated foreign impls
  do not block an exact local selection. Repeated, reordered, nested, fixed,
  omitted, or unused impl parameters remain unsupported, as do predicates and
  where clauses, specialization, defaults, lifetime/const impl generics,
  generic traits, trait or associated arguments, GATs, blanket impls,
  cross-crate selection, alias normalization during matching, and recursive
  projection selection. Method bodies are retained as unlowered
  `(source, expression-id)` identities; this checkpoint does not type-check or
  execute them and does not implement method lookup or dispatch. Source-backed
  graph lowering preserves effective outer method/item attributes as ordered
  structural metadata, including metadata bodies, source attribute identity,
  span, and expansion depth. Graph attributes also expose an explicit owner;
  a HIR attribute's owner is its containing item. Raw lowering still rejects
  attributed associated items, and standalone expanded lowering still rejects
  attributed associated types. Attribute preservation is not semantic handling:
  consumers must
  implement effectful attributes such as `track_caller` or hard-error. Graph
  completeness is checked over only the cfg-active recursive child set, so a
  disabled required trait method does not require an impl definition; a
  missing active required method still fails the transaction. Bounds and
  predicates, method generics/where clauses, non-Rust ABI methods, const/async
  methods, trait associated consts, inherent impl features beyond safe positive
  headers and bounded const definitions, semantic use of retained negative
  impls, shorthand `T::Assoc`,
  `<T>::Assoc`, expression UFCS,
  associated bindings, anonymous trait imports, and unsupported body-bearing
  graph root shapes also remain explicit errors or open work as applicable.
  The pinned
  Rust 1.90 `core`/`alloc` census has 407 explicit associated-type impls, 385
  method-bearing. Fifty-eight fit a broader nominal header funnel; the exact
  parser-compatible ordered type-only boundary leaves 14 non-`impl const`
  forms, all 14 method-bearing with 21 methods total. Twelve impl parents have
  attributes (ten ordinary and two cfg-only); 13 methods across eight impls
  have ordinary `inline` attributes, so 12 of 14 impls require ordinary
  attributes somewhere while the other two are cfg-only. This exact set has no
  cfg_attr or generated impl/method occurrences. Two methods have typed
  receivers and 19 have simple non-self parameter shapes; all 21 are
  non-generic, safe, non-const, non-async, non-extern, non-variadic, and have no
  method where clause. Four signatures contain a supported single
  `Self::Assoc` projection. The trait grouping is `Future` 2/2,
  `Iterator` 5/10, `Try` 2/4, `IntoIterator` 2/2, `Deref` 1/1, and
  `AsVecIntoIter` 2/2 (impls/methods). All require their trait definition to be
  active and lowered. `AsVecIntoIter` was the first compact compatible
  definition. An extracted three-file fixture from
  `library/alloc/src/vec/in_place_collect.rs:428` and
  `library/alloc/src/vec/into_iter.rs:531` now proves its unsafe trait/impl
  safety, sibling-module reexports, ordered generic substitution,
  `Self::Item` projection, mutable receiver, body provenance, and
  `cfg(not(no_global_oom_handling))` disappearance together. The fixture
  intentionally simplifies the surrounding `IntoIter` definition, but now
  retains upstream `#[stable]` on its public reexport. Its public and
  `pub(crate)` import visibility is structural HIR metadata, not evidence that
  HIR consumers enforce import accessibility. It has one associated type and
  one attribute-free method; the binary-heap impl
  at `library/alloc/src/collections/binary_heap/mod.rs:1680` under
  `cfg(not(test))` is its sibling. A second extracted fixture admits the exact
  Rust 1.90 `Future` declaration and `Ready<T>` impl signature. Custom receiver
  types may now follow references and single-argument nominal wrappers to the
  enclosing `Self`, so `mut self: Pin<&mut Self>` retains both its custom
  receiver shape and mutable body local. Alias normalization rechecks that
  root; an unrelated or alias-erased receiver hard-errors. This is a
  structural precondition, not proof of the eventual `Receiver` trait
  obligation. The fixture preserves upstream trait/associated/method/impl
  attributes and source provenance while intentionally minimizing surrounding
  definitions and the method body. The same fixture now admits the exact
  `Pending<T>` impl signature and `PhantomData<fn() -> T>` field. Bare `_`
  parameters are explicit nameless signature discards and do not create body
  locals; each real parameter local records its original signature index, so
  `Future::poll`'s `cx` and `Ready::poll`'s `_cx` remain named while
  `Pending::poll` has two signature positions and only the `self` body local.
  A third extracted fixture admits the exact Rust 1.90 `Try` declaration,
  including its ordered `~const FromResidual` supertrait edge, plus the
  `ControlFlow<B, C>` and `NeverShortCircuit<T>` `Try` impl signatures. It
  deliberately replaces the surrounding generic
  `FromResidual<R = <Self as Try>::Residual>` declaration with a nongeneric
  empty trait: this proves the selected `Try` declaration and impl headers,
  not generic-trait defaults. Structured trait supertraits are retained in
  source order as locally resolved named trait types with their bound spans
  and an exact required-versus-`~const` modifier. Reserved definitions support
  forward edges. HIR insertion prevents a reserved target from later binding
  as a non-trait or with an incompatible generic signature and rejects an edge
  that completes a cycle; final lowering also rejects unresolved/non-trait
  targets and duplicate direct edges. Reachability and final graph validation
  use explicit work stacks rather than recursion.
  A fourth extracted fixture admits the exact Rust 1.90 `IntoIterator`
  declaration, including the ordered required `Iterator` bound on `IntoIter`
  and its resolved `Item = Self::Item` equality, and the exact `Deref`
  declaration, including its `PointeeSized` supertrait and relaxed
  `type Target: ?Sized` bound. Associated-type bounds and equality bindings
  retain source order, spans, resolved trait/associated identities, and the
  enclosing trait as the `Self` owner. Only local nongeneric bound traits,
  nongeneric type equalities, and the exact relaxed `?Sized` form are admitted;
  defaults, GATs, positional arguments, other relaxed bounds, and semantic
  obligation solving hard-error. Canonical dumps expose supertraits and these
  records as `hir-v15`. This is structural representation only: supertrait or
  associated-bound obligations, conditional-const selection, unsizing, and
  the complete upstream `try_trait.rs` are not implemented or claimed. The
  omitted generic `FromResidual` declaration is now admitted by registering
  targetless associated types before trait generic defaults while preserving
  later projection, cycle, and transactional rollback validation. The fifth
  extracted fixture preserves the exact Rust 1.90 attributes and
  signatures of 68 `Iterator` methods. Trait and trait-method type parameters,
  ordered inline-plus-where predicates, positional trait arguments,
  associated equalities, shorthand `T::Assoc`, generic supertrait arguments,
  and trailing type defaults such as `PartialOrd<Rhs = Self>` are structural.
  Callable notation canonicalizes `FnMut(A, B) -> R` to
  `FnMut<(A, B), Output = R>`; a parser-only provenance marker admits its
  one-input tuple without broadening the deliberately ambiguous `(T)`/`(T,)`
  source-tuple slice. Predicate equalities retain resolved inherited associated
  definitions, values, order, and exact spans in deterministic `hir-v15` dumps.
  Required versus default body presence is retained, while default bodies are
  deliberately minimized because expression lowering is not claimed. The nine
  excluded methods are `next_chunk`, `map_windows`, `array_chunks`,
  `partition_in_place`, `copied`, `cloned`, `try_collect`, `try_reduce`, and
  `try_find`; they require const/lifetime method generics or nested associated
  constraints and `impl Trait`. Predicate storage does not implement obligation
  solving, method applicability, or dispatch. These are declaration and
  graph-admission facts, not evidence of expression semantics,
  method lookup/dispatch, code generation, or selection.
  M4-02 remains `ACTIVE`, and this focused checkpoint does not satisfy its
  selected-impl oracle gate or the Rust-core acceptance gate.
- M3-02 now also has parser and model evidence for named struct expressions.
  Explicit, shorthand, empty, and final `..base` syntax is retained, with
  control-head brace disambiguation and deterministic update-base dumps.
  Unsafe blocks retain an explicit AST marker instead of being stolen as a
  keyword-named struct. A
  byte-identical Rust 1.90 `library/core/src/future/pending.rs` fixture parses.
  HIR `hir-v15` structurally authenticates complete constructions of bound
  local nongeneric named structs: every source-ordered field carries its exact
  declaration ordinal, typed body-owned value, and nested span; insertion
  validates complete unique coverage before allocation and deep-copies the
  records. Source-qualified body lowering now accepts those same complete
  local constructions, authenticates direct, qualified, and imported-alias
  paths against the exact resolver/graph/module-map revision, enforces item
  and field visibility, and recursively types heterogeneous or nested field
  values. Reordered fields remain in source order; shorthand and empty named
  structs preserve exact spans. Calls and aggregate fields share one
  preflight-sized transaction allocation with exactly one owner, and stale
  resolver snapshots, malformed coverage, unsupported update/generic/private
  construction, or allocation failure publish no body, local, expression,
  arena, or interner mutation. Named `.field` syntax retains exact base, name,
  and full spans while numeric tuple projection rejects explicitly. Graph HIR
  materializes a nominal self type for every local nongeneric named struct;
  body lowering uses it to authenticate field reads from parameters, fresh
  reordered constructions, and nested projection chains. Each `FIELD` node
  records the exact base, aggregate DefId, declaration ordinal, and declared
  result type. Wrong base/type/name/visibility, stale resolver identity, and
  cyclic syntax reject transactionally. Struct update, generic or cross-crate
  construction, MIR places, and code generation remain explicit open work.
  A checked layout query now covers i32/u32 leaves and recursively nested
  same-crate local nongeneric named structs. It computes declaration-order
  offsets under explicit 32- and 64-bit address models with checked arithmetic
  and atomic outputs, while empty, generic, tuple, union, represented,
  recursive, cross-crate, unsupported-leaf, malformed, or capacity-limited
  requests reject. Strict GCC, TinyCC 0.9.27, and focused ASan/UBSan/LSan gates
  pass. This is not aggregate ABI or MIR/codegen admission and does not close
  the graph-wide M3-02 or M5-04 gates.
  A strict whole-file census over every `.rs` file below pinned Rust 1.90
  `library/core` and `library/alloc` (334 files, without cfg or test pruning)
  moves from 23 successes at `b3e04694` to 41, with no previously successful
  file regressing. This is parse acceptance only, not graph or semantic
  admission.

The current front-end checkpoint passes `make -B -C cmrustc check-all` under
strict C99 GCC and TinyCC 0.9.27. The same complete test target passes GCC
ASan/UBSan with leak detection enabled. These gates establish regression and
portability evidence only; they do not close any corpus-wide milestone above.

The first bounded executable checkpoint crosses the previously absent semantic
pipeline with one deliberately exact program shape:

```text
source graph -> declaration HIR -> typed integer/block HIR -> MIR
             -> portable C -> TinyCC executable -> observed exit status
```

The accepted crate has exactly the three `feature(no_core)`, `no_core`, and
`no_main` inner attributes and exactly one public, nongeneric, safe,
non-variadic `#[no_mangle] extern "C" fn main() -> i32`. Its body is one
suffixed in-range `i32` literal. Body lowering authenticates the exact graph
revision and HIR owner through the module map, then retains source-qualified
block and integer expressions; MIR contains one return local, one constant
assignment, and `RETURN`; the C emitter validates the whole crate envelope and
emits a guarded `int32_t` implementation. The driver finishes all semantic
work in memory and atomically publishes through a same-directory temporary.
Input/output inode aliases hard-error, and rejection preserves a preexisting
output.

The GCC and TinyCC executable gates require nonempty C and executable
artifacts, no output, and exit status 7. Exact-envelope tests also reject an
unsupported binary-expression body and an additional exported function
without changing a sentinel output. This is evidence for the first vertical
slice only.

The second bounded executable checkpoint adds typed named-local reads,
resolved free-function calls with ordered substitutions and arguments, exact
u32 monomorphized MIR instances, move operands, direct call terminators,
explicit public no-mangle roots, recursive callee-before-caller reachability,
and emission of every admitted reachable instance. Its C harness proves four
u32 values through a real emitted `identity::<u32>` function and `probe` call
under both GCC and TinyCC. Rejection tests cover unresolved, missing, and
unsupported substitutions plus unsupported exported roots; a private
unreachable unsupported function compiles but is absent from the artifact.
HIR and MIR dumps are `hir-v12` and `mir-v3`. This remains a deliberately
bounded identity/reachability slice: general expressions, control flow,
matches, generic multi-crate reachability, layout, and a full link driver are
still absent, so M5-01, M5-03, M5-05, and M5-07 remain `TODO`.

The third bounded executable checkpoint adds body-owned typed u32 literals and
recursive HIR add trees, explicit MIR `USE`/`BINARY` rvalues, distinct i32/u32
constant storage, and exact one- or two-argument u32 instances. The portable C
emitter now preserves a two-parameter public `add(left, right)` ABI and emits
real unsigned addition. GCC and TinyCC harnesses prove ordinary values,
`0x7fffffff + 1`, `UINT32_MAX + 1 == 0`, `UINT32_MAX + UINT32_MAX`, and a
nontrivial wrap. Unsupported operators, mismatched suffixes, and an unsupported
second exported root preserve a sentinel or publish no artifact; the same
unsupported private body is omitted. GCC- and TCC-built compilers emit
byte-identical arithmetic and identity C. The later nested checkpoint advances
the MIR schema to `mir-v4`; general operators, control flow, and the
corpus-wide milestone gates remain open, so no M5 task is closed by this
focused evidence.

The fourth bounded executable checkpoint lowers recursive u32-add trees in
deterministic postorder. Every non-root binary value owns one typed temporary
local assigned exactly once before use; the root alone assigns `_0`. MIR model
validation authenticates the complete statement tree against HIR and rejects
wrong ordering, destinations, operands, types, duplicate or unused temporary
storage, and cyclic HIR. The C emitter declares each temporary and emits the
validated assignments in MIR order with explicit `uint32_t` casts. The nested
canary `left + (1u32 + right)` requires a real `_3` temporary and two ordered
additions, and its executable harness covers ordinary and wrapping cases. A
reachable inner subtraction preserves a sentinel or publishes no artifact.
The complete strict GCC and TinyCC matrices and the GCC ASan/UBSan/leak matrix
pass; GCC- and TCC-built compilers emit byte-identical nested C. Canonical
dumps are `hir-v12` and `mir-v4`. Other operators, non-u32 trees, general
statements/control flow, and all corpus-wide M5 gates remain open.

The fifth bounded executable checkpoint composes recursive arithmetic with
resolved reachability: `identity::<u32>(left + (1u32 + right))`. Source-backed
HIR transactionally owns the complete call-argument tree. MIR emits both
postorder argument temporaries in the call block, then passes the final
temporary to the already-published exact identity instance; the continuation
block returns the call result. Model validation consumes the exact HIR tree,
all temporary locals and statements, and the call operand together, rejecting
reordered, fabricated, missing, unused, cyclic, or wrong-callee shapes. The C
emitter preserves that order and performs the real call with the computed
temporary. The executable gate covers ordinary and wrapping cases, while an
inner subtraction preserves a sentinel or publishes no artifact. This remains
a bounded single-call/single-argument slice. The complete strict GCC, TinyCC,
and GCC ASan/UBSan/leak matrices pass, and GCC- and TCC-built compilers emit
byte-identical computed-call C. General expression nesting, control flow, and
the corpus-wide M5 gates remain open.

The sixth bounded executable checkpoint admits an ordinary monomorphic u32
callee with one or two parameters and zero substitutions. Calls preflight and
own each argument independently, lower recursive add trees left-to-right
through one shared postorder temporary sequence, retain every ordered MIR call
operand, and recursively publish the exact callee instance before its caller.
The canary `add_pair(left + 1u32, right + 2u32)` emits two distinct temporary
assignments followed by a real two-argument call. Its GCC and TinyCC harnesses
prove `left + right + 3` modulo 2^32. Swapped, missing, extra, mistyped,
misordered, cyclic, fabricated, or wrong-callee MIR shapes fail exact model
validation; an unsupported second argument preserves a sentinel or publishes
no artifact. The generic identity path and prior arithmetic canaries remain
supported. This is still a bounded call-expression slice; general statements,
control flow, methods, ABI/layout, multi-crate reachability, and all corpus-wide
M5 gates remain open.

The seventh bounded executable checkpoint composes call results within the
admitted u32 expression tree. HIR preflights literals, locals, additions, and
calls recursively, adopts every nested call slice in one allocation, and rolls
the whole body back on rejection or OOM. Driver reachability walks the complete
tree and lowers every callee before its caller. MIR preserves left-to-right
evaluation as a linear CFG: each call terminates its current block, a nested
result owns the next temporary, and evaluation resumes in the immediately
following block. Exact model validation authenticates the full HIR tree against
all statements, calls, destinations, instance keys, operands, targets, blocks,
and temporary uses. The canaries prove both
`add_pair(add_pair(left + 1u32, right + 2u32), left + 3u32)` and
`add_pair(left + 1u32, right + 2u32) + (left + 3u32)` execute as
`2 * left + right + 6` modulo 2^32. A reachable subtraction rejects the whole
crate while preserving a sentinel or publishing nothing. Full strict GCC,
TinyCC 0.9.27, and GCC ASan/UBSan/leak matrices pass; GCC- and TCC-built
compilers emit an identical 1,102-byte artifact with SHA-256
`3121d75d53590e428025b810cc83c1e794017eac13ff2fb9fe57f8492d232e23`.
General statements, control flow, methods, ABI/layout, multi-crate reachability,
and every corpus-wide M5 gate remain open.

The eighth bounded executable checkpoint adds corpus-driven immutable local
bindings. The pinned Rust 1.90 core/alloc cache contains 4,127 `let`
declarations across
1,750 recognized function bodies. Of 2,549 immutable identifier bindings with
an inferred type, initializer, and no `let`-else, 296 have an initializer whose
syntax is composed only of identifiers, integer literals, addition, free calls,
and parentheses. A stricter whole-body syntax proxy admits only two additional
core/alloc bodies when those lets are added to the current expression
vocabulary, before type or resolution checks. The slice is selected to build a
durable statement/local sequencing spine, not as an immediate corpus-coverage
claim. The first admitted boundary remains narrower and explicit:
top-level immutable identifier bindings with an exact written `u32` type,
required initializer, no shadowing, and the already authenticated u32
literal/local/add/direct-call expression tree. HIR owns a flat ordered
statement array rather than recursively nesting `let` expressions, and
preflights the complete block before transactionally extending the body local
table. Visible-prefix lookup prevents self and forward reads. MIR places user
locals after arguments and before generated temporaries, lowers each
initializer directly into its user-local destination, and publicly revalidates
the stored body by replaying every statement, call, destination, block,
temporary, and tail against HIR before C publication. Reachability walks let
initializers before the tail and keeps its current expression stable across
callee publication, which can reallocate HIR storage.

The executable canary evaluates a call-bearing initializer and later tail as
`2 * left + right + 6` modulo 2^32. A reachable subtraction in an initializer
preserves an existing sentinel and publishes nothing at a fresh path, while an
unsupported private unreachable body remains omitted. Full strict GCC,
TinyCC 0.9.27, and GCC ASan/UBSan/leak matrices pass. GCC- and TCC-built
compilers emit an identical 689-byte artifact with SHA-256
`944b2e203c93278c1f8d45b3f8632894d29807a1637e31590ff5b4fb0ae9299c`.
Canonical dumps are `hir-v15` and `mir-v6`. Mutation, destructuring, type
inference, shadowing, nested statement blocks, general control flow, and every
corpus-wide M5 gate remain open, so this checkpoint does not change a
milestone state.

The bounded aggregate MIR checkpoint represents a local or temporary place as
an exact type/span plus up to 16 authenticated DefId/ordinal projections.
Direct bodies evaluate aggregate initializers in source order, normalize the
stored rvalue to declaration order, retain actual nominal temporary types, and
flatten nested reads. Scalar i32/u32 fields use `COPY_PLACE`; aggregate-valued
fields use `MOVE_PLACE`. Stored MIR owns deep copies of every field and
projection slice and rejects malformed coverage, identities, types, spans, or
chains transactionally. Focused strict GCC, TinyCC 0.9.27, and GCC
ASan/UBSan/leak gates pass. Aggregate calls, projected writes, exported
aggregate ABI, and C emission remain open, so M5-01 is active but not complete.

The first aggregate C99 checkpoint integrates checked layout with reachable
MIR. It discovers the complete nominal closure before output, emits
dependency-first ordinal-named structs with C99 `sizeof`, `offsetof`, and
alignment proofs, lowers declaration-ordered aggregate rvalues to designated
compound literals, and emits flattened ordinal member reads. A nested,
reordered executable canary runs under strict GCC and TinyCC 0.9.27 and checks
deterministic output plus atomic rejection of aggregate exported ABI,
represented layout, and aggregate calls. GCC- and TCC-built compilers emit the
same 2,031-byte aggregate artifact with SHA-256
`5c5c5363db4d2f711304a1d8bf74a5ff1860eb85bbcde1521725b5e103e26faf`.
Exported signatures and calls remain scalar-u32, and projected writes remain
rejected, so M5-05 is active rather than complete.

The next corpus-driven arithmetic checkpoint adds exact wrapping u32
subtraction without broadening the integer or operator boundary. A syntax-only
body census over pinned Rust 1.90 `library` covers 334 files and 6,803 function
bodies; its test-pruned proxy covers 327 files and 5,674 bodies. Method calls
remain the largest unsupported family (7,803 occurrences across 3,335 proxy
bodies), followed by non-add binary operators (2,909 across 1,051), unsafe
blocks (1,667 across 1,372), `if` (1,529 across 880), and references (1,437
across 1,000). The non-add operator counts are `==` 435, `-` 433, `<` 319,
`>` 215, `&` 172, `&&` 172, `!=` 168, `*` 164, `<=` 152, `>=` 147, `/` 138,
`||` 108, `>>` 77, `|` 76, `<<` 64, `%` 54, and `^` 15. Subtraction was
selected first because it reuses the authenticated typed-u32 expression path
without pretending that bool or control-flow semantics exist.

HIR and MIR retain distinct add/subtract operators and validate their exact
correspondence; portable C emits `(uint32_t)(left - right)`. The executable
gate proves ordinary subtraction, underflow, both `UINT32_MAX` boundary
directions, deterministic emission, ordered let assignment, and atomic
rejection of multiplication and signed subtraction. The complete strict GCC
and TinyCC 0.9.27 matrices and the GCC ASan/UBSan/leak matrix pass. GCC- and
TCC-built compilers emit an identical 349-byte artifact with SHA-256
`a17532521ea7b06e4f1cc3d8b94104c0b4f082c99b47c088e65ef4e75ed5d375`.
Every broader operator, bool/control-flow behavior, and corpus-wide M5 gate
remains open, so this checkpoint changes no milestone state.

The following aggregate-call checkpoint closes the narrow internal call gap
identified after the aggregate C99 slice. The Rust 1.90 library syntax proxy
contains 397 function tails that are struct expressions and 90 calls with a
direct struct-expression argument. Source-backed HIR now types each argument
of a one- or two-parameter monomorphic call from its exact signature. Each may
be u32 or a checked same-crate nongeneric named struct, including mixed
`(Outer, u32) -> u32` calls. Aggregate arguments require an internal Rust-ABI
callee; the existing generic identity path stays u32-only. Aggregate returns,
generic/cross-crate aggregate calls, and exported aggregate ABI still reject.

MIR materializes every aggregate argument before its call terminator, retains
exact `MOVE` or `MOVE_PLACE` operands, and validates the callee parameter type,
monomorphic instance, same-crate identity, projection chain, and scalar-u32
result by replaying HIR. Stored operands and projections are deeply owned;
corruption tests cover types, kinds, ordinals, and aggregate-return mismatch.
Portable C emits checked ordinal-named structs by value only in internal static
signatures while exported declarations remain scalar-u32-only. The executable
canary constructs a reordered nested aggregate, passes it with a separately
materialized u32 argument, reads a nested field in the callee, and observes the
real result under both compilers. The complete strict GCC/TinyCC 0.9.27 and GCC
ASan/UBSan/leak matrices pass. GCC- and TCC-built compilers emit an identical
2,059-byte artifact with SHA-256
`e422dd6a827d93e5dd74d02a977bb8f860a8398ea1981f9f064d29f11f36c59e`.
Projected writes, aggregate returns/exports, broader call resolution, and every
corpus-wide M5 gate remain open, so M5-01 and M5-05 stay active.

The first conditional checkpoint adds the smallest corpus-backed value CFG.
The pinned Rust 1.90 library syntax proxy contains 1,529 `if` expressions in
880 test-pruned functions and 435 `==` operators in 329. Root equality-if
forms occur in 212 expressions across 175 functions; 115 expressions across
98 functions include `else`. A strict call-free current-shape proxy leaves
nine functions, with an upstream u32-aligned anchor at
`library/core/src/num/f32.rs:765`.

The admitted boundary is exactly root
`if left == right { then_u32 } else { else_u32 }`: operands are u32, both
statement-free block tails are u32, and each arm may use the authenticated
local/literal/add/subtract tree. HIR creates or reuses one canonical bool type
transactionally. MIR `mir-v7` stores equality as a distinct bool-producing
rvalue and replays one four-block switch/true/false/join diamond, including
every operand, destination, statement, edge, and span. Portable C materializes
the internal bool as exact `uint8_t` 0 or 1 and exposes no bool ABI. Nested
conditionals, calls in the condition or arms, if-let, else-if, missing else,
`!=`, signed or aggregate equality, bool parameters/returns, and every other
reachable graph reject atomically.

The executable canary covers ordinary equality, unequal subtraction,
equal-path overflow, and unequal-path underflow. The complete strict GCC,
TinyCC 0.9.27, and GCC ASan/UBSan/leak matrices pass. GCC- and TCC-built
compilers emit an identical 493-byte artifact with SHA-256
`e0dce3f5cd322c89f64787a28a307095de2f27d8c8791cc5f01233e81d9275d7`.
The corpus-wide M5 gates remain open, so M5-01 and M5-05 stay active.

The target-width scalar checkpoint adds the first exact `usize` execution
spine. A pinned Rust 1.90 library census finds 691 functions with exact
`usize` parameters, 246 with exact `usize` returns, and 13 explicit
`let name: usize` declarations. The smallest production body already matching
the bounded source shape is `const_min` at
`library/core/src/slice/rotate.rs:275`:

```rust
const fn const_min(left: usize, right: usize) -> usize {
    if right < left { right } else { left }
}
```

HIR remains target-neutral and owns exact decimal `usize` literals through
`UINT64_MAX`, exact parameters/returns and immutable lets, wrapping `+`/`-`,
one- or two-argument same-module monomorphic calls, exact `usize < usize`, and
the corresponding root value diamond. MIR is target-qualified before
publication with an immutable 32- or 64-bit pointer width, stores `usize`
constants in `uint64_t`, rejects 32-bit overflow, and advances the canonical
dump to `mir-v9`. The legacy width-zero MIR path remains valid only for graphs
with no reachable `usize`; generic identity substitution remains u32-only.

Canonical callable instance format v2 and MIR `mir-v9` separately retain the
selected dispatch/symbol definition and `body_definition`, the definition
whose signature and HIR body execute. Semantic results, admission, reachable
MIR, lowering, dumps, and C emission authenticate both identities. Qualified
receiver-free calls can now inherit a nongeneric trait default through a
concrete local impl. Such an instance deliberately keeps
`selected_callable == body_definition == declared_trait_callable`: the
concrete `selected_impl`, `self_owner`, and `self_type` supply dispatch
identity, and the complete canonical key therefore gives two impls sharing
one default body distinct MIR and C symbols. No synthetic impl-method DefId is
created.

Portable C maps Rust `usize` to `uintptr_t`, emits explicit
`(uintptr_t)UINT32_C(...)` or `(uintptr_t)UINT64_C(...)` constants, and adds
compile-time `UINTPTR_MAX` and `sizeof(uintptr_t)` target proofs. The executable
canary combines the production `const_min` diamond, explicit lets, wrapping
subtraction/addition, two real internal calls, and `4294967301usize`. Atomic
negative gates cover i686 overflow, mixed u32/usize ABI, aggregate source-call
boundaries, context-free or malformed literals, generic usize substitution,
and target/MIR/host width mismatch. The complete strict GCC, TinyCC 0.9.27, and
GCC ASan/UBSan/leak matrices pass. GCC- and TCC-built compilers emit identical
artifacts for both target widths: the x86-64 executable canary is 1,277 bytes
with SHA-256
`0260d79f545b2b2646f9f458166e04ead626e9cae78ca8da2e60f14e3e09b90f`;
the width-only fixtures are 869 bytes on x86-64
(`ff03a288fd5dee04948f0834877d2321089d870e132647482181194f59bb6f8d`)
and 737 bytes on i686
(`599b085f5cff06ba1b1beb4f67d9b2619f3ba10ff3e2286f7f31762024a7e9f4`).
Inference, mixed-width operations, methods, casts, division, shifts, bool ABI,
aggregate usize source calls, and every corpus-wide M5 gate remain open, so
M5-01, M5-04, and M5-05 stay active.

The authenticated Rust 1.90 `library/core` module graph now completes with
293 loaded source files, 378 modules, and zero errors under the empty cfg gate.
Context-aware impl-member reparsing clears both
`zip_impl_general_defaults!` calls; parser-validated `ty` fragment boundaries
and transactional repetition-index rollback clear the adjacent generic types
generated by `integer_sum_product!`. A deliberately bounded parameter-style
declarative path admits only private, same-AST, definition-before-use,
unqualified parenthesized source invocations by translating their stored
parameter/body pair into one rules arm. This clears `impl_atomic_primitive!`
without claiming imported, public, qualified, forward, generated, or
cross-module Macro 2.0 scope and hygiene. Bounded `${concat(...)}`,
`${ignore(...)}`, `${index()}`, `${count(...)}`, and same-crate `$crate`
transcription clear the later `fmt`, tuple, and portable-SIMD graph gates.
Trait-associated macro invocations and full associated-item grammar validation
remain unsupported. This is graph construction, not HIR lowering, type
checking, library metadata, or executable core support.

The canonical x86_64 target-cfg graph separately loads all 363 reachable Rust
1.90 `core` sources and constructs 451 modules without an error.
Outer attributes on `let` statements retain ordered statement-owned IDs and
spans instead of being misparsed as let-condition expressions; block-local
`static` declarations are ordinary item statements and never enter module
namespaces or effective roots. HIR body lowering validates attributed-let
storage and rejects it until statement-attribute semantics exist. Bare cfg
predicates test key presence, so value-bearing target atomic cfg facts also
activate their guarded macro declarations. This is a successful
target-configured graph, not a HIR, type-checking, library metadata, or
executable core claim.

`extern crate self as core` now publishes the alias, rather than the reserved
source name, as a type-namespace binding to the crate-root module. The import
resolver exposes that binding through its extern-prelude fallback in every
module. This clears 49 target-configured core import failures. A graph-owned
cfg-filtered enum variant view then clears all 38 remaining unresolved
imports; the exact frontier is now 30 errors: 0 unresolved, 13 ambiguous, and
17 cyclic. HIR still rejects before mutation while any import diagnostic
remains.

Enum variants retain ordered outer attribute IDs and exact spans instead of
discarding attributes during parsing. The module graph applies the same
cfg/cfg_attr processor used by items and publishes only active variants, with
their exact enum declaration and original zero-based AST index. Enum self,
explicit variant, alias, glob, and checked-path lookup preserve that identity;
all variants occupy the type namespace, while unit and tuple constructors also
occupy the value namespace. HIR rejects variant import bindings until its
canonical import schema gains a distinct variant target instead of pointing at
the parent enum definition.

The authenticated Rust 1.90 `library/alloc` empty-cfg graph now completes with
all 62 source files, 67 modules, and zero errors when supplied the successful
core dependency artifact. Generic parameters retain ordered outer attributes
and exact declaration provenance, clearing `#[may_dangle]` on `RawVec`'s
`Drop` impl; HIR rejects those attributes explicitly until their semantics
exist. Unicode character literals distinguish a bounded UTF-8 scalar such as
`'Σ'` from a Unicode lifetime without embedding Unicode tables in the
TinyCC-compatible lexer. All twelve `core::bstr` imports now resolve through
exact consumer use leaves. Nested zero-width repetition choices backtrack
without becoming global syntax errors, a consumed trailing separator counts
as progress, and type-position macro invocations retain their path, delimiter,
arguments, and span. Those last two syntax fixes clear alloc's generated
`to_string_str!` implementations without pretending type macros are already
expanded for HIR.

The first cross-crate hygiene substrate retains `$crate` as a distinct
transcriber node and accepts only one explicit ASCII crate identifier from an
exact resolver-certified binding. Planner bindings carry that borrowed
defining-crate identity through recursive generated invocations, so an
imported definition using `$crate` cannot silently target the consuming crate.
Local and legacy-null bindings remain exactly `crate`; path-like guesses such
as `core::guessed` reject.

A live dependency-macro artifact binds one successful dependency graph, its
exact revision, an extern name, and a distinct `$crate` identifier. Consumer
graph options accept a bounded artifact array only for the synchronous build.
Each unqualified source invocation must have one exact unresolved external
use leaf; local collisions, dependency-rooted globs, private paths, competing
leaves/artifacts, stale/null/self artifacts, and duplicate extern or hygienic
crate identifiers reject. Unrelated globs do not block an otherwise exact
certificate. Source-written hygienic paths remain unauthorized.

The planner uses graph-local 64-bit owners above the source-ID range for
dependency ASTs. Generated-only lookup asks the registered artifact on demand
for the authenticated hygienic path, so an imported outer macro can emit a
different `$crate::module::helper!` without enumerating guessed public macros.
Private and ambiguous generated paths retain fail-closed classifications.
When final effective items are published, high owners become a separate
dependency reference; dependency source IDs never enter the consumer's local
macro-definition field.

That reference carries the exact consumer graph/generation, a per-definition
certificate, the consumer-local dependency ID, dependency revision, and
dependency-scoped declaration. A revision-checked accessor validates only
definitions present in the final published registry, not abandoned staging
rounds. The result is a consumer-owned snapshot, so validation does not require
the dependency graph or temporary artifact to remain live. Field splices,
cross-graph references, prior consumer generations, failed rebuilds, and
uncertified declarations reject. The graph also retains the exact consumer
module, source-qualified use declaration, local alias, invocation AST owner,
and producing dependency definition for each published external expansion.
HIR may therefore treat only that leaf's otherwise-unresolved macro import as
consumed; grouped sibling errors and unrelated imports still reject before
mutation. Supported body-free declarations and authenticated root consts
generated into the consumer AST now lower after both the import leaf and
dependency certificate validate, even after the producer graph and artifact
are destroyed. No-op dependency macros remain conservatively unresolved
because they publish no generated syntax.

The first HIR library artifact snapshots one successfully lowered producer's
public module/type namespace from its exact graph revision and HIR module map.
It copies public module edges plus same-crate traits, ADTs, type aliases,
extern types, and type-namespace reexports; producer graph, AST, import
resolver, and map lifetimes therefore do not leak into consumers. The shared
HIR context remains the owner of producer DefIds and must stay live. A
consumer lowering call may borrow multiple artifacts with unique extern names
and resolve explicit paths such as `dep::api::Type` or `dep::Trait`; local
definitions win, producer aliases normalize through their loaded HIR, and
private paths or duplicate extern identities reject transactionally. One exact
unresolved, non-glob external type-namespace leaf may be authenticated against
an artifact and retained as a structural HIR binding to a producer type,
trait, or module DefId. Aliases and grouped leaves preserve source order, and
a qualified path may continue below an imported module alias. Producer-owned
traits now work in impl headers, supertraits and generic bounds, associated
bounds, and qualified projections; generic signatures and associated
declarations are read from bound producer HIR rather than fabricated local
records. A private sibling, local collision, competing leaf/artifact, glob, or
unrelated resolver error rejects the whole transaction. Values, external
globs, transitive external reexports, standalone/serialized metadata, and
executable alloc remain the next library-artifact boundary.

M3-04 now has transaction, restoration, and format foundations plus one
narrow semantic round trip. Public
HIR-context marks capture every append-only model vector plus its interner and
arena;
rewind releases expression-owned heap storage before truncation, commit keeps
the appended graph, and resolving an outer mark safely invalidates nested
marks. A graph-independent owned namespace representation validates a supplied
same-context crate/root and every module/entry DefId, then moves into an
artifact only after all checks; failed replacement preserves both the prior
artifact and the exact candidate allocations. The initial `cmhir-meta-v1`
codec now emits and validates a deterministic 40-byte versioned envelope,
64-MiB-bounded little-endian section frames, and IEEE CRC32 without zlib.
The semantic layer now uses six fixed-order sections: `CRAT`, `MODS`, `GPAR`,
`TYPE`, `ITEM`, and `NSPC`. It encodes crate name/edition, a root-connected
module tree, public extern types, structs, unions, enums and variants, free
type aliases, owner-grouped lifetime/type generics and type defaults,
supported structural types and scalar constants, module/type aliases and
reexports, and builtin primitive bindings. One-based
file-local module/item handles replace runtime IDs; pointers, intern IDs,
DefIds, and source IDs never enter the file. Decode parses and validates the
complete wire payload before mutation, creates fresh runtime identities under
a `CmHirContextMark`, restores owner-contiguous generics/defaults and
child-first structural types, binds ADT/alias payloads, and restores the
namespace over remapped DefIds before commit and atomic artifact replacement.
Failure destroys the candidate and rewinds the context. Tests cover a producer
runtime
crate ID of two loading as consumer ID one, producer destruction before load,
a zero-item primitive-only artifact, CRC and truncation damage, and a valid-CRC
namespace conflict that reaches restoration before proving exact
context/artifact rollback. Encoding now canonicalizes modules by full
declaration path, items by owner path and declared name, and namespace
entries by owner path, exported name, wire kind, and canonical target,
remapping all one-based handles after sorting. Two fresh producer processes
deliberately reverse module/item creation and owned module/namespace insertion
yet write byte-identical nonempty files; a fresh prepopulated consumer then
proves nonzero runtime-ID remapping and lookup.

The expanded fixture now starts from parsed Rust and runs through the real
module graph, import resolver, HIR lowerer, public library-artifact capture,
and metadata encoder. It round-trips `Wrapper<T = u32>`, generic enum
`Choice<T>`, a generic union, lifetime-bearing references, structural tuple,
pointer, and literal-array types, `Alias = Wrapper<u32>`, fields/variants,
primitives, and child-module reexports. Forward/reverse parsed producers remain
byte-identical after source-side graph/import state is destroyed; a fresh
prepopulated consumer proves ID remapping, alias normalization, and exact
rollback for prevalidation and post-mark semantic corruption. Lifetime generic
names such as `'a` are validated without broadening item or module identifiers.
Traits, associated declarations, projections, impls, bodies, const-generic
arguments/defaults, unevaluated constants, and dependency-backed type edges
remain mandatory
before M3-04's acceptance is met, so M3-04 stays `ACTIVE`. The private driver
now provides `--emit-cmhir SOURCE --crate-name NAME -o FILE` and repeated
`--extern-cmhir NAME FILE`. Separate processes prove parsed-source production,
atomic publication, fresh-ID dependent loading, external ADT resolution, and
dependent republication. These are still raw `cmhir` declarations rather than
`.rmeta`/`.rlib`, object/archive production, or link orchestration. Values,
macros, native links, bodies, and dependency graphs remain later work. The
complete strict GCC and TinyCC
matrices plus a focused GCC ASan/UBSan/leak run pass with this narrow layer
linked into the compiler.

The earlier `pub macro assert_matches` syntax gate in `macros/mod.rs` is also
cleared by opaque, declaration-preserving parsing. Later
front-end/library-artifact gates include HIR handling for the retained
self-crate alias plus real `.rmeta`/`.rlib` production. Once source
reachability is honest, the largest measured backend lever is unsuffixed
integers (18,102 occurrences across 5,605 pinned library bodies). The first
exact contextual slice now handles bare decimal `i32`, `u32`, and `usize`
literals in exact return, explicit-let, aggregate-field, branch/operator, and
direct-call contexts. It deliberately does not implement inference variables,
unification, or fallback defaulting. Implicit unit/zero-argument calls and named
aggregate returns remain subsequent high-frequency levers. None of those
counts changes a milestone state.

## M4: Types and semantic lowering

| ID | State | Task | Acceptance |
|---|---|---|---|
| M4-01 | ACTIVE | Type variables and constraint solver | Focused inference corpus passes |
| M4-02 | ACTIVE | Trait/impl and associated-type selection | Selected impl dump matches oracle |
| M4-03 | TODO | Coercions, unsizing, const generics/eval | Core/alloc semantic corpus passes |
| M4-04 | TODO | Closures, async/generators, vtables | Layout and call behavior matches oracle |
| M4-05 | TODO | Conservative lifetime/borrow policy | Bootstrap corpus accepted; no runtime erasures |

The semantic implementation order is fixed so declaration progress cannot
bypass proof obligations: immutable parameter environments; canonical,
binder-aware tabled goals; transactional generic impl replay; recursive
predicate evaluation; projection normalization; authenticated closed-universe
metadata; body constraint generation and adjustments; post-typecheck rewrites;
const evaluation; and conservative region/borrow acceptance. Solver results use
the explicit lattice `PROVEN`, `NEGATIVE`, `NO_SOLUTION`, `AMBIGUOUS`,
`DEFERRED_INFERENCE`, `DEFERRED_METADATA`, `UNSUPPORTED`, and `OVERFLOW`.
Unknown facts, ordinary cycles, unsupported candidates, and incomplete metadata
never become success. Only an explicitly modeled coinductive auto-trait cycle
may use coinduction, and negative candidates are considered before structural
auto-trait recursion.

The semantic-pass foundation now reaches its first source-facing inference
slice without advancing an M4 state. Type checking can transactionally bind
every still-unresolved canonical
integer variable to an exact HIR default such as `i32`, while leaving already
constrained roots and general variables untouched. Projection normalization
can publish an ordered, all-or-nothing trace containing the normalized input
projection, the proof-selected raw target, the recursively normalized target,
and the exact parameter-environment or impl/associated-item proof locator. The
semantic-session wrapper authenticates the same session and substitution
before exposing that trace; failed, stale, cyclic, or fuel-limited attempts
expose no partial evidence. Successful nonempty traces retain their exact
scratch-term owner, a process-unique owner lifetime, and the scratch-state
revision at publication. Rollback, variable rebinding, or destroy/reinitialize
therefore prevents coincident numeric IDs from becoming evidence for unrelated
terms. Production expression-type normalization now synchronously stages each
nonempty trace at the exact final, non-speculative decision that required it.
Semantic results durably deep-copy, group, rebase, seal, and query those records
for ordinary bodies and exact instances, keyed by body, expression, decision
kind, and decision index. Seal validation authenticates expression ownership,
unique keys, complete disjoint step ranges, every type endpoint and proof
locator, and equality between the trace result and the published unadjusted
expression type. Failed normalization, stale or foreign scratch evidence,
duplicate decisions, later checked-body failure, and transaction rollback
publish no partial trace. MIR does not yet consume projection traces, so they
are causal semantic evidence rather than an executable projection recipe.

MIR now has fail-closed schema support for tagged field/dereference place
projections and shared-borrow rvalues over validated places. Dereference steps
require a reference input and a canonical empty payload; shared borrows require
an erased immutable reference target whose pointee exactly matches the source
place. Storage sizing, deep copy, equality, replay scratch, and dumps preserve
the tags. HIR now has explicit body-owned shared-borrow and built-in-dereference
expressions with place, type, region, span, and ownership validation. Semantic
admission, reachable discovery, and semantic-results publication reject both
nodes explicitly until exact adjustment/place-use recipes exist; source body
lowering also remains closed. The C backend is ready for immutable erased
references: it emits const-pointer declarators, parenthesized tagged
dereference places, and shared-address rvalues while rejecting mutable, raw,
non-erased, and trait-deref forms. Authenticated HIR-to-MIR matching still has
no borrow/dereference cases. Focused MIR tests construct valid typed reference
HIR and prove that both a shared borrow and a borrow-then-dereference reject
transactionally without increasing the MIR body count. Aggregate reference
layout remains open, so this is representation/backend readiness rather than
executable source borrow support.

M4-01 now has a session-owned scratch type DAG for primitives, references,
pointers, tuples, arrays, slices, function pointers, ADTs, rigid parameters,
and projections. General, integer, and float inference variables use
rank-balanced union-find with deterministic canonical IDs, transactional undo
snapshots, iterative occurs checks, shared-pair structural unification, atomic
failure rollback, cached HIR import, and solved-only HIR freeze. HIR rewinds or
context reinitialization invalidate stale sessions, recursive import/unify/
freeze depth fails closed, and region identity is retained for output while
equality is deferred to later region obligations. Focused tests cover class
conflicts, rollback, deep/shared DAGs, stale HIR, and freeze atomicity; the
complete strict GCC, TinyCC, and GCC ASan/UBSan/leak matrices pass. Exact-owner
instantiation substitutes lifetime, type, and const arguments plus a separately
authenticated trait/impl `Self`; foreign parameters remain rigid, const values
are checked against their declared parameter types, shared HIR DAGs retain
sharing, and every mismatch or overflow rolls back atomically. The first
production body constraint slice assigns private general terms to immutable
unannotated simple lets, assigns integer terms to unsuffixed decimal literals,
imports exact suffixed and local types, and recursively propagates equality
through prior-local references plus primitive addition/subtraction trees and an
exact tail-return context. Arithmetic inference is limited to the existing
wrapping `u32`/`usize` slice when a concrete HIR type is already available;
literal/local-only roots may still default to `i32`, while signed fallback
arithmetic remains closed until overflow semantics are modeled. Only solved
integer kinds are staged before concrete HIR publication.
Scratch IDs never escape. Rejection performs no HIR rewind or semantic
mutation, preserving both HIR generations as well as every body, local, type,
expression, interner, and arena snapshot; the crate-wide outer mark remains
authoritative across multiple bodies. Calls, operators, branches,
aggregates, general unresolved variables, coercions, const inference, trait
obligations, associated-type normalization, method lookup, and diagnostics
still need full draft-based constraint generation, so M4-01 remains `ACTIVE`.

M4-02 now has an immutable deterministic impl-head index and a transactional
selector for positive ordinary impls. Type-only impl generics are instantiated
with fresh inference variables;
candidate trials roll back and a unique winner is recreated and committed.
Repeated and nested constrained parameters are supported, while unused,
lifetime, const, defaulted, or otherwise unauthenticated impl parameters block
explicitly. Stale or cross-HIR sessions reject, malformed named terms and named
regions do not become proofs, and shared DAG depth is height-checked. A narrow
predicate slice instantiates required, binder-free implemented-trait predicates
under the candidate's exact impl-owner substitution and solves them recursively
through the same canonical table. Candidate and predicate trials roll back;
ordinary re-entry is ambiguous, cycle-tainted non-proofs are not cached, and an
unproved predicate blocks an otherwise structural winner. Auto traits, negative
impls, projection predicates, higher-ranked/outlives predicates, and other
unsupported forms block rather than being skipped. The metadata universe is
deliberately open: absence always
returns `DEFERRED_METADATA`, because HIR and `cmhir-meta-v1` do not yet provide
an authenticated complete impl universe. Immutable owner-authenticated
parameter environments now collect exact and enclosing predicates, outlives
facts, trait `Self`, supertraits, and positive impl headers from one exact HIR
snapshot. Facts materialize transactionally under separate exact/enclosing
substitutions; higher-ranked, projection, modifier, outlives, recursive,
mixed-owner, foreign-owner, and overflow cases remain explicit pending or
blocking outcomes. Environment assumptions are tried before the impl index and
a winning proof is replayed atomically. The first immutable goal table keys
implemented-trait goals structurally across the goal, binder universe/de Bruijn
identity, and exact/enclosing substitutions. Type and lifetime inference
variables are alpha-canonicalized by first occurrence; iterative tri-color DAG
walking bounds depth/nodes, rejects malformed cycles, and makes shared versus
duplicated structure equivalent. Deterministic non-proof outcomes may be
cached, ordinary evaluating re-entry is ambiguity rather than proof, and
PROVEN is deliberately replayed through the transactional solver. A
generation-bound semantic session now composes one index, exact/enclosing
parameter environment, canonical table, and type-check context. The bounded C
emission path publishes its complete reachable HIR closure, creates one such
session per caller owner, and proves every supported generic call predicate
before constructing any MIR; every non-proof blocks output atomically.
Projection normalization, broader predicate forms, semantic metadata, and
general body-constraint integration remain required, so M4-02 stays `ACTIVE`.

A whole-local-crate semantic-item barrier now runs after complete reachable HIR
publication and before per-owner semantic sessions or MIR. For supported local
positive impls it authenticates trait and associated-item DefIds, instantiates
concrete trait type arguments plus `Self`, requires targetless associated types
and required methods, permits omitted default methods, and checks supplied
method receiver/arity/types/return/ABI/safety/const/async/variadic shape. Rigid
or foreign parameters cannot enter an impl header, associated target, or
nongeneric impl method. Generic impls, GATs, generic methods, higher-ranked,
outlives, predicate-bearing, associated-bound, projection,
associated-type-default, negative, auto-trait, cross-crate, and unsupported
associated-item shapes block with an explicit status; specialization remains
an explicit earlier HIR-lowering rejection. Focused strict GCC, TinyCC,
ASan/UBSan/leak, and end-to-end atomic-output tests pass, as do the complete
strict GCC and TinyCC suites. This closes only the supported item-conformance
slice; associated consts, generic impl/method conformance, projection
normalization, complete metadata, and general body type checking keep M4-02
`ACTIVE`.

Canonical projection-equality goals now share the implemented-trait goal
table. Their keys include both projection and expected terms; proof requires a
unique recursively proven impl, an authenticated impl-associated definition,
and target instantiation under the replayed substitution. Root projection
targets normalize recursively through the same table. Every non-proof rolls
back scratch state and evidence, overlap remains ambiguous even when only one
target matches, and a unique target mismatch is `NO_SOLUTION`. GATs, nested
structural projections, associated defaults, specialization, and incomplete
foreign metadata remain distinct fail-closed outcomes. The next body-semantic
integration must retain durable projection-proof recipes and broaden their
source-expression use. Callee predicate checking already constructs and
dispatches projection-equality goals through the semantic session; it no
longer rejects them or treats them as implemented-trait facts.

The next body-typing milestone generalizes this bounded inferred-let draft
instead of adding another reachable-expression special case. It is an
all-local, cfg-active transactional typed-HIR barrier that runs
before reachability and MIR. Its dependency sequence is general constraint
generation/finalization; path, trait, and projection lookup; coercions and
immutable adjustments; methods/operators/callables; patterns and binding
modes; closures/coroutines; post-typecheck rewrites; region/outlives and
conservative borrowing; then CTFE and broad validated MIR. No later pass may
consume partially typed local HIR or reinterpret an unsupported semantic state
as proof.

Production now has the first bounded whole-crate ordering barrier. The C
driver invokes transactional `cm_semantic_admit_local_crate` immediately after
module-graph HIR lowering and before legacy entry or exported-root discovery.
Every cfg-active supported free-function or concrete trait-impl method body is
therefore lowered and definition-checked, and an invalid unreachable body
rejects atomically before MIR or output publication. Original mrustc completes
outer and expression type checking, usage
annotation, lifetime/reborrow, and validation passes before translation
enumeration and monomorphization. The next production barrier must invoke the
remaining semantic passes in that same causal position. The driver now retains
the all-local admission capability through root discovery, reachability, exact
admission, MIR construction, validation, and commit. Reachability is read-only:
it carries no module/import lowering state, never invokes body lowering, and
requires current all-local semantic evidence for every discovered body before
it can enter the exact-instance closure. Cfg-disabled invalid bodies are removed
before this barrier and neither poison admission nor enter emitted reachability.
The current semantic
metadata artifact contains declarations plus trait/impl universe, not typed
bodies or semantic recipes; it must not be described as this barrier until a
versioned durable body/results schema and completeness manifest exist.

Semantic results now publish immutable, canonical per-expression adjustment
sequences plus exact primitive-binary and named-field selection recipes.
Ordinary-body and exact-instance MIR lowering consume those recipes, and MIR
replay authenticates the same expression identities, operator or aggregate
selection, field ordinal, and operand/result types. Adjustments are represented
and queried in order, but current HIR and MIR have no executable borrow,
deref, unsize, reification, or other adjustment forms; every nonzero sequence
therefore remains fail-closed instead of being rediscovered from endpoint
types. Focused tests cover ordinary and exact-instance recipe queries,
malformed or duplicate primitive and field facts, malformed and noncontiguous
adjustment facts, wrong instance identities, and publication of no stage state
after every rejected writeback. This establishes the authenticated evidence
boundary only; coercion generation and executable adjustment lowering remain
open under M4-03 and the all-local typed-HIR sequence above.

Exact free-function closures, including recursive strongly connected
components, now cross the admitted HIR-to-MIR
boundary with type-only item arguments and the currently supported proven
callee predicates. Admission derives every
callee's canonical identity from the authenticated caller instance and the HIR
call substitutions, requires both endpoints in the closed set, rejects
duplicate or omitted edges atomically, and seals cross-instance return and
parameter types. Exact direct-call queries are consumed by both MIR lowering
and replay; an unselected or phantom substitution cannot fall back to a
definition-level `DefId`. Semantic type-view authentication recognizes each
sealed instance-owned byte arena. This remains a bounded bridge: stored MIR
identity is still `DefId + CmHirTypeId[]`, the driver materializes only its
existing optional-u32 substitution subset. MIR publication reserves stable
instance IDs, defines and validates the complete overlay, and commits all
bodies atomically; incomplete reservations never become visible through the
public MIR context. Transactions pin process-unique MIR-context lifetime and
semantic-admission capability identities, so destroy/reinitialize ABA cannot
re-authorize a draft. Compile-only self and mutual recursion pass, while useful
terminating recursion still needs calls nested in general conditional control
flow. M4 remains active until the all-local semantic barrier and the pass
sequence above replace these bounded slices.

## M5: MIR, monomorphization, and TCC C backend

| ID | State | Task | Acceptance |
|---|---|---|---|
| M5-01 | ACTIVE | HIR-to-MIR including matches | Unoptimized MIR matches normalized oracle dump |
| M5-02 | TODO | MIR cleanup and required intrinsics | Existing MIR tests pass; optimizer may be off |
| M5-03 | TODO | Reachability and monomorphization | Generic multi-crate corpus links |
| M5-04 | ACTIVE | Layout and i386 SysV ABI | ABI probes and oracle facts match |
| M5-05 | ACTIVE | Portable C emitter | No-core output passes TCC; full output passes source-built GCC |
| M5-06 | TODO | Runtime shims: atomics/TLS/u128/panic | Core/std runtime probes pass |
| M5-07 | TODO | External C/link driver | No-core and core-linked executables run |

For M5-03, the compile driver's reachability worklist now owns and deduplicates
canonical free-function identities, retains canonical callee identity on every
edge, admits the entire reachable set through one closed exact-instance
semantic transaction, reserves all MIR instance IDs before lowering, then
defines, revalidates, and commits the complete graph atomically. This accepts
self and mutually recursive SCCs without exposing placeholders; the C backend
now emits forward declarations for every reachable private or exported body.
The former open-session/raw-MIR generic fallback is gone. Flat `DefId +
optional u32` remains temporary lowering material, so this is still bounded
monomorphization evidence rather than the completed task. The next authority
checkpoints are general substitutions and the full semantic/MIR pass sequence.

The explicit local UFCS trait-method checkpoint now crosses that recipe
boundary. HIR retains `<Type as Trait>::method(arguments)` as unresolved
qualified callable syntax with the requested concrete `Self`, requested trait,
declared trait method, ordered child expressions, and receiver ordinal, but no
selected impl or method. The all-local semantic pass proves the implemented-
trait goal, requires one supported positive concrete local impl, authenticates
the exact impl method's declaration link and signature, and atomically seals the
declared trait item, selected impl/callable, child expressions, receiver
boundary, and normalized parameter/return types in semantic results. Missing,
ambiguous, negative, malformed, and unsupported candidates publish no recipe.

Read-only reachability obtains the callee only from that sealed selection and
then authenticates its exact DefId parent links; it does not repeat trait or
method selection. Exact admission, MIR lowering, and MIR replay query and
authenticate the same selection before emitting a direct call to the separately
reachable impl method. A source fixture executes a public C-ABI wrapper calling
the selected private impl method, while missing and ambiguous fixtures prove
atomic output rejection. This follows original mrustc's resolve, expression
typecheck/selection, typed-HIR validation, MIR, and translation ordering without
performing lookup during reachability or MIR construction.

This checkpoint remains deliberately bounded to concrete nongeneric local
positive trait impls and methods with the currently supported scalar call
shape. Qualified calls may use an explicit override or inherit a receiver-free
nongeneric trait default whose body has the existing closed scalar shape.
Bodyless linked overrides block fallback, and multiple linked overrides
reject. Inherent lookup, dot-syntax inherited defaults, defaults containing
calls or meaningful `Self`, generic traits, source-level blanket inherited
defaults, broader substitutions and projections, coercions,
autoderef/autoref/reborrow adjustments, cross-crate incomplete metadata, and
general callable traits remain fail-closed. Stored MIR locals carry the
normalized semantic signature types. MIR now exposes narrow admission-bound
signature and parameter views by reconstructing the exact instance internally;
the C backend validates admitted caller and callee locals through those sealed
views and no longer interprets impl-owned symbolic `Self`. Declaration
inspection remains only on the legacy evidence-free compatibility path and for
exact callable identity authentication.

The next callable milestone generalizes body constraints rather than adding a
new reachability special case. Source HIR now retains unresolved
`receiver.method(arguments)` syntax as a separate receiver, ordered explicit
arguments, the exact method name, and exact deduplicated in-scope trait DefIds.
The scope includes same-module and enclosing traits plus structural trait-import
bindings; named bindings are authenticated against the effective checked
resolver result, while anonymous `use Trait as _` bindings remain present even
though they intentionally publish no namespace name. Shadowed glob results are
therefore excluded. Explicit method generic arguments and independently
uninferable children remain fail-closed. The model deep-copies all slices,
validates ownership/source order and trait identity, and lowering rewinds arena,
interner, expressions, types, and locals on failure. Semantic results,
reachability, and MIR-facing traversal reject the unresolved node until a
selection recipe exists. Focused strict GCC, TinyCC, and Clang ASan/UBSan/leak
model/body tests pass; this is producer evidence, not method selection.

The first dot-method semantic-selection slice now jointly constrains the
receiver and ordered explicit arguments for primitive, by-value receivers at
autoderef depth zero. It scans every exact in-scope trait containing the named
method, solves the implemented-trait goal, authenticates one concrete local
nongeneric positive impl and linked impl method, and counts all viable
candidates before selection. More than one viable candidate is therefore an
order-independent ambiguity; stable identity never breaks a semantic tie.
Missing, negative, unsupported, and unsolved candidates publish no writeback.

Successful selection seals the declared trait item, selected impl/callable,
receiver-first argument order, requested concrete `Self`, normalized parameter
and return types, and an empty adjustment sequence. Durable-results validation
is syntax-aware, authenticates the dot receiver slot against the concrete
requested `Self`, and keeps explicit arguments tied to the selected signature.
Focused strict GCC, TinyCC, and Clang ASan/UBSan/leak semantic-body and durable-
results tests pass. This is deliberately limited to concrete local trait
methods, primitive by-value receivers, depth zero, and zero adjustments;
inherent methods, generics, autoderef/autoref/reborrow, metadata-incomplete
selection, and nonprimitive receiver shapes remain fail-closed.

Reachability, exact admission, MIR lowering/replay, and executable C acceptance
now consume only this sealed recipe for the same bounded dot-method slice. The
driver validates syntax-aware receiver-first arguments, selected identity,
concrete `Self`, and zero-adjustment expression facts before deriving a
reachable callee from `selected_callable`. Exact admission authenticates the
same selected trait/impl/method edge. MIR lowering and authoritative replay
synthesize `[receiver, explicit arguments...]`, require the sealed normalized
signature, reject every nonzero adjustment, and emit an ordinary direct call;
the C backend remains method-agnostic and consumes only validated MIR.

Impl-owned symbolic `Self` is concretized only for a method whose exact parent
is the already selected impl, using that impl's concrete self type. This does
not perform method lookup or reconstruct selection. A no-core fixture executes
a public wrapper calling a separately reachable private impl method with one
explicit argument. Missing impl, two viable in-scope traits, and a valid trait
left outside lexical method scope all reject while preserving an existing
output and creating no fresh output. The complete strict GCC suite, focused
TinyCC, Clang ASan/UBSan/leak, MIR/admission/results tests, and executable
acceptance pass. Inherent methods, generics, nonzero adjustments, borrowed
receivers, and broader argument/result shapes remain fail-closed.

Callable evidence now also seals the exact nongeneric instance shape needed by
later consumers. Each selection retains the enclosing impl, implemented trait,
and `Self` owner as separate domains, with authenticated zero counts for item,
method, enclosing-impl, and implemented-trait arguments. Exact-instance MIR
lowering and authoritative MIR replay resolve the emitted callee and query the
selection and parameter types against its canonical instance key; caller-only
selection queries remain definition-lookup hints, not final call authority.

Projection normalization is durable per callable slot. Requested `Self`, every
parameter, and the return type retain both their pre-normalization and
normalized terms. Nonempty traces authenticate the scratch input/result IDs,
are bound to the exact expression, slot kind, and slot index, and must cover
exactly every changed callable slot once while leaving unchanged slots
trace-free. Duplicate, relabeled, orphaned, incomplete, and overlapping traces
fail final checked-body validation. Generic-argument trace kinds are reserved
but rejected until their corresponding durable argument arrays exist.

Unique impl selection now optionally publishes a separate caller-owned winning
impl-substitution witness without adding ownership to the by-value selection
result or goal-table cache. The witness retains the exact selected impl and its
generic arguments in declaration order, including authenticated present
evidence for zero-generic impls. Exposure reconstructs a validated typeck
instantiation and is tied to the exact typeck lifetime/revision plus the HIR
storage lifetime, semantic/rewind generations, and index-relevant collection
counts. Rollback, later binding changes, HIR/index mutation, destroy/recreate,
parameter-environment proofs, every non-proof, completed non-proof cache hits,
and outer goal-table commit failure therefore leave no current impl evidence.
Recursive predicate/projection evaluators stay witness-free so nested proofs
cannot replace the root winner. The borrowed instantiation view must be copied
before the witness is cleared, destroyed, or reused.

Typeck now also supports one synchronous scoped instantiation containing any
bounded set of unique exact parameter-owner frames plus a separate `Self`
binding. Lifetime, type, and const parameters dispatch only through their exact
owner; missing owners remain rigid, declaration arity/kinds are authenticated
for both bound items and reserved construction fixtures, and cross-owner const
declared types see the complete scope. Frame order has no semantic effect:
const validation visits owners in canonical DefId order, and lookup is by exact
owner. Every operation remains snapshot-atomic. Explicit empty method-owner
frames alongside a nonempty impl-owner frame, duplicate owners, bound-item
arity mismatch, frame permutation, cross-owner const validation, foreign
sessions, and rollback are covered. Scoped capabilities also retain a rollback
generation so a borrowed scratch TypeId cannot become valid again after its
numeric ID is reused. Legacy single-owner entry points remain wrappers and
preserve their established caller lifetime behavior.

The next checkpoint immediately copies the root impl witness into durable
payloads for all four callable argument domains: item, method, enclosing impl,
and implemented trait. Those domains must stay distinct and derive the
canonical callee from the same normalized values before generic qualified-call
and dot-method gates open. All initialized witnesses must be destroyed on every
path, and helper-local partial callable facts must be cleaned before returning
because outer cleanup cannot see a slot until its count is committed. General
adjustments follow that authority boundary. Operators, indexing, and
callable-trait syntax then reuse the same selection and adjustment machinery
instead of introducing parallel lookup paths.

The scratch producer side of that checkpoint now accepts a type-only blanket
`impl<T>` for both qualified and dot calls. Selection asks the solver for a
caller-owned root witness, immediately copies its declaration-ordered impl
arguments, authenticates the witness owner, and instantiates the selected
signature under distinct empty-method and nonempty-impl frames plus a separate
impl-owned `Self` binding. The first exact fixture is `Echo for T`: both
`<u32 as Echo>::echo(receiver, value)` and `receiver.echo(value)` retain
enclosing-impl arguments `[u32]`; item, method, and implemented-trait domains
remain empty; every selected parameter and return term normalizes to `u32`.
Each of the four argument domains retains separate input/output arrays and
normalizes type payloads independently with its reserved projection-decision
kind. Const argument declared types are normalized while the const value and
kind remain fixed; lifetime values and any other changed non-type payload fail
closed. Helper-local cleanup now owns all eight arrays until the callable count
commits. Semantic-results storage durably owns those four input/normalized
domains for both definition and exact-instance records, rebases their ranges
transactionally, authenticates declaration arity and argument kinds, and
exposes domain-specific borrowed views. Qualified `u32` and dot-method `u8`
records round-trip independently, and sanitizer coverage guards the publication
counter reset that previously corrupted cleanup. Type-generic impl method
definitions also check under a rigid enclosing-impl frame plus a separate
impl-owned `Self` binding. The supported-owner classifier authenticates every
nested ADT and implemented-trait target against a bound declaration, exact
generic arity, declaration-ordered parameter owner/index/kind, and recursive
argument payload before admitting that frame. Exact callee construction and
semantic admission now cross that boundary without flattening the binders.
Durable qualified-call and dot-method evidence frames directly into the same
owned canonical identity as a manual `CmHirInstanceSpec`; the encoder
authenticates every declaration-ordered domain and the selected
method/impl/trait/`Self` relations transactionally. Its structural payload
validator rejects unresolved parameters and projections, and function-pointer
ABI identity uses spelling bytes rather than process-local intern IDs.

The first generic-impl executable semantic fixture admits the same blanket
method independently for `u32` and `u8`, installing distinct method and
enclosing-impl frames plus the impl-owned `Self`. Wrong owners, arities, kinds,
domains, and `Self` fail without publication, and a valid retry succeeds.
Strict GCC, TinyCC, and Clang ASan/UBSan/leak semantic-body, admission, and
semantic-results gates pass. Reachability now reserves each complete canonical
identity in MIR, lowering retains that reservation as the body identity, exact
call edges resolve through the durable semantic callee key, and call
terminators carry the complete callee identity. The C backend compares those
canonical identities and derives deterministic private names from a
domain-separated SHA-256 digest of the full key, while duplicate-name checking
turns a hypothetical digest collision into transactional emission failure.
Flat substitutions remain only as transitional executable type materialization.
Canonical expression, adjustment, primitive, field, signature, direct-call,
callable, argument-domain, parameter, and projection-trace queries now replay
authoritative exact-instance semantics directly from the retained structural
key. The key decoder validates the complete tagged payload, owner and body
relations, truncation, and trailing bytes, then requires a byte-for-byte
canonical re-encoding before lookup. MIR lowering and admitted MIR revalidation
use those accessors whenever a body owns a canonical key; mixed canonical/flat
call endpoints fail closed instead of discarding the canonical half. The old
flat reconstruction remains only for legacy exact-instance compatibility APIs
whose input is still `DefId + CmHirTypeId[]`.

Canonical closure admission now accepts the complete retained keys directly.
An owned decoder authenticates every tag, domain, argument payload, owner/body
relation, truncation/trailing byte, and byte-identical canonical re-encoding;
its argument arrays are owned while payloads borrow from the source key.
Direct free-function calls are re-encoded structurally from the decoded caller.
Selected qualified and dot calls derive the complete expected callee key from
the just-checked staged semantic evidence before results commit. The closure
requires unique member identities, unique caller/expression edges, complete
edge coverage, exact caller body ownership, and full-key callee equality. The
old flat leaf/closure APIs are compatibility wrappers over this canonical core.
Malformed, duplicate, missing, nonmember, and same-ABI-but-wrong-generic
identities reject without an admission capability or HIR type/expression or
generation mutation.

The driver now interns each selected callee from the durable canonical identity
and passes those same retained identities into closure admission instead of
rebuilding flat impl-method specs. Canonical bytes alone control reachability
deduplication and identity. A concrete nongeneric impl retains zero executable
substitutions. The one admitted generic-impl bridge reads exactly one normalized
type argument from the enclosing-impl domain and materializes it read-only to
the lowest equivalent existing HIR `TypeId`; item, method, implemented-trait,
non-type, or wider generic shapes fail closed. That `TypeId` is transitional
MIR storage only and never identity. Strict GCC, TinyCC, and Clang
ASan/UBSan/leak gates pass for instance/admission/results, MIR model/lowering,
C codegen, qualified UFCS, dot methods, generic free-function chains, and
recursive publication.

MIR no longer assumes that its optional flat executable substitution belongs
to the selected function item. The bounded canonical impl-method bridge
authenticates a zero-method-generic function under an exact single-type-
parameter impl, resolves both impl-owned `T` and symbolic `Self` through the
one retained enclosing-impl type, and replays that owner domain from sealed
callable generic-argument evidence. Canonical re-encoding remains the authority
for owner, domain, arity, kind, `Self`, body, and identity; the retained
`CmHirTypeId` is checked only as executable material and cannot rename or merge
instances. The legacy flat free-function path remains item-owned and u32-only.

The same checkpoint carries the narrow u8 execution path through MIR locals,
moves, parameters, returns, calls, exact-body replay, and the C backend. C
emits exact `uint8_t`, preserves wrapping casts for supported u8 arithmetic,
and still derives private names from the complete canonical-key digest. Source
HIR now admits only the authenticated `impl<T> Trait for T` subset whose
children are methods; blanket associated types and overlapping same-trait
candidates remain rejected. A qualified u32 call and dot-method u8 call through
one `Echo for T` impl execute independently, retain two canonical MIR bodies,
emit two distinct digest-derived private C definitions, and call the matching
body from each public wrapper. Canonical u32 bytes paired with u8 executable
material and missing material fail without a MIR body; admission's wrong
owner/domain/arity/kind/`Self` cases remain transactional with unchanged HIR
types, expressions, and generations. GDB traces at flow lowering and exact
publication replay localized and closed the two stale item-owned assumptions.
The complete strict GCC and TinyCC suites pass, as do focused Clang
ASan/UBSan/leak HIR admission/results, MIR model/lowering, C codegen, and live
blanket acceptance.

The active architectural gate is the all-local semantic barrier. Original mrustc
runs outer and expression typecheck, then usage/static-borrow/lifetime/closure/
vtable/UFCS/reborrow/erased-type rewrites, independent expression validation,
whole-crate MIR lowering/validation/cleanup/borrowcheck, and only then
MIR-driven translation enumeration. cmrustc must seal a generation-bound
`STRUCTURAL -> TYPED -> MARKED -> REGIONS -> REWRITTEN -> VALIDATED` manifest
for every
cfg-active function, const, static, and represented type-position body atom;
rollback or HIR mutation/ABA must stale the whole capability. Exact closure
admission must derive from that barrier and MIR publication must pin both
capabilities. The production emit-C path now has no raw semantic, MIR-lowering,
or C-emission bypass; a source-boundary regression guard enforces that route.
Rewrites must precede durable final semantic-results allocation, and
phase names may advance only after a real validator rather than treating an
unsupported form as completed.

The first barrier slice now exists as a separate process-local capability.
It derives an immutable manifest in stable HIR item order, records each
cfg-active local function, const, and static body together with its exact
owner, declared type, and source-expression identity, and authenticates the
complete HIR storage/generation/length tuple. Appends and rewinds stale every
query, reconstruction on an unchanged HIR receives a new nonwrapping
capability identity, and semantic/rewind generations abort before wrap rather
than returning to an earlier value. The borrowed graph/import/module-map
snapshot is checked for its exact revision, root, complete module bijection,
and HIR ownership on every query. Graph, import-resolver, and module-map
object lifetimes plus import/map snapshot generations prevent both
destroy/reinitialize address reuse and identical re-resolve/clear-rebind ABA.
Structural preflight rejects orphaned or
aliased body ownership without mutation. The implemented transitions begin
with a real atomic `STRUCTURAL -> TYPED` pass through local-body lowering:
failure
rolls the entire HIR transaction back and recaptures the new generation so the
same manifest remains inspectable and retryable at STRUCTURAL. Successful
transitions and mutating rollbacks mint a fresh exact-generation capability.
Free monomorphic const and immutable static initializers now participate in
the same atomic TYPED transition as functions and publish canonical
HIR block roots (wrapping non-block initializers only). Ordinary call typing
matches original mrustc here; const-operation legality remains a later
marking/evaluation obligation. Associated/generic value bodies remain present
but explicitly block TYPED. A first bounded trait-default slice now
joins the atomic transition: the parent must be a local safe non-auto trait
with no generics, predicates, outlives clauses, or supertraits; the
receiver-free safe Rust-ABI method must have only closed
`i32`/`u32`/`usize` parameters and return; and the body syntax is limited to
literals, locals, wrapping primitive arithmetic, and the existing closed
`if`/block forms. Both lowering and semantic definition checking authenticate
that exact typed-HIR shape, so model construction cannot bypass the slice.
Calls or method/qualified dispatch inside the default body, fields, aggregates,
`Self`, projections, lifetimes, generic frames, and opaque returns fail closed
before mutation. Semantic definition checking and durable results include these
defaults. Exact instance admission and canonical MIR now execute the same body
for a qualified call through a concrete local impl, retaining the concrete impl
as dispatch evidence but using the trait-owned substitution domain for the
body. Explicit overrides still win; a bodyless linked override blocks fallback.
The complete typed manifest now mints MARKED
by atomically publishing value-usage and checked-not-promoted static-borrow
evidence. A separate read-only `MARKED -> REGIONS` validator authenticates
owner/body links, signature/local/expression/direct-call type roots, bounded
MARKED evidence, selected method/qualified-call argument recipes, nested
type/const/generic structure, structurally closed predicates/outlives terms,
scoped early- and late-bound regions, and graph integrity. Success preserves
both HIR generations and rotates only the process-local capability. This proves
zero inference variables and structurally scoped regions in the represented
roots, not lifetime inference, trait/equality/outlives solving, promotion
eligibility, or borrow validity. Declaration fields, discriminants, and
type-position expression bodies remain outside this checkpoint. No API can yet
mint REWRITTEN or VALIDATED, and normalized array lengths/discriminants are not
misrepresented as type-position body atoms while HIR lacks stable source body
identities for them.

REGIONS now owns the production authority chain rather than serving only as a
diagnostic phase. A read-only whole-local admission is derived from the exact
REGIONS barrier capability. A canonical reachable instance closure is then
derived from that whole-local parent and the same retained barrier authority;
destroy/reinitialize ABA of either wrapper cannot revive descendants because
retained authority tokens carry liveness and capability identity. Durable
semantic results distinguish `WHOLE_LOCAL`, `REACHABLE_BODIES`,
`LEAF_INSTANCES`, and `INSTANCE_CLOSURE` seals. REGIONS publication accepts only
an exact `INSTANCE_CLOSURE` child with a live whole-local parent and latches all
three admission, barrier, and parent capabilities into MIR. Hosted `main` uses
that transactional publication and admitted C emitter. Direct negative tests
cover foreign, recreated, stale, destroyed-parent/barrier, unpublished-MIR,
wrong-root/body/source, and sentinel-preserving emission rejection. Full normal,
strict GCC, and TinyCC matrices exercise the same route; sanitizer validation
is part of each checkpoint gate.

The remaining non-item body frontier is explicit enum discriminants followed
by array-length expressions. Discriminants can reuse stable variant DefIds but
must retain an expression body separately from a later evaluated scalar and
use the exact enum repr type. Array lengths require stable type-position
definition identities keyed by their source AST type occurrence; HIR TypeIds
are not source identities and one item can own several independent lengths.

As the prerequisite source-closure representation checkpoint, canonical
`hir-v30` adds a stable one-based closure identity, a context-owned closure
arena, closure-owned lexical parameters, nominal closure types keyed by that
identity, and source-closure/parameter expression nodes. A closure signature
is reserved before its type and expressions, then its nested expression root
is bound exactly once. Nested code remains in the enclosing item body and may
see only the authenticated outer-local prefix plus its own parameters.
Construction, finalization, deterministic dumps, typed barrier snapshots, and
all HIR currentness boundaries cover the new arena. Semantic body checking,
MARKED, REGIONS, durable results, driver reachability, MIR, and C emission
still reject these nodes fail-closed: capture class/absence and Copy facts are
deliberately uncomputed, and invocation, lifetime inference, expansion, and
code generation remain later ordered checkpoints. Existing item-source body
origins and callable dispatch/execution identity are unchanged.

The dependency-macro provenance follow-on must also pin producer and consumer
module-graph lifetime IDs through dependency definitions/imports and
`CmResolveDependencyItemRef`. Revision/certificate identity alone still admits
an old consumer reference after destroy/reinitialize/rebuild at the same graph
address.

Committed MIR now also latches the exact semantic-admission capability which
authorized its first body. A newly constructed admission for the same HIR
generation cannot validate or extend that MIR, closing the downstream
same-generation authorization ABA while barrier-derived admission is built.

## M6: Build orchestration

| ID | State | Task | Acceptance |
|---|---|---|---|
| M6-01 | TODO | Cargo manifest and cfg parser | Pinned crate graph matches minicargo |
| M6-02 | TODO | Dependency scheduler and artifact cache | Deterministic parallel/serial builds agree |
| M6-03 | TODO | Build scripts and override mechanism | Rust 1.90 overrides reproduce oracle outputs |
| M6-04 | TODO | Native/proc-macro dependency handling | Proc macros and native libraries load |
| M6-05 | DONE | Complete target-configured Rust 1.90 core HIR | `make check-core-hir`: 363-source/451-module core lowers with zero graph, import, and HIR errors (38176 items, 22524 bodies) |
| M6-06 | ACTIVE | Emit consumable core metadata and `.rlib` | Nonempty artifacts load in a dependent compile |
| M6-07 | TODO | Build alloc against core artifacts | alloc consumes core macro/metadata artifacts and emits a nonempty `.rlib` |
| M6-08 | TODO | Run a core/alloc-linked executable | Linked probe has the expected status and output |
| M6-09 | TODO | Implement C `hcargo` Rust 1.90 DAG build | Complete pinned crate DAG produces checked nonempty artifacts |

The bounded bootstrap-parity G3 gate is complete as of 2026-08-25.  It is
strictly smaller than M6-06: an admitted `no_core` provider emits an exact
v3.2 archive containing authenticated trait/primitive-impl facts, one generic
`RETURN_ARGUMENT` recipe, its native object and link manifest.  A fresh
consumer process uses only that archive, proves the transported bound,
instantiates the generic, and links/runs under GCC and TinyCC.  Twin producers,
strict GCC, a TinyCC-built compiler, Clang ASan/UBSan/LSan, and atomic negative
artifact cases pass through `tests/codegen/run_g3_cross_crate.sh` and the
focused executable metadata/capture/materialization/recipe tests.  M6-06 stays
`ACTIVE` because no `core.rlib` exists and this exact profile rejects general
library declarations, bodies, MIR, layout, and ABI.

The review-hardened 2026-08-26 current tree revalidates M6-05 rather than
relying on the historical census.  Two optimized runs load 363 sources and
451 modules, lower 38,176 items and 22,524 body records with zero graph,
import, or HIR errors, and capture 451 modules, 1,658 public type entries, and
20,747 public value entries after preserving independently visible struct and
enum-variant identities.  Complete strict GCC and TinyCC suites plus the
focused Clang ASan/UBSan/LSan run pass.  Declaration v2.6 then rejects the
known ITEM-family format boundary with zero output.  M6-06 therefore proceeds
through real v3 ordinary items, values, types, traits, and namespace identity,
followed by aliases, associated declarations, and impl headers; another opaque
v2 reference or skipped family would not be a consumable `core` artifact.

The first bounded v3.0 declaration step is now complete and consumed in a
fresh HIR context.  `declaration_capture` lowers the focused
`Gate<T: ?Sized>`/`GateReexport`/`needs<X: Gate<u8>>` provider from exact
graph/import/module-map authority; `declaration_metadata` emits and replays
the 12-section schema-1 `LOWER_SAFE` envelope deterministically; and
`declaration_materialize` enforces exact crate, target, layout, edition,
panic, and cfg expectations before transactionally binding HIR and library
namespace state.  A new consumer successfully lowers direct and reexported
trait predicates to one DefId.  Strict GCC and TinyCC full suites and focused
Clang ASan/UBSan/LSan pass.  M3-04 and M6-06 remain `ACTIVE`: the implemented
profile intentionally rejects associated declarations, trait aliases,
projections, function pointers, impls, macros, complete semantic attributes,
and general executable bodies.  A later bounded ITEM slice transports public
unit structs and their independently visible constructors.  The fresh
constructor-aware core probe reaches 451 modules, 1,632 public type entries,
and 20,721 public value entries, then identifies the exact current v3.0
frontier as the `LayoutErr` type-alias reexport at
`core/src/alloc/mod.rs:19` (`stage=namespace`,
`reason=binding-shape-unsupported`).  Bounded v3.0 now transports that free
alias as a distinct ITEM, its named `LayoutError` ADT target and private
defining module, while preserving constructor absence and fresh-consumer alias
normalization.  Later bounded slices transport explicit and implicit unit
enums, namespace aliases to exact enum variants, declaration-only CONST,
and named aggregate/union profiles.  Commit `b78a18a8` preserves
`Assume`, `ManuallyDrop<T: ?Sized>`, and `MaybeUninit<T>` through exact field,
generic, lang, repr, direct/reexport, fresh-materialization, and rollback
checks.  Its pinned-core probe reaches the complete 451/1,658/20,747 library
census and measures the next v3.0 rejection at public static
`CACHED_POW10`, `core/src/num/flt2dec/strategy/grisu.rs:29`
(`stage=namespace`, `reason=binding-shape-unsupported`, `binding=value`,
`ast_item=static`, `def=1:2845`).  The next artifact work is therefore a
declaration-only STATIC plus tuple/array type closure and exact scalar array
length.  Initializer/storage/CTFE transport remains a separate executable
gate; exact trait-associated ownership follows the later declaration
prerequisites before projection and impl families.
Commit `12c92627` completes that bounded STATIC/TUPLE/ARRAY slice without
inventing storage or a body.  The fresh pinned-core probe remains green through
graph, HIR, and the exact 451/1,658/20,747 library census, then measures the new
v3.0 frontier at `core/src/prelude/mod.rs:21`: the TYPE namespace binding for
tuple variant `Option::Some` (`def=1:22477`) is rejected with
`stage=namespace`, `reason=binding-shape-unsupported`,
`binding=enum-variant`, and `ast_item=enum`.  The active declaration task is
therefore a generic Rust-default mixed UNIT/TUPLE enum profile with retained
item/variant lang identity, tuple-field generic ownership, generic enum ADT
applications, and exact TYPE/VALUE variant twins.  Constructor expressions
and executable bodies remain separate later gates.
Commit `5d055423` completes that generic enum slice and closes recursive
aggregate-field generic ownership in both the live HIR model and the generic
metadata formats.  Its pinned-core probe then measures the next v3.0 namespace
frontier at `core/src/primitive.rs:41`, the definition-free TYPE binding from
`pub use bool;`.  Commit `eb8281c1` transports all declaration-supported
primitive namespace bindings and aliases without fabricating DefIds, ITEMs,
VALUE mates, or TYPE-DAG records.  The following whole-core probe reaches the
same zero-error 363-source/451-module HIR and 451/1,658/20,747 library census,
then measures `#[rustfmt::skip]` on the `CharTryFromError` reexport at
`core/src/char/mod.rs:28` (`def=1:19977`, source item `117:4`) as the next
reexport-projection boundary.  Commit `2bbd80f8` admits only the exact bare,
depth-zero, source-authenticated reexport form and keeps malformed, duplicate,
generated, private-count, and provenance mutations fail-closed.  The next
pinned-core probe measures the current v3.0 frontier at
`core/src/ffi/mod.rs:12`: exact `doc(inline)` on the `CStr` reexport
(`def=1:20058`, source item `121:1`) is rejected at `stage=namespace` with
`reason=reexport-attribute-projection-unsupported`.  The active bounded task is
the corresponding authenticated reexport-only projection; item census has not
yet been proven to start, so `Allocator` remains a prediction rather than the
measured frontier.
Commit `659d29e4` projects exact `doc(inline)` on authenticated public
reexports, including stable coexistence, grouped aliases, projection erasure,
private-import exclusion, and atomic malformed/provenance rejection.  Its
pinned-core probe measures the remaining reexport kind at
`core/src/hash/mod.rs:91`: `doc(hidden)` on `SipHasher13` (`def=1:24504`,
source item `203:2`).  Commit `53ff35f2` adds that reexport-only projection
without broadening ordinary item profiles.  The following whole-core probe
proves namespace collection complete and measures the current v3.0 frontier
at `core/src/alloc/mod.rs:105`: unsafe, unstable `Allocator` (`def=1:26860`,
source item `252:21`, rejected item `229`) fails at `stage=items` with
`reason=item-source-invalid`.  The immediate source gate sees the retained
`unstable` attribute before the existing trait-shape gate encounters unsafe
safety and seven associated methods.  The active declaration dependency is a
byte-preserving trait-associated METHOD foundation with exact parent/source
order, safety, receiver/default-body promises, `Self`, erased receiver
references, method-owned predicates, and trait-owned library reachability;
the full `Layout`/`NonNull`/`Result` closure follows before exact Allocator can
be transported.
Commit `c74680bf` lands that independently consumable foundation: byte-exact
legacy zero-AITM compatibility, unsafe NOMD safety/ranges, trait-owned METHOD
records, `Self`, erased shared receivers, method safety/default promises,
method-owned marker predicates, complete library child authority and associated
lookup, reserve-before-bind materialization, fresh consumer lowering, and
atomic rollback.  It intentionally rejects ordinary elided `-> &Self`, whose
live HIR region remains `INFER` rather than the receiver's `ERASED` region.
Commits `310a7e1c` and `159686dd` add exact generic ENUM applications,
SLICE/raw-pointer capture, and scoped receiver-driven recursive output elision.
The clean pinned-core probe at `159686dd` remains at the same exact Allocator
parent and `item-source-invalid`, but the first residual fact is no longer a
signature type: source-ordinal-6 `by_ref` carries `#[inline(always)]`, which is
not yet classified by the associated-function projection and therefore fails
before its now-representable signature is emitted.  The active step is an
exact, byte-neutral inline-hint projection with provenance and malformed/
duplicate negatives.  Commit `5b5509f6` closes that associated-only projection.
Its clean whole-core probe advances to `core::alloc::Layout` at
`alloc/layout.rs:40` (`def=1:26881`, source item `252:4`, rejected item
`11511`) with `stage=items`/`item-shape-unsupported`.  The active closure is
`Layout -> Alignment -> private AlignmentEnum -> usize`: a private-field named
struct, transparent tuple struct, and transitively reachable private repr(u64)
unit enum with explicit ISIZE-carried discriminants.  It must not publish the
private enum as a namespace binding or replace it with an opaque nominal.
Commit `e79c819e` also replaces the capture TYPE
canonicalizer's repeated scans with a memoized, stable `O(E log E)` pipeline
and an authenticated multi-crate trait DefId index; this resource hardening
does not itself change the whole-core semantic frontier.  Commits `b4c065aa`,
`e1eba5b9`, and `c5b4d093` implement the complete Layout descriptor,
materializer, and producer chain, including tuple fields, exact u8/u16/u32/u64
enum reprs, high-bit ISIZE-carried discriminants, and public-rooted private
dependency closure.  The clean `c5b4d093` pinned-core probe preserves the
zero-error 363-source/451-module graph, 38,176-item HIR, and exact
451/1,658/20,747 library census, then measures the new frontier at
`core/src/any.rs:113`: `Any` (`def=1:18858`, source item `99:3`, rejected item
`110`) fails with `stage=items` and `reason=item-source-invalid`.  The active
slice is an exact safe trait with retained diagnostic identity, a
`Self: 'static` outlives fact, and its required shared-receiver associated
method; a childless or unsafe substitute would not clear the real boundary.
Commits `0f7b9fb7`, `c0189406`, and `0256f506` implement the descriptor,
materializer, and capture portions of that exact shape.  Their clean pinned
Rust 1.90 probe preserves the zero-error graph/HIR and exact library census,
then advances to `core/src/any.rs:711`: `TypeId` (`def=1:18886`, source item
`99:30`, rejected item `10800`) fails at `stage=items` with
`reason=item-shape-unsupported`.  The active bounded dependency is its exact
crate-visible `data` field of type
`[*const (); 16 / size_of::<*const ()>()]`: field visibility must not collapse
to private, and the evaluated array length must derive from the configured
target pointer width rather than `sizeof(void *)` on the bootstrap host.
Commits `2b19ef47` and `592d17a1` preserve that exact declaration with
explicit 32/64-bit target authority.  Their clean pinned-core probe keeps the
zero-error graph/HIR and exact 451/1,658/20,747 library census, then advances
to `core/src/any.rs:856`: `type_name` (`def=1:18898`, source item `99:42`,
rejected item `10801`) fails at `stage=items` with
`reason=item-source-invalid` as a public VALUE/FUNCTION declaration.  Commits
`1f8f6d22`, `1447e225`, and `a58142db` transport that const declaration while
leaving its implementation unavailable.  Their clean pinned-core probe then
measures `core/src/any.rs:896`: `type_name_of_val` (`def=1:18899`, source item
`99:43`, rejected item `10802`) at `stage=items`/
`item-source-invalid`.  Commits `bef6bc8a`, `c03b3378`, and `b0a41eef`
constrain erased declaration roots, normalize only the exact input-only
lifetime boundary, and capture the declaration.  The clean pinned-core probe
at `b0a41eef` keeps the zero-error graph/HIR and exact
451-module/1,658-type/20,747-value library census, then measures
`core/src/arch.rs:76`'s public `breakpoint` function (`def=1:36071`, source
item `332:5`, rejected item `19448`) at `stage=items`/
`item-source-invalid`.  Commits `9b7740c7`, `19635436`, and `cd76600e`
complete its exact zero-generic unit declaration profile without transporting
the inline intrinsic body.  The clean pinned-core probe at `cd76600e`
preserves the zero-error graph/HIR and exact
451-module/1,658-type/20,747-value library census, then measures
`core/src/array/iter.rs:20`'s `IntoIter` (`def=1:19103`, source item `100:18`,
rejected item `10819`) at `stage=items`/`item-shape-unsupported`.
Commit `112049fe` transports its exact const-generic declaration and reachable
non-public structural closure while keeping impls, drop glue, and bodies
unavailable.  The clean pinned-core probe at `112049fe` preserves the same
zero-error graph/HIR and 451-module/1,658-type/20,747-value library census,
then measures `core/src/array/mod.rs:181`'s `TryFromSliceError`
(`def=1:18905`, source item `100:24`, rejected item `10808`) at
`stage=items`/`item-shape-unsupported`.
Commit `ba215beb` adds the exact zero-generic Rust-repr tuple-error producer.
The clean pinned-core probe at `ba215beb` again preserves the zero-error
graph/HIR and 451-module/1,658-type/20,747-value library census, then measures
`core/src/array/mod.rs:108`'s `from_fn` (`def=1:18901`, source item `100:20`,
rejected item `10804`) at `stage=items`/`item-source-invalid`.
Commit `68e338e9` adds its exact const-generic declaration plus the complete
reachable `Tuple`/`FnOnce`/`FnMut`, associated `Output`, rust-call method,
projection, supertrait, and equality closure. The function remains
declaration-only with `BODY_NONE`. Commit `62fd1289` admits foreign associated
equalities only after the existing exact definition, parent-trait reachability,
shape, and name-ambiguity authentication. The clean pinned-core probe at
`62fd1289` preserves the zero-error graph/HIR and exact
451-module/1,658-type/20,747-value library census, then measures
`core/src/array/mod.rs:173`'s `from_mut` (`def=1:18904`, source item `100:23`,
rejected item `10807`) at `stage=items`/`item-source-invalid`.
Commit `292adaf1` transports its exact source-authenticated paired mutable
reference declaration with no executable body authority. The clean pinned-core
probe at `292adaf1` preserves the zero-error graph/HIR and exact
451-module/1,658-type/20,747-value library census, then measures
`core/src/array/mod.rs:165`'s shared twin `from_ref` (`def=1:18903`, source
item `100:22`, rejected item `10806`) at
`stage=items`/`item-source-invalid`.
Commit `fbe4cee4` generalizes that exact paired-erasure boundary to matching
shared/shared as well as mutable/mutable array-reference declarations, while
mixed mutability and lifetime-sensitive roots remain closed. The clean pinned
Rust 1.90 probe at `fbe4cee4` again reports zero graph/import/HIR errors,
38,176 HIR items, and the exact 451-module/1,658-type/20,747-value library
census. It clears both `from_ref` and `from_mut`, then measures the next v3.0
frontier at `core/src/array/mod.rs:54`: `repeat<T: Clone, const N: usize>`
(`def=1:18900`, source item `100:19`, rejected item `10803`) fails at
`stage=items` with `reason=item-source-invalid` as a public VALUE/FUNCTION
declaration. Its declaration requires the already-retained value-owned const
generic and `[T; N]` return plus an exact `T: Clone` predicate closure; the
generic inline body remains outside declaration metadata.
Commit `9c8f6439` transports that exact declaration together with the complete
reachable `Clone`/`Sized`/`MetaSized`/`PointeeSized`/`Destruct` declaration
closure, including `clone`, `clone_from`, compiler flags, and the
`CONST_IF_CONST` predicate, while keeping every imported body unavailable.
The clean pinned Rust 1.90 probe at `9c8f6439` again reports zero graph,
import, and HIR errors, 38,176 HIR items, and the exact
451-module/1,658-type/20,747-value library census. It advances the v3.0
frontier to `core/src/array/mod.rs:146`: `try_from_fn<R, const N: usize, F>`
(`def=1:18902`, source item `100:21`, rejected item `10805`) fails at
`stage=items` with `reason=item-source-invalid`. Its declaration introduces
the exact `Try`/`Residual` projection and associated-equality closure used by
`ChangeOutputType<R, [R::Output; N]>`; its generic inline body remains a
separate executable-metadata boundary.
Commit `574fda73` captured that declaration and its `Try`/`Residual` closure in
a single-module fixture, but the real crate still rejected it: `core::array`
sorts before `core::ops`, and the value pass authenticated `Try`'s captured
profile before the trait pass had reached it. Commit `ebdcf4bc` collects every
public trait before any other binding (the captured arrays are sorted
afterwards, so emitted bytes are unchanged) and proves the split layout with a
cross-module fixture. The same session repaired the development, strict, and
TinyCC lanes, red since `f9e5726d`/`dcb7e0d4`, by admitting unrestricted `pub`
foreign types and moving the v2 driver fixtures to named-field structs
(`16200315`).
The clean pinned Rust 1.90 probe at `ebdcf4bc` again reports zero graph, import,
and HIR errors, 38,176 HIR items, and the exact 451-module/1,658-type/20,747-value
library census. It advances the v3.0 frontier to `core/src/array/iter.rs:356`:
`pub trait NonDrop {}` (`def=1:19141`, source item `104:48`, rejected item
`113`) at `stage=items`/`item-source-invalid`. Because traits are now collected
first, this is the first public trait in the whole crate outside the admitted
trait declaration profile; its attributes are `#[doc(hidden)]`,
`#[unstable(issue = "none", feature = "std_internals")]`, and
`#[rustc_unsafe_specialization_marker]`, and the next trait slice must
authenticate that exact declaration or report the precise rejected fact.
Commit `58b4090e` retains that marker as a closed compiler flag with the exact
`doc(hidden)` + `unstable` projection, admitted only on an empty, safe,
generic-free, bound-free public trait; codec validation and materialization
accept the same shape and nothing else with the bit set.
The clean pinned Rust 1.90 probe at `58b4090e` again reports zero graph, import,
and HIR errors, 38,176 HIR items, and the exact 451-module/1,658-type/20,747-value
library census. It advances the v3.0 frontier to `core/src/asserting.rs:105`:
`pub trait Printable: Copy + Debug {}` (`def=1:19374`, source item `108:21`,
rejected item `117`) at `stage=items`/`item-source-invalid`. This is the first
plain public trait with ordinary supertraits; admitting it requires the
reachable `Copy` (`Clone`/`Sized` family) and `fmt::Debug` (`Formatter`,
`fmt::Result`) declaration closures rather than another single-trait
attribute profile.

The authoritative progress metric is the deepest nonempty artifact that a
later stage can consume and, where applicable, execute. Parser, graph, and HIR
coverage are necessary diagnostics but do not substitute for metadata,
libraries, executables, or self-host stages. M6-05 through M6-09 are explicit
vertical gates preventing that substitution. `tools/probe_core_hir.c` and the
`$(BUILD_DIR)/probe_core_hir` Make target make the M6-05 diagnostic
reproducible against an explicit Rust `library/core/src/lib.rs`; the probe
returns nonzero until the HIR gate actually closes.
`make check-core-metadata` runs the same real-core path through value-aware
library capture and declaration-v2 metadata encoding, requires the metadata
boundary to succeed, and remains red until M6-06 closes.
The 2026-08-23 post-v2.3 gate reaches the complete M6-05 census, then its
first value-aware library capture rejected `core/src/contracts.rs:19`'s
`build_check_ensures<Ret, C>(cond: C) -> C`, whose `C: Fn(&Ret) -> bool + Copy
+ 'static` clause lowers to two trait predicates and one outlives predicate.
Library artifacts now deep-copy and authenticate predicate scopes, trait
predicates, and outlives predicates for public free functions. Declaration
metadata v2.4 transports the bounded `build_check_ensures` shape, including
its predicate-owned late-bound callable input, associated equality, and
`'static` outlives fact. Referenced traits and associated types decode as
opaque RESERVED identities rather than fabricated declarations.
Predicate capture additionally retains a sorted, deduplicated reference-only
nominal closure and exact associated-type availability witnesses. The
`build_check_ensures` fixture records `Fn`, `FnMut`, `FnOnce`, `Copy`, and
`FnOnce::Output` with authenticated owner, name, generic schema, and declaring
parent. The records remain opaque: they create neither namespace bindings nor
fake empty traits. Strict GCC, TinyCC, and Clang ASan/UBSan/LSan library and
metadata tests pass. V2.4 fresh-process decode and byte-identical re-encode
also pass while solver/projection consumers remain deferred.
The next instrumented whole-core capture failure was
`core::ptr::null<T: PointeeSized + Thin>` at DefId `1:9501`: `Thin` is a trait
alias. Opaque closure capture now distinguishes trait aliases and records
`Thin`, `Pointee`, and `PointeeSized` identities, while deliberately omitting
Thin's definition-owned `Metadata = ()` equality and any fabricated
function-predicate availability. Alias expansion and solving remain deferred.
The optimized post-`1746e681` whole-core gate now completes declaration
library capture with 451 modules, 1,632 public type entries, and 20,692 public
value entries. The v2.3 encoder then returned `unsupported HIR` with zero
bytes. V2.4 now implements the first predicate/reference-only nominal wire
slice; the real-core gate must be re-run to identify the next exact unsupported
shape.
The optimized post-`4bd79751` gate restores the same 451/1,632/20,692 capture
after fixing availability validation to compare deduplicated identity sets
rather than equality-occurrence counts. Declaration v2.4 still returns
`unsupported HIR` and zero bytes. The next bounded encoder frontier is the
first unsupported core predicate form. A temporary encoder-policy trace
identifies function DefId `1:9087` with a scope-free `CONST_IF_CONST`
(`~const`) predicate before the known direct `Thin` alias predicate. Its
`direct=0` trace field is the authenticated ordinary-trait enum tag, not a
missing reference, so the modifier is the sole policy rejection. The trace was
removed after measurement.
Declaration v2.5 now preserves the complete closed predicate-modifier enum as
stable wire tags, restores exact modifiers in fresh contexts, and upgrades
modifierless v2.4 predicates as REQUIRED. Unknown and truncated current-version
records reject atomically without legacy fallback. These modifiers remain
opaque declaration facts: const-trait capability, selection, and associated
projection semantics are still deferred. The post-v2.5 optimized gate proves
the same zero-error HIR census and
451-module/1,632-type/20,692-value capture, then returns `unsupported HIR`
with zero metadata bytes at the direct `Thin` trait-alias predicate boundary.
Declaration v2.6 now transports that direct alias identity while keeping it
opaque and RESERVED. It deliberately creates no alias expansion, associated
equality or availability, item, namespace binding, or solver evidence; alias
predicates with direct equalities remain fail-closed. Exact v2.5/v2.4/v2.3
decode compatibility and fresh-process byte-identical v2.6 replay pass. The
post-v2.6 optimized gate again proves the complete zero-error HIR and library
census, then returns `unsupported HIR` with zero bytes. Temporary failure-only
instrumentation localizes the rejection to ITEM collection. Declaration v2
cannot represent core's traits, aliases, associated children, impl headers, or
trait namespace entries; skipping those families would produce an artifact
that alloc could not safely consume. The next metadata boundary is therefore
cmhir-meta v3 with explicit family capabilities and real declaration items,
not another opaque v2 omission. V3.0 targets declaration-safe trait/alias/
associated/impl headers plus projection and function-pointer types; impl
absence remains deferred until a later cfg-active completeness certificate.
The first v3 prerequisite is now canonical `hir-v35`: traits preserve exact
effective `#[const_trait]` capability and trait impls preserve their const
header bit, including safe itemless negative impls. Const inherent impls,
non-word or misplaced `const_trait` attributes, and const impls of non-const
traits reject. Semantic metadata v1.1 rejects either new bit rather than
silently erasing it; v3 will transport both as authenticated declaration
facts without claiming const-trait solving.
Canonical `hir-v35` additionally records resolver-authenticated conservative
public/crate-visible reachability on each import binding. Public declaration
artifacts can now retain a public struct type reexport while omitting its
private, restricted, or `#[non_exhaustive]` constructor; exact restriction
module identities remain future schema work.
Canonical `hir-v35` also separates the promise that a trait method or
associated const has a default from the availability of its executable body.
Impl completeness permits omission or exact override from that promise,
defaulted associated consts remain overrideable, and a metadata-only default
call returns unsupported until its body is transported. Semantic metadata
v1.1 rejects these promises; declaration v2.x remains compatible because its
trait identities are explicitly reference-only. V3 will carry the promise
separately from body availability.
Canonical `hir-v36` adds a closed value-definition provenance. An authenticated
dependency declaration may bind an immutable free const without an executable
body as `METADATA_DECLARATION`; ordinary source, trait, impl, and static values
remain `SOURCE`. A bodyless source free const, metadata static/associated const,
mutable metadata const, or metadata declaration carrying a body rejects. This
is declaration identity and type availability only, not CTFE or codegen
evidence.
Canonical `hir-v37` retains optional enum-variant language-item identity as
structural HIR, rejects duplicate non-empty identities within an enum, and
prints the retained identity canonically. Graph-backed lowering authenticates
the source/effective variant provenance; cfg-disabled raw variants do not
inherit the attributes of the next effective variant.
Parenthesized callable-trait input elision is now canonical HIR: omitted input
lifetimes become deterministic predicate-owned late-bound parameters. An
elided callable output inherits the sole distinct input lifetime; ambiguous
outputs and predicate-prefix binders combined with additional elided inputs
remain fail-closed.
Function-pointer lifetime binders are also canonical `hir-v35`: explicit
`for<'a>` names and implicit input elision lower to declaration-order local
indices under the nearest function-pointer binder. Each nested function
pointer is independently closed; item early-bound lifetimes remain visible,
but capture of any enclosing late binder rejects rather than falling through
to a same-named item lifetime. A cached derived requirement authenticates
every committed type in O(immediate-child) time and nested function pointers
consume that requirement. Binder names are alpha-irrelevant provenance while
arity is structural identity. Typeck and in-memory semantic-results storage,
canonical instance v2, semantic metadata v1.1, and declaration metadata v2.x
fail closed on nonzero function-pointer binders because those consumers cannot
preserve the region topology;
their binder-zero bytes remain unchanged. Binder depth and executable support
remain deferred.
Reservation impls are now canonical `hir-v35`: impl polarity is a closed
positive/negative/reservation enum, exact cfg-effective reservation metadata
is authenticated during lowering, invalid placement/forms and `Drop`
reservations reject, and coherence permits overlap without treating a
reservation as a positive provider. Legacy formats without the third polarity
must reject it.
Impl-header unification remains symmetric across generic parameters. A narrow
compiler-closed coherence rule authenticates a local safe, non-auto,
zero-generic `lang = "fn_ptr_trait"` trait together with the exact word
`rustc_deny_explicit_impl` attribute and a direct self-parameter predicate;
only then is that blanket disjoint from structural non-function-pointer self
roots. Explicit positive or negative impls of deny-marked local traits reject,
while all ordinary predicates and imported/reference-only traits remain
predicate-blind overlap candidates.
The same symmetric gate uses an owned bare type parameter's implicit `Sized`
bit only against intrinsically unsized roots (`str`, slice, and trait object)
or a bounded final-field chain of local zero-generic structs. This covers the
Rust 1.90 `CloneToUninit` slice, `CStr`, and `ByteStr` families without treating
generic or arbitrary ADTs as unsized. `?Sized` and parameter-chain cases remain
overlap candidates in both declaration orders.
Inherited specialization members compare fail-closed beyond 256 nested type
edges or a shared 4,096 nodes across the complete alias or method signature.

## M7: Bootstrap and current stable

| ID | State | Task | Acceptance |
|---|---|---|---|
| M7-01 | TODO | Build Rust 1.90 core/alloc/std/test | Libraries are non-empty and execute probes |
| M7-02 | TODO | Build Rust 1.90 rustc and cargo | Binaries report pinned versions and compile hello |
| M7-03 | TODO | Reproduce Rust 1.91.1 self-host | Deterministic stage comparison passes |
| M7-04 | TODO | Advance official version ladder | Each pinned rung builds the next |
| M7-05 | TODO | Build Rust 1.97.1 | rustc/cargo compile representative workspace |

Only the checksum-exact Rust 1.90.0 source archive is currently cached; its
extracted cache is library-only and does not contain compiler/bootstrap
sources. The committed 1.91.1 through 1.97.1 hashes match the official Rust
distribution checksum endpoints, but those later archive bytes are not local
and therefore have not been content-verified here. The highest-value next
ladder artifact is a no-network, checksum-enforcing 1.90.0-to-1.91.1 stage3
comparison gate that consumes explicit rustc/cargo and archive inputs, works
in isolated scratch, records normalized artifacts, and requires version,
hello-world, and Cargo-workspace probes. The root `TestRustcBootstrap.sh` is a
useful upstream-mrustc oracle for that rung, but its unchecked downloads and
repository-local scratch are not acceptable as final `cmrustc` evidence.

## M9: Lenient bootstrap pipeline (revised strategy, 2026-08-26)

See `docs/ROADMAP_190.md`, "Revised strategy". These tasks supersede the
declaration-profile path for M6-06/M6-07; M4/M5 entries are absorbed by
M9-04..M9-06 and will be closed when those land.

| ID | State | Task | Acceptance |
|---|---|---|---|
| M9-01 | DONE | Expression-position macro expansion for core bodies | `probe_core_hir --body-census` at `da886a58`+: 0 failed, 0 unsupported; only 56 `asm!` and 3 `offset_of!` retained for HIR lowering |

M9-01 baseline (2026-08-26, `probe_core_hir --body-census` on pinned core):
22,524 bodies, all with an AST; 19,298 macro-free and 3,226 containing 4,510
macro invocations; 8,547 `let` statements (24 `let-else`), 486 item
statements inside bodies. Expression kinds: path 96,623, block 34,879, call
26,684, literal 22,622, method_call 17,572, unary 8,901, binary 7,902, tuple
4,906, macro 4,510, field 3,538, if 3,000, cast 2,795, assign 2,723,
tuple_field 2,032, match 698, index 696, return 599, array 569,
qualified_path 513, struct 409, try 383, closure 362, range 361, while 199,
for 133, loop 117, break 71, try_block 52, continue 14, raw_reference 10,
let 2. Invocations are dominated by `core::arch` `macro_rules`
(`static_assert_uimm_bits` 932, `simd_shuffle` 468, `static_assert_rounding`
389, `simd_extract` 373, `swap_bytes` 244, ...) and ordinary `macro_rules`
(`assert_unsafe_precondition` 130, `matches` 75, `debug_assert` 69, `write`
55, `unreachable` 9); the compiler builtins reached are `cfg` 264, `assert`
221, `panic` 119 (via `panic_2021` to `const_format_args`), `stringify` 85,
`asm` 42, `format_args` 10, `cfg_select` 5, `offset_of` 3, `addr_of` 3.

M9-01 progress (2026-08-26, commit `078f2496` and follow-ups): the lenient
pass in `src/resolve/body_expand.c` reaches 14,889 invocations (nested
expansions included) and expands 8,329 `macro_rules`/`macro` and 6,488
builtin uses with zero failures. The whole-core census now reports 64 bodies
still holding 69 invocations: 56 `asm!` and 3 `offset_of!` (a `builtin #`
`pub macro`) are retained on purpose for HIR lowering, and 10 `format_args!`
uses need width/precision placeholders (`rt::Placeholder`). Fixes on the way
there: textual scope now includes ancestor modules, `use`-imported macro
bindings, and the crate-root macro namespace; relative first segments resolve
through imported modules; nested `concat!`/`stringify!` fold textually;
`cfg_select!` arms evaluate through the cfg environment with comments
skipped; and the `macro_rules` matcher rejects `expr` candidates containing a
top-level `,`/`;`/`=>` and requires them to parse, which turned SipHash's
`compress!` from a step-limit failure into a 0.2 s pass (the whole-core probe
is back to about 13 minutes, of which expansion is under a second).
The final slice adds width/precision/fill/alignment/sign/`#`/`0` and
`{:x?}` specs through `Arguments::new_v1_formatted` with `rt::Placeholder`
records and `rt::Argument::from_usize` count arguments, closing M9-01:
14,889 invocations, 8,329 rules and 6,501 builtins expanded, zero failures,
59 retained (`asm!` 56, `offset_of!` 3) that M9-02 lowers to dedicated
HIR nodes.

M9-02 (2026-08-27, `bd0f4917`+): `src/hir/ubody.c` lowers every core body
into the untyped `ubody` IR — 22,524/22,524 bodies, 321,921 expressions,
743 nested item statements recorded, 59 retained `asm!`/`offset_of!`
nodes, 0 failures. Path resolution inside bodies covers locals, items,
enum variants (including bare identifier patterns), `Self`, generic
parameters, primitive prefixes, type + associated tails, the implicit
`crate::prelude::v1`, and body-local items; 2,257 paths remain unresolved,
dominated by `core::arch` SIMD type names (`i32x4::new`, ...) reached through
glob imports of macro-generated items, which typeck or the resolver must
still cover. Nested items are not yet lowered as HIR items.

M9-04 first pass (2026-08-27): `src/hir/ty.c` (hash-consed structural type
arena with union-find inference variables, occurs check, substitution,
HIR-to-Ty conversion, printer) and `src/hir/tyck.c` (lenient inference over
`ubody`: literals, paths, calls, method calls with autoderef, fields,
indexing, operators with operator-trait fallback, closures, match/if/loops
with break typing, struct literals and patterns, coercions, a pending
worklist retried until it stops progressing, then integer/float defaulting).
Whole core: 16,339/22,524 bodies fully typed, 6,185 partial, 8,043
unresolved nodes, 12,858 error nodes. Error classes, largest first:
body-local item not lowered 5,564; unresolved value path 2,180; body type
does not match its signature 1,099; call argument mismatch 1,058;
qualified path not resolved 653; call through non-function type 540; range
type not modelled 387; method not found 333; method argument mismatch 260;
operator on unsupported type 169; struct literal path 127; for loop over
non-array iterable 122; assignment mismatch 109; branch types 67; deref of
non-pointer 66; return mismatch 35; field not found 25; associated value
16; tuple field 14; path pattern 7.

M9-04 second pass (same day): body-local items are typed from their AST
declaration, `<T as Trait>::item` resolves through the same associated-item
search, `a..b` builds the real `core::ops` range ADTs (found through a
source-indexed AST map so re-exported items resolve), calls through
`Fn`/`FnMut`/`FnOnce` bounds take their parameter tuple and `Output`
equality from the predicate, callable ADTs go through `call`/`call_mut`/
`call_once`, and `for` loops derive the item type from `into_iter`/`next`.
Whole core: 17,101/22,524 bodies typed (was 16,339), 5,478 unresolved
nodes (was 8,043), 6,254 error nodes (was 12,858).  Remaining classes:
unresolved value path 2,180 (the `ubody` SIMD paths); call argument
mismatch 1,388; body vs signature 1,101; method not found 354; method
argument mismatch 297; operator on unsupported type 194; call through
non-function 159; struct literal path 127; assignment mismatch 114.

M9-04 third pass (same day): associated-type projections normalize —
`<Self as Trait>::Assoc` resolves through matching impls' associated type
items, and on `T`/`Self` receivers through trait-predicate equalities;
coercion normalizes both sides before unifying and stays lenient about
residual projection/param mismatches.  Alongside this the type arena and
typeck were hardened against `CmTy*` pointers held across type creation
(the arena vector moves): loop counts and children are copied to locals
before any allocating call (`cm_ty_unify`, occurs/infer/subst/resolve
walkers, `cm_ty_print`, matcher, normalize, fn-bound and iterator-item
helpers); the tyck unit test is ASan/UBSan clean.  Whole core:
18,196/22,524 bodies typed (was 17,101), 5,034 unresolved nodes (was
5,478), 4,413 error nodes (was 6,254).  Remaining classes: unresolved
value path 2,180 (the `ubody` SIMD paths); body vs signature 608; method
not found 354; call argument mismatch 195; operator on unsupported type
194; method argument mismatch 180; call through non-function 159; struct
literal path 127; assignment mismatch 95.

M9-04 fourth pass (2026-08-27): resolver bindings now join to HIR items
by exact AST identity — `CmHirItem` records its declaring
`(ast_source, ast_item)` and `ubody`/typeck look items up by that pair
instead of by span, which failed for every macro-generated item (their
HIR spans are coarse invocation anchors).  `ubody` also honors
body-local `use` declarations: block item statements collect parsed
use-tree entries (named, aliased, enum-variant globs, module globs) and
path resolution substitutes them before and after module-level lookup.
Together these clear the `core_arch` SIMD family and
`IntErrorKind`-style variant imports.  Whole core: `ubody` unresolved
paths 2,257 -> 44; 19,928/22,524 bodies typed (was 18,196), 4,089
unresolved nodes (was 5,034), 2,115 error nodes (was 4,413).  Remaining
classes: body vs signature 608; method not found 346; operator on
unsupported type 194; call argument mismatch 191; method argument
mismatch 177; call through non-function 152; assignment mismatch 93;
deref of non-pointer 73; branch unify 71; qualified path 57.

M9-04 fifth pass (same day): coercion falls back to a deep lenient
structural equality — any position holding a projection, generic
parameter, `Self`, opaque, or const argument on either side is accepted
(clears `Result<T, TryFromIntError>` vs `<T as TryFrom>::Error` and the
`Option<...Item>` families); branch joins route through full coercion;
method receivers normalize projections and `[T; N]` unsizes to `[T]`
during lookup, so slice inherent methods (`len`, `as_ptr`, `iter`)
resolve on arrays.  Whole core: 20,323/22,524 bodies typed (was
18,196 two passes back), 1,463 error nodes (was 2,115).  Remaining:
body vs signature 316; operator 196; method-arg 163; call-through-
non-fn 152; call-arg 130; method not found 119; assignment 75; deref
73; qualified 57; unresolved value path 44 (the `impl_Display!` fmt
helpers); branch 40; field 31.

M9-04 sixth pass (same day): body expansion strips cfg-inactive
statements and tails from blocks in place (a trailing semicolonless
expression statement is promoted to the new tail), so
`#[cfg(feature = "optimize_for_size")]` alternates and their dead
callees disappear (`ubody` unresolved 44 -> 33).  Typeck: tuple/unit
constructors called through a non-function callee type resolve via
`cm_tyck_ctor_of`; binary operators normalize their left operand and
leniently accept projections/params/`Self`/const params; deref falls
back the same way (a `&`-pattern often binds the reference away);
qualified-path associated names are interned, not looked up.  Whole
core after the operator/deref/ctor pass: 20,437/22,524 typed, 1,187
error nodes (operator and deref classes cleared).  Remaining: body vs
signature 316; method-arg 162; call-through-non-fn 152 (unchanged —
population not the ctor shape); call-arg 129; method not found 116;
assignment 75; qualified 57 (fix landed after this run); branch 40.

M9-04 seventh pass (same day): method lookup runs in two phases —
inherent impls across every autoderef step win before any trait impl
is consulted, so blanket impls (`impl<H: Hasher + ?Sized> Hasher for
&mut H`) no longer capture inherent methods like `DebugStruct::finish`;
`Self(v)` and aliased tuple constructors type through the ADT-typed
callee; field and tuple-field bases normalize projections and accept
params/`Self` leniently; the probe gained `--source-map` (id -> path)
so debug spans resolve to files.  Whole core: 20,663/22,524 typed, 945
error nodes (was 1,187).  Remaining: body vs signature 244; method-arg
160; call-arg 130; method not found 111; assignment 75;
call-through-non-fn 70; branch 36; unresolved value path 33.

M9-04 eighth pass (same day): trait methods reached as plain
definitions get a fresh inferred `Self` in their first FN_DEF slot
(`fmt::UpperHex::fmt(self, f)` no longer borrows the enclosing impl's
Self); projection normalization scans specific impls before blanket
ones (`impl<T, U: Into<T>> TryFrom<U> for T` no longer shadows
`impl TryFrom<u16> for u8`); and the parser's binary precedence now
matches Rust — bitwise `&`/`^`/`|` bind tighter than comparisons, all
comparisons on one level (`x & FLAG != 0` parsed wrong before).  Whole
core: 20,854/22,524 typed, 747 error nodes (was 945).  Remaining:
method-arg 160; call-arg 127; method not found 111; body vs signature
103; call-through-non-fn 70; branch 33; unresolved value path 33.

M9-04 ninth pass (same day, runs 15-21): trait bounds with structured
ADT subjects (`Simd<T, N>: SimdUint`, `P::Searcher: Searcher`) supply
methods for matching receivers (bare-parameter subjects still match
exactly); normalization matches parameterized trait arguments
(`SliceIndex<[T]>` vs `SliceIndex<str>`), with generic projection
arguments as wildcards; **statement-position expressions stop after a
block-like expression unless continued by `.` or `?`** — previously
`while .. {} (a, b)` parsed as a call (this silently mis-parsed 8 core
files; the probe now prints graph-error details); compiler-known method
names are interned, not looked up, so operator/deref/call lookups no
longer depend on a name appearing textually in some body.  Whole core:
20,827/22,524 typed, 759 error nodes — method-not-found 111 -> 85 and
non-fn-callee 70 -> 50 improved; assignment (72) and body-vs-signature
(129) regressed vs the eighth pass and are the next targets (the
`Wrapping<$t>` `*self = *self | other` family and `SliceIndex` bodies).
Remaining: method-arg 156; body vs signature 129; call-arg 126; method
not found 85; assignment 73; call-through-non-fn 50; branch 31.

M9-04 tenth pass (same day): method lookup selects among overlapping
impl candidates by the call's argument types — arguments are typed
first (closures deferred to the chosen method's expectation), then the
first candidate whose parameters accept them non-bindingly wins, with
the first candidate as fallback (`cm_tyck_lookup_assoc_in` gained a
`skip`).  This resolves the `impl_partial_eq!` double-direction impls
(`PartialEq<ByteStr> for [u8]`), the 27 overlapping `SliceIndex` impls
([T]/str/ByteStr on the same ranges), and friends.  Whole core:
20,966/22,524 typed, **534 error nodes** (was 759).  Remaining:
call-arg 106; method not found 85; body vs signature 75; assignment 73
(the Wrapping family — operator-route instrumentation in flight);
call-through-non-fn 50; unresolved value path 33; branch 31.

M9-04 eleventh pass (same day): projection receivers resolve methods
through the associated type's declared bounds (`trait Pattern { type
Searcher: Searcher<'a>; }` — clears the `haystack`/`next_match`
family); predicate subjects may also be projections; normalization
skips impls whose equality or Output target fails HIR conversion
instead of recursing on an invalid id.  Whole core: 20,970/22,524
typed, 516 error nodes (was 534).  Remaining: call-arg 106; method not
found 75 (30 = Simd `count_ones` family); assignment 73 (Wrapping —
`coerce-null=0` rules out invalid ids; next suspicion: normalize picks
a wrong-width impl); body vs signature 70; call-through-non-fn 50.

M9-04 twelfth pass (same day): the parser attaches expression-statement
attributes to the expression, so cfg stripping now also consults the
statement expression's attributes — `#[cfg(feature =
"optimize_for_size")] { offset = fmt_u64(...) }` statements inside
macro-generated bodies are finally stripped (the `impl_Display!`
`${concat(...)}` helper family).  Whole core: 21,009/22,524 typed, 497
error nodes (was 516); `ubody` unresolved paths 33 -> 14.  The Wrapping
assignment family is now pinned by `assignment-normalized` output:
`<Wrapping<u8> as BitOr>::Output` normalizes through the blanket
`impl<T> BitOr<NonZero<T>> for T` because no specific impl matches in
scan-0 — the blanket-fallback statistics run will show why.
M9-04 thirteenth pass (same day, runs 31-35): a large correctness batch
— argument-aware candidate selection extended to binary operators
(blanket `impl<T> BitOr<NonZero<T>> for T` no longer shadows the
Wrapping impls) and to qualified associated calls (`<u64>::from`);
slice `rest @ ..` bindings lower as real REST patterns and type as the
subslice (the AST REST kind silently lowered to WILD before); the
lexer no longer eats `999.try_into()` as a float — this alone cleared
the whole call-through-non-function class; speculative candidate
probes roll back inference bindings via a new arena undo log
(`cm_ty_undo_mark`/`cm_ty_undo_to`); candidate cap 32; lenient
projection-receiver methods; field access through user `Deref`
(`ManuallyDrop<Waker>`); debug spans carry the enclosing fn name.
Whole core: 497 -> 444 -> 415 -> 361 -> 355 -> **327 error nodes**,
21,082/22,524 bodies typed.  Remaining: call-arg 87; method not found
69 (30 = Simd integer-op family); body vs signature 56; assignment 19;
branch 17; assoc-value 16; unresolved value path 14; field 7.

M9-04 fourteenth pass (2026-08-27 late, runs 43-49): generic associated
types normalize by freshening the associated item's own parameters
(`type Cast<T2> = Simd<T2, N>` — LENIENT(gat), GAT arguments not yet
threaded); method lookup gained a four-pass order — inherent, specific
trait impls with the item, blanket (bare-parameter-self) trait impls,
then declaration fallbacks — so `impl<T: Clone> SpecArrayClone for T`
no longer captures every `clone` and childless marker impls no longer
shadow real ones (the `searcher_methods!` wrapper family); method-call
turbofish arguments bind the method's own type generics positionally.
Whole core: 310 (GAT alone) -> 302 error nodes, 21,104/22,524 typed.
Remaining: call-arg 85; body vs signature 69; method not found 31;
branch 17; assoc-value 16; assignment 16; unresolved value path 14
(cfg'd arch helpers); field 10.

M9-04 fifteenth pass (2026-08-27, run 50): projections carry their
associated type's own arguments (children = [self, trait args...,
GAT args...], trait-arg count in `b`) and normalization binds the
freshened GAT parameters to them — the LENIENT(gat) hole is closed;
the argument-compatibility retry extends to path callees resolving
through primitives, `Self`, and type-assoc bases (`usize::from(x)`,
`Self::try_from(n)`).  Whole core: **208 error nodes** (was 302),
21,156/22,524 bodies typed; call-arg 85 -> 7, branch 17 -> 9,
assignment 16 -> 8.  Remaining: body vs signature 69; method not
found 31; assoc-value 16; unresolved value path 14 (cfg'd arch
helpers); field 10.

M9-04 sixteenth pass (2026-08-27, run 51): method-call turbofish
arguments are read directly from the AST method-call node (ubody's
generic-arguments path ref was a stub that was always NONE) and bound
to the chosen method's own type generics — `self.cast::<$unsigned>()`
finally pins the element width, collapsing the Simd `Unsigned` family.
Whole core: **179 error nodes** (was 208), 21,214/22,524 bodies typed.
Remaining: body vs signature 44; method not found 29; assoc-value 16;
unresolved value path 14 (cfg'd arch helpers); field 10; branch 9;
path-pattern 8; assignment 8.

M9-04 seventeenth pass (2026-08-27, run 54): method autoderef takes one
user-`Deref` step when reference/pointer stripping is exhausted
(`&ByteStr` receivers reach `[u8]` inherent methods), with candidate
enumeration and rollback intact.  Whole core: **175 error nodes** (was
179), 21,215/22,524 typed.  The identical-print SliceIndex body-sig
family (~16) is under def-identity diagnosis — both sides normalize to
the same rendered projection, so the suspected culprit is two distinct
HIR defs for one nominal type.

M9-04 eighteenth pass (2026-08-27, run 56): the candidate
argument-compatibility check compares pointees across reference/pointer
mixes (`&str` coerces to `*const str`) — previously every candidate for
`self.get_unchecked(slice)` looked incompatible and the fallback picked
the first (`[T]`) impl, poisoning the projection's trait arguments.
Whole core: **156 error nodes** (was 175), 21,234/22,524 typed;
body-vs-signature 44 -> 25.  Remaining: method not found 25; body vs
signature 25; assoc-value 16; unresolved value path 14 (cfg'd arch
helpers); field 10; branch 9; path-pattern 8; assignment 8.

M9-04 nineteenth pass (2026-08-27, run 57): unresolved projections act
as wildcards on the receiver side of impl matching (`Option<&T>`
patterns accept `Option<<I as Iterator>::Item>` receivers), so
`Option::copied`/`cloned` beat Iterator's provided adapters.  Whole
core: **153 error nodes** (was 156); method-not-found 25 -> 17, body
vs signature 25 -> 23, operator resurfaced at 8 (shifted receivers).
Remaining: body vs signature 23; method not found 17; assoc-value 16;
unresolved value path 14; field 10; branch 10.

M9-04 twentieth pass / stage checkpoint (2026-08-27, run 62): **153
error nodes**, 21,232/22,524 bodies typed (99.3% of expressions).  The
residue is explained scatter, none blocking: body-vs-signature 23
(SlicePattern/Copied remnants, `position` closure ties), method not
found 17 (spec-trait `&mut Self`, SliceIndex-Output receivers),
assoc-value 16 (dec2flt/portable-simd assoc consts), unresolved value
path 14 (cfg'd target-feature helpers), field 10 (error cascades),
branch/path-pattern/operator/assignment 8-10 each.  M9-04 moves to
residue mode — revisited when MIR consumption (M9-05) shows which
residue matters — and **M9-03 (whole-context multi-crate) is now
ACTIVE**: lower alloc, then std, against the in-memory core context.

M9-04 statement-boundary refinement (2026-08-28, run 63): **151 error
nodes**, 21,233/22,524 bodies typed.  Block-like expressions in
statement position now stop only when the expression begins at the
statement's first token, so parenthesized forms like
`(unsafe { … }) != 0` keep their binary tail (alloc parse regression);
core improved 153 -> 151.

M9-03 first pass (2026-08-28): cross-crate import resolution.  The
import resolver accepts registered dependencies
(`cm_import_resolver_add_dependency`, stored across resolve cycles);
a still-unresolved leaf whose first segment names a dependency is
resolved through the dependency's own resolver — single and aliased
leaves via `cm_import_resolve_path_checked` in all three namespaces,
globs by enumerating the target module's public bindings, and
`{self}` leaves by dropping the `self` segment (module, enum, and
type prefixes).  Synthesized bindings keep the dependency's
source-qualified declaration refs (valid via the shared CmSourceSet)
and carry a new `CmResolvedBinding.dependency` tag so dep-graph
`target_module` ids are never read as local module ids.
`probe_core_hir --with-core <core lib.rs>` builds core (graph 0
errors, 451 modules; imports 0 errors; dependency-macro artifact ok)
and resolves the target against it: alloc `use core::…` imports
**598 -> 0** (426 shared sources, 68 alloc modules).  Next frontier:
alloc HIR lowering rejects the `unsafe extern "Rust"` allocator
block (abi allowlist + rustc_* foreign-fn attributes).

M9-03 second pass (2026-08-28): whole-pipeline cross-crate lowering.
`extern "Rust"` blocks admitted; core lowers into the shared
CmHirContext first (`core-hir errors=0 items=38,626`, ~11 min);
dependency-tagged bindings map into that HIR — import targets by
context-wide AST identity, type/trait/alias paths via on-demand
heap-cached item records backed by the dependency graph's declaring
ASTs, module imports/paths via the dependency's module map
(`CmHirLowerDependency` on the lower options).  The import resolver
delegates direct `core::…` paths and imported-dependency-module
paths (`ptr::null_count`) to the dependency's resolver mid-walk.
ubody/tyck accept dependency bundles (`CmUBodyDependency`) and swap
the active graph bundle per body by module-map ownership, with
source-AST tables ingesting all graphs.  Dependency macro-namespace
bindings stay in the namespace tables but off the leaf records, so
`use` retention never stores them (macro semantics flow through the
dependency-macro artifact).  Acceptance: minicore repro
(`--body-census --with-core`) fully green — imports 0, hir 0
errors, ubody 5/5, tyck 5/5, 0 error nodes — covering direct paths,
module value paths, enum `{self}`, and cross-crate trait impls.
Whole-crate: alloc `use` imports stay 0, alloc HIR advances to
`raw_vec/mod.rs:402` (generic parameter attributes,
`#[unstable] A: Allocator = Global`).  Core single-crate regression
run 64: **151 error nodes** unchanged.  All three lanes + ASan/UBSan
green.

M9-03 third pass (2026-08-28): structurally inert generic-parameter
attributes (`may_dangle`, `unstable`, `rustc_*`, `allow`, `doc`) are
accepted and dropped during HIR lowering; semantic ones (`cfg`)
still reject.  Attribute text is the raw `#[...]` source range, so
matching strips the prefix.  The fail-closed lower test now asserts
the lenient contract for `#[may_dangle]` and keeps `#[cfg(test)]`
fail-closed.  Whole-crate: alloc HIR advances past `raw_vec` to
`collections/mod.rs:172` (supertrait path unresolved).  Lanes +
ASan green.

M9-03 fourth pass (2026-08-28): dependency prelude injection.  A
crate resolved against registered dependencies with no explicit
`#[prelude_import]` (rustc injects it implicitly; alloc has none)
synthesizes its prelude from the first dependency's
`prelude::rust_2024` module — public bindings re-interned,
dependency-tagged, pushed into the resolver's prelude table.
Minicore acceptance: an unimported `T: Hash` bound resolves and
types (6/6 bodies).  Whole-crate: alloc HIR advances past the
`SpecExtend<I: IntoIterator>` supertrait to
`in_place_collect.rs:216` (associated-type constraints).  Lanes +
ASan green.

M9-03 fifth pass (2026-08-28): associated-type constraints
(`Trait<Assoc: Bound>`) in dyn-trait bounds, where/bound
predicates, and supertraits are accepted and dropped (M9 leniency:
bound promises are not enforced by the lenient tyck; `cfg`-like
semantic forms unaffected).  The graph-lower fail-closed fixture
for `Shr<u8, Output: Marker>` moved out of the rejected list.
Minicore acceptance: `trait U: core::Sourcey<Item: Hash>` lowers,
6/6 bodies typed.  Whole-crate: alloc HIR advances past
`in_place_collect.rs` to `rc.rs:2937` (ADT generic default
structural substitution).  Lanes + ASan green.

M9-03 sixth pass (2026-08-28): closed zero-argument ADT generic
defaults (`A = Global`) reuse the declaration-owned type when
instantiating an ADT application that omits them (`Rc<[T]>`).
Minicore acceptance: `RcLike<T, A = Global>` instantiated as
`RcLike<u32>` lowers and types (7/7 bodies).  Whole-crate: alloc
HIR advances past `rc.rs` to `task.rs:93` (custom `self: Arc<Self>`
receiver whose wrapper now carries the defaulted allocator
argument).  Lanes + ASan green.

M9-03 seventh pass (2026-08-28): custom receiver wrappers accept
extra generic arguments beyond the Self chain (`self: Arc<Self>`
where `Arc<Self, Global>` carries the defaulted allocator) — the
first wrapper argument must still chain to the enclosing Self.
Minicore acceptance: `fn wake(self: RcLike<Self>)` trait method
lowers (7/7 bodies typed).  Whole-crate: alloc HIR advances past
`task.rs` to `raw_vec/mod.rs:402` (`unsafe impl Drop` — dropck
eyepatch impl safety vs safe trait).  Lanes + ASan green.

M9-03 eighth pass (2026-08-28): `unsafe impl` of a safe trait is
admitted in both the lowerer and the model validator (the
dropck-eyepatch pattern `unsafe impl<#[may_dangle] T> Drop`
requires it); a safe impl of an unsafe trait still rejects.  The
two fail-closed fixtures asserting the old symmetric rule
(test_model, test_lower) moved out of their rejected lists.  With
that admitted, alloc HIR lowering proceeded past every prior
frontier and the census timed out inside the O(items) linear
dependency-item scan — replaced by a lazily built sorted
(source, ast_item) index with binary search
(`cm_lower_build_dependency_index`).  Minicore acceptance: unsafe
impl of a safe dep trait lowers and types (8/8 bodies).  Indexed
whole-crate census in flight; numbers in the next entry.  Lanes +
ASan green.

M9-03 ninth pass (2026-08-28): the synthesized dependency-record
cache also rides the sorted (source, ast_item) index — each index
entry memoizes its record pointer, so every dependency type-path
lookup is one binary search instead of a linear scan over the
growing record list (the previous census was killed ~30 min in,
still inside alloc HIR lowering).  Minicore acceptance unchanged
(8/8).  Lanes + ASan green; census re-running with the indexed
cache.

M9-03 tenth pass (2026-08-28): dependency macros in body
expansion.  `CmBodyExpandOptions` accepts the dependency-macro
artifacts; unqualified body macros fall back to
`artifact_lookup(extern_name, name)` after every local scope
fails, and dependency-qualified / `$crate`-generated macro paths
(`core::write!`, `core_crate::…`) resolve through the artifacts
before the local module walk.  Expanding a dependency macro
substitutes `$crate` with that artifact's extern name, so
generated paths (`core::cmp::Ordering::Equal`) resolve via the
resolver's dependency delegation.  Minicore acceptance: 3/3
dependency invocations expand (unqualified, qualified, `$crate`),
9/9 bodies typed, 0 errors.  Targets the 158 alloc body-expand
failures (debug_assert 85, panic_2021 27, debug_assert_eq 26,
unreachable_2021 8, write 7, assert_unsafe_precondition 3,
debug_assert_ne 2).  Lanes + ASan green.  Whole-crate confirmation:
alloc body-expand **574/574 invocations, 0 failures** (was 158).

M9-03 eleventh pass (2026-08-28): type-position macro expansion.
Body expansion now expands macros in type position in place —
alloc's `impl SpecToString for to_string_str_wrap_in_ref!(...)`
family (string.rs:2985, the whole-crate HIR frontier after body
macros).  The expansion text parses as one type
(`cm_parse_type_fragment`) and splices over the macro node,
recursing through type children and nested invocations up to the
depth cap, with the per-target `$crate` identity; impl headers
expand in the module walker (top-level impls bypass the item
walker).  The probe prints per-stage lowering seconds.  Minicore
acceptance: recursive local `wrapref!(x)` self type lowers and
types (10/10 bodies, 0 errors).  Lanes + ASan green.

M9-03 twelfth pass (2026-08-28): perf-profiled lowering (perf on a
live probe: `cm_lower_find_graph_declaration` 41%,
`cm_hir_associated_equalities_valid` 24%, `cm_vec_at_const` 17%).
The local item-record lookup now rides a sorted
(source, ast_id) -> record-position index maintained on every
push (binary search + neighbor scan for duplicate counting;
insertion memmove is negligible).  The previous census timed out
at 90 min still inside alloc HIR lowering; core-hir alone measured
731s.  Minicore acceptance unchanged (10/10).  Lanes + ASan green;
timed census running.

M9-03 thirteenth pass (2026-08-28): the global associated-name
ambiguity authentication is retired (S-shortcut).  perf on the
indexed binary showed `cm_hir_associated_equalities_valid` at ~95%
of alloc lowering — `cm_hir_associated_name_unambiguous` scanned
every context item (38k+) per associated-type equality
(`Iterator<Item = T>` appears thousands of times in alloc) and
rejects only inputs rustc already rejected.  It now returns 1; the
caller's trait-reachability check still anchors each equality.
The diamond-ambiguity model fixture asserts the lenient contract.
Lanes + ASan green; timed census running.

M9-03 fourteenth pass (2026-08-28): with the indexes and the
ambiguity shortcut, **core-hir 731s -> 54s** and alloc HIR lowering
runs to completion of its frontier in 2063s (still hot; profile
re-running).  The frontier moved to `btree/node.rs:103` —
`[MaybeUninit<...>; 2 * B]`, a const-expression array length.  The
literal const-expression evaluator now resolves single-segment
const names through the module scope and evaluates their
initializers recursively across ASTs (`B = 6`,
`CAPACITY = 2 * B - 1`), depth-capped, overflow-checked.  Minicore
acceptance: `[u8; 2 * BB]` and `[u8; CAP2]` lower and type (12/12
bodies, 0 errors).  Lanes + ASan green.

M9-03 fifteenth pass (2026-08-28): O(1) definition lookup.  perf on
the alloc phase showed `cm_hir_lookup_definition` + comparators at
~99% — the O(1) path was gated on `crates.len == 1`, so a two-crate
context degraded every lookup to a 38k-entry scan.  `CmHirCrate`
now records `definition_base` (the vector position of its
definition index 1); both lookups compute the position directly
and verify the id before trusting it, falling back to the scan
only if contiguity broke.  **alloc HIR lowering: 2063s -> 3s**
(core-hir stays 54s).  The frontier advances to `btree/map.rs:2354`
(tuple parameter patterns in trait/impl methods).  Lanes + ASan
green.

| M9-02 | DONE | Untyped HIR lowering of every core body (all expression and pattern forms) | `probe_core_hir --body-census` at `bd0f4917`+: 22,524/22,524 bodies lower into `ubody` (321,921 expressions, 0 failures) |
| M9-03 | ACTIVE | Whole-context HIR snapshot and multi-crate lowering | alloc and std lower against a loaded core snapshot with zero errors |
| M9-04 | ACTIVE | Inference-based typeck over lowered bodies | all core/alloc/std bodies typed; no fabricated types (first whole-core pass: 16,339/22,524) |
| M9-05 | TODO | HIR-to-MIR for all bodies | all typed bodies produce MIR |
| M9-06 | TODO | C emission and link for core/alloc/std | `hello` links and runs under TinyCC and GCC |

## M8: Stage0 provenance

| ID | State | Task | Acceptance |
|---|---|---|---|
| M8-01 | TODO | Add `cmrustc-stage0` live-bootstrap recipe | Bounded internal-CI image succeeds |
| M8-02 | ACTIVE | Prove TCC-built compiler reproducibility | Producer chain and checksums recorded |
| M8-03 | TODO | Resolve i686-musl vs x86_64-musl Rust host convergence | Chosen target builds/runs stage compiler |
| M8-04 | TODO | Add source-built CMake/LLVM prerequisite path | LLVM closure contains no imported binaries |
| M8-05 | TODO | Run full builder-hex0/QEMU chain | Clean image reaches cmrustc milestone |
| M8-06 | TODO | Run complete Rust chain in provenance closure | No unaccounted host binary remains |

### Active stage0 evidence

- M8-02 has a reproducible development-host gate for the HCC path in
  `blynn-bootstrap` commit `c1684269`. A forced local rebuild of
  `tinycc.m2.precisely.m2` completed the byte-identical stage3/stage4 TinyCC
  fixpoint and reproduced Nix output
  `ngj284h9ag7fq8m39ramj667379hndr2-tinycc-boot-hcc-m2-precisely-m2-unstable-2025-12-03`.
  Its 459,844-byte TCC has SHA-256
  `0a523db52cc19827d2954d7b6470bd377eed62eea8dfb967fc3cb0f8ffe8c296`.
  At historical compiler commit `4363c1ed`, that exact compiler and bundled
  Mes libc built `cmrustc` twice to the same 807,524-byte executable with
  SHA-256
  `91a316ca42472a89afcb17e933a31fde9d2292bfba6519f9f87df1e27cb84a6f`.
  This is a same-source reproducibility record, not the artifact identity of
  later commits; every release/provenance record must bind its binary hash to
  the exact `cmrustc` source revision and repeat the twin build.
  `make stage0-hcc` runs source diagnostics, lexer/codegen tests, an AST parser
  smoke, and the complete no-core compile/link/execute acceptance suite using
  only that TCC output. The bootstrap libc omits standard integer constructor
  macros, `mkstemp`, and several declarations; local fallbacks preserve exact
  integer widths, collision-safe atomic output publication, process status,
  and formatting behavior. This is an x86-64/TinyCC 0.9.28-era development
  proof. The HCC dependency was substituted, and the i386 live-bootstrap
  recipe/QEMU closure remain open, so M8-01, M8-02, and M8-05 are not complete.

  A forced current-source rebuild at exact `cmrustc` commit `0e042300` also
  passed `make -B stage0-hcc` under Nix with that pinned HCC-origin TinyCC.
  The resulting 956,980-byte compiler identifies TinyCC 0.9.28 and has SHA-256
  `0f96f9080efe7ca85c12eb3e48d432878a01c7f6973dcdf93bd807d3d1df7a41`.
  This binds a newer development artifact to its source revision but remains
  x86-64 host evidence, not the native i386 live-bootstrap proof.

  At exact commit `05f38c38`, two isolated `git archive` source snapshots built
  concurrently with the same pinned TinyCC output both passed the complete
  stage0 smoke suite and produced byte-identical 1,070,684-byte binaries with
  SHA-256
  `4d6716ce234415acf2d6f282e294098768d4b2545aec5027a3a0deefc825f69c`.
  This is a refreshed deterministic x86-64 development record; it does not
  close the substituted-HCC or native i386 live-bootstrap requirements.

The authoritative integration tree is
`/home/siraben/nixos-full-source-bootstrap/live-bootstrap` at detached commit
`dd8ac27bf959344b9bcf5e876bdd7716879bbc70`; that checkout is dirty, so proof
runs require a clean clone or worktree. The practical package insertion is
immediately after musl pass 3 at manifest line 68, before `sed`, after TinyCC
pass 4 and `grep`. The native environment is static
`i386-unknown-linux-musl`, with prefix `/usr`, target library directory
`/usr/lib/i386-unknown-linux-musl`, `CC=tcc`, and `AR='tcc -ar'`. The existing
`rootfs.py --internal-ci cmrustc` value parses, but the manifest has no matching
package or conditional stop. A native proof still needs a clean checkout, a
pinned hosted `cmrustc` source archive and checksum, a pass1 recipe, the
manifest entry/stop, the package checksum, and native i386-musl link/run. At
that early insertion point the minimal acceptance is build/install plus one
no-core emit-C, link, and exit-status probe; the wider shell suite depends on
tools such as `awk` and `cmp` that appear later in the manifest.
The exact build/emit/TCC-link/exit-7 shape passes on x86-64 with Nix TinyCC,
but that is only recipe validation. The bounded native command after recipe
integration is:

```sh
nix develop --command ./rootfs.py --bwrap --arch x86 \
  --target /absolute/fresh/cmrustc-proof --external-sources \
  --cores 4 --internal-ci cmrustc
```

M9-03 sixteenth pass (2026-08-28): tuple parameter patterns in
bodyful methods.  Lowerer gate and model placement relaxed for the
two-binding case (`extend_one(&mut self, (k, v): (K, V))`); the
general case requires exactly two bindings, unary tuples stay
rust-call-only.  Shared-reference patterns over shared-reference
elements bind the pointee (`(&k, &v): (&K, &V)`, btree map/set) in
both the lowerer and the model local check.  Fixtures updated
(model binary-tuple and pointee-local sub-cases, lower method-form
and rust-call negatives).  Minicore: tuple-param and deref-binding
methods lower and type (14/14, 0 errors).  Lanes + ASan green.

M9-03 seventeenth pass (2026-08-28): lenient impl-self class.  Impl
self types outside the authenticated subset (`Pin<Box<G, A>>`
nested applications, dependency-ADT heads, repeated/unconstrained
parameters, non-ADT slice elements — boxed.rs:2094 frontier) are
admitted as `CM_LOWER_IMPL_SELF_LENIENT` and excluded from the
overlap authentication (rustc already validated coherence); genuine
same-class overlaps still reject.  Seven lower-test fixtures moved
to the lenient contract.  Minicore: a dependency-ADT impl head
(`impl Marker2 for core::RcLike<Eyepatch>`) lowers and types
(15/15, 0 errors).  Lanes + ASan green.

M9-03 eighteenth pass (2026-08-28): ADT-argument implicit-Sized
disjointness.  Two ordered-generic impls over the same ADT head are
disjoint when one binds an owned, implicitly Sized type parameter
at an argument position where the other holds a definitely unsized
concrete type — core's `DerefPure for Cow<'_, T>` vs
`Cow<'_, str>` / `Cow<'_, [T]>` family (borrow.rs:354 frontier).
Minicore: the CowLike<T>/CowLike<str> impl pair lowers and types
(17/17, 0 errors).  Lanes + ASan green.

M9-03 nineteenth pass (2026-08-28): positive/negative impl
coexistence.  A negative impl exists to carve a shape out of a
positive blanket (`!Iterator for Box<[I], A>` vs
`Iterator for Box<I, A>`, boxed/iter.rs:98 frontier); the polarity
conflict now fires only for exactly equal self types.  Minicore:
a blanket-plus-negative pair lowers and types (18/18, 0 errors).
Lanes + ASan green.

M9-03 twentieth pass / MILESTONE (2026-08-28): the full cross-crate
pipeline runs end-to-end.  With the trait-argument guard —
"duplicate exact" and same-polarity overlap require structurally
alpha-equal trait arguments (positional parameter equality,
same-kind-unhandled compares equal) — alloc's `Join<&T>` vs
`Join<&[T]>` family admits, and census25 completed every stage:
**alloc HIR lowering 0 errors** (42,158 items, 24,797 bodies across
both crates, hir-lower 5s), body-expand 664/664, ubody
21,613/24,797 lowered, tyck 20,136 typed / 424 error nodes /
275,537 expressions.  M9-03's HIR acceptance (alloc lowers against
in-memory core with zero errors) is met.  Closed-world fixtures
whose authentications exceed rustc semantics moved to the lenient
contract (fn-ptr non-self bound, chained parameter overlaps,
open/closed trait-argument providers, TryFrom conservative loop).
Next frontier: ubody failed=3,184 "unexpanded macro invocation in
body" — with-core mode never body-expands the core graph; core's
bodies retain expression macros.  Lanes + ASan green.

M9-03 twenty-first pass (2026-08-28): core body expansion in
with-core mode.  The census path now runs cm_body_expand_graph on
the core graph too (core's own imports, crate_identifier "crate")
before core-hir lowering, so core's ~23.7k bodies get
expression-macro expansion instead of failing ubody with
"unexpanded macro invocation in body" (was 3,184 failures).
Minicore acceptance unchanged (20/20).  Lanes + ASan green;
census re-running.

M9-03 acceptance numbers (2026-08-28, census26): the two-crate
pipeline is green through every stage — core-body-expand 23,683
bodies / 14,860 invocations / 0 failed; core-hir 0 errors (51s);
alloc hir-lower 5s, 0 errors; combined HIR 42,158 items / 24,797
bodies; **ubody 24,797/24,797 lowered, 0 failed** (19 unresolved
paths: avx2/neon helpers, lang shims); tyck 23,071/24,797 typed,
**420 error nodes** over 352,590 expressions.  The loop moves to
the cross-crate tyck residue.

M9-04 two-crate pass (2026-08-28): unsize coercion in tyck.
`cm_tyck_coerce` gains `[T; N] -> [T]` directly and argument-wise
through one ADT application with the same definition
(`Box<[T; 1], A>` vs `Box<[T], A>` — the dominant family in the
alloc residue sample: 62 of ~93 alloc-side debug samples were
sig-normalized, led by unsize shapes in boxed/convert families).
Minicore acceptance: an array-to-slice unsize through a local
wrapper types (21/21, 0 errors).  Lanes + ASan green; census
re-running.

Census29 (2026-08-28, after unsize coercion): tyck error nodes
**420 -> 404** (body-signature 83 -> 75), 23,081/24,797 typed.
Remaining top families: body-sig 75, field-not-found 49,
method-not-found 41, call-arg 37, method-arg 28.

M9-01 grouping pass (2026-08-28): `$x:expr` captures transcribe as
one invisible group.  Textual macro transcription now parenthesizes
expr-fragment captures (rustc splices them as a single token
group), so `$is_zero(*self)` with `$is_zero = |x| x == 0` no longer
reparses as calling the literal `0` (alloc's impl_is_zero! — the
16-sample "non-function callee: i32" family).  Minicore: an
immediately-invoked closure through an expr capture types (22/22,
0 errors).  Lanes + ASan green; census re-running.

Grouping refinement (2026-08-28): census30 showed the parentheses
cut error nodes 404 -> 372 (body-sig 75 -> 59) but broke 1,504
body expansions — nested macro forwarding (`inner!($e)`) matches
raw tokens, not parenthesized groups.  The wrap now applies only
to multi-token expr captures; single tokens stay bare (already
unambiguous, matcher-compatible).  Minicore: 22/22, ubody 0
failures.  Lanes + ASan green; census re-running.

Grouping call-position rule (2026-08-28): census31 still showed 860
ubody failures (multi-token expr captures forwarded into nested
macro matchers).  The wrap now applies only in call position — the
transcriber peeks the next pattern node and parenthesizes a
multi-token expr capture only when a paren group follows
(`$f(args)`); all other splices stay raw.  Minicore: 22/22, ubody
clean, impl_is_zero-style call still fixed.  Lanes + ASan green;
census re-running.

Census32 (2026-08-28): the call-position grouping rule verified
whole-crate — core-body-expand 23,683 bodies **0 failed**, ubody
**24,797/24,797** lowered, tyck 23,097 typed / **372 error nodes**
(404 before grouping; the 32-node gain held with no expansion
breakage).  Top residue families: body-sig 59, field-not-found 49,
method-not-found 41, call-arg 37, method-arg 28.

Receiver normalization (2026-08-28): method lookup normalizes the
receiver first, including through one reference/pointer layer.
Targeted traces (CM_TYCK_DEBUG_FN=into_key_val_mut_at) showed
btree's `(&mut *p.get_unchecked_mut(i)).assume_init_mut()` chain
reaching candidate matching with the raw
`&mut <usize as SliceIndex<[T]>>::Output` projection — the
signature-returned projection was never normalized before lookup.
Minicore: a method call on a projection-typed receiver resolves
(24/24, 0 errors).  Lanes + ASan green; census re-running.

Census35 (2026-08-28): 372 -> 370 error nodes — the receiver
normalization landed but barely fired; the SliceIndex projections
evidently miss inside cm_tyck_normalize itself.  Targeted trace
re-running on the new binary to capture the normalize-miss reason.

Inner-lookup normalization (2026-08-28): the receiver
normalization moved into cm_tyck_lookup_assoc_in — the method-call
candidate loop calls the inner lookup directly and bypassed the
wrapper, so btree's projection receivers still reached matching
raw.  All seven lookup callers now normalize (root plus one
reference/pointer layer).  Minicore 24/24.  Lanes + ASan green;
census re-running.
- Measured pass: pointer unsizing now binds the slice element (`*const [T; N]` → `*const [_]` coercion unifies the array element into the elided slice element instead of leaving it unbound behind the lenient accept; covers REF→PTR too). Repro: miniuser `ptr_unsize` (let-ascribed `*const [u32]` from `*const [u32; 3]`) — 28/28 bodies typed, 0 error nodes. Gates: gcc test, clang strict, tcc, ASan/UBSan all green. Full alloc census (41) in flight to measure btree `SliceIndex` projection cascade collapse from the 370-node residue.
- Measured pass: dependency index depointered (root-cause of position-dependent cross-crate misresolution: the lazily-built (source, ast_item)->HIR-item index cached `CmHirItem*` pointers into `hir->items`, which reallocates while the user crate lowers — appending one item to the dep crate flipped unrelated lookups to garbage records). Index now stores vec indices, dereferenced at lookup; also excludes the currently-lowering crate's own items (key spaces collide across crates). Repro: minicore trait SIx2<[T]> + inherent `impl<T> [T]` + miniuser `use_slice_proj`/`core::hash::Hash` impl — 30/30 bodies typed, 0 hir errors (was: wrong-namespace/resolver failures at unrelated lines). Gates: gcc test, clang strict, tcc, ASan/UBSan all green. Census43 in flight.
- Measured pass: unsize coercion through a reference to one ADT application (`&PolymorphicIter<[T; N]>` -> `&PolymorphicIter<[T]>`, core's array iter unsize helpers): REF/REF coerce now recurses into same-def ADT pointees before falling back to unify/lenient-eq. Repro: minicore `PolyIt<Tp: ?Sized>` + miniuser `unsz` (&PolyIt<[u32; 4]> -> &PolyIt<[u32]>) — 31/31 bodies typed. Gates: gcc test, clang strict, tcc, ASan/UBSan all green. Also: normalize's silent-miss diagnostics widened to fn-matched primitive selves (usize SliceIndex projections were invisible — miss lines were ADT-gated). Census45/46dbg in flight.
- Measured pass: normalize now binds the matched impl's trait arguments against the projection's (`SliceIndex<[T]> for usize` normalizing `<usize as SliceIndex<[MaybeUninit<V>]>>::Output` binds T; previously Output substituted to an unbound fresh var and btree's `(&mut *p.get_unchecked_mut(i)).assume_init_mut()` method lookup starved silently — no miss line because miss diagnostics were ADT-self-gated). Census47: error nodes 366 -> 348, method-not-found 41 -> 24, typed 23113 -> 23137, unresolved 4707 -> 4584. Gates: gcc test, clang strict, tcc, ASan/UBSan all green.
- Measured pass: function-body-local items (`fn retain { struct SetLenOnDrop { .. } }`) are now reserved and lowered as body_local HIR items in the graph path — invisible to module-level name scans (three owner-module scans filtered), recursive through nested fn bodies, excluding impls/traits (their paths name body-local types module resolution cannot see). Lowering is fail-soft per body-local record: unresolvable types (body-local `use super::ManuallyDrop` in transmutability.rs) roll back the record and clear its AST identity instead of failing the crate. Tyck struct literals resolve NESTED_ITEM by exact declaring-AST identity. Probe now prints core-hir first-error location. Repro: miniuser `guard_use` (local struct literal + field reads) — 32/32. Real core: 38,835 items (+209), 0 errors. Gates: gcc test, clang strict, tcc, ASan/UBSan all green. Census49 in flight.
- Measured pass: normalize no longer scans impls for an unresolved-projection self (`<<I::Item as Try>::Residual as Residual>::TryType` leniently matched the first Residual impl — the Option flavor — normalizing Result-typed from_iter/transpose bodies to Option and failing body-signature coercion; the projection now stays symbolic and coercion's projection leniency applies). Gates: gcc test, clang strict, tcc, ASan/UBSan all green. Census50 in flight.
- Measured pass: unresolved single-segment value paths in bodies now fall back to a unique same-source top-level body-local function (a body-local fn's ubody starts with an empty nested scope, so task.rs's clone_waker/wake/wake_by_ref/drop_waker vtable set and btree's self-recursive clone_subtree arrived unresolved; census51dbg showed all 44 unresolved-value errors were kind=UNRESOLVED, dominated by these). Ambiguous names stay errors. Also: unresolved-value paths now print segments+span under CM_TYCK_DEBUG. Repro: miniuser `vtable_like` (mutually + self-recursive nested fns) — 35/35. Gates: gcc test, clang strict, tcc, ASan/UBSan all green. Census52 in flight.
- Measured pass: deref coercion at argument position (`&Vec<u8>` where `&[u8]` is expected): the REF/REF coercion tries the actual pointee's user `deref` before lenient equality. Repro: miniuser `pass_veclike` (&VecLike -> &[u32] via inherent deref) — 38/38. Census53: error nodes 314 -> 286, call-arg 36 -> 26, typed 23229 -> 23256. Census52 recorded the prior pass: 326 -> 314, unresolved-value 44 -> 37. Gates: gcc test, clang strict, tcc, ASan/UBSan all green.
- Measured pass: method lookup now runs three phases across all autoderef steps — inherent impls, then specific trait impls, then blanket impls + declaration/projection/dyn fallbacks (`(&str).to_owned()` reached `impl<T: Clone> ToOwned for T` at step 0 with T := &str before `ToOwned for str` could match at the deref step, typing String-returning bodies as &str — the from x13 / spec_to_string body-signature family). Lookup mask gains a blanket bit; explicit callers search all phases. Also: body-expand gained an env-gated dump of generated format_args text. Gates: gcc test, clang strict, tcc, ASan/UBSan all green. Census59 in flight.
- Correction + measured pass: commit 3e2c9eb6 was pushed on a RED ASan lane (compile error: GNU-only memmem in the temporary format_args dump — the gate's echoed exit line said asan exit=1 while the task status read completed; lesson recorded in memory). The dump is removed (it served its purpose). Root cause it exposed: a dependency's `format_args!` resolves to core's `#[rustc_builtin_macro]` stub and was routed through macro_rules, expanding to `{{ }}` (unit) — every formatted assert/panic argument typed `()` (`Option<()> vs Option<Arguments>` x14 across call/method args). Builtins now win over resolved macro declarations when the name is a known builtin. Census59 (blanket-phase commit): error nodes 286 -> 274, typed 23,261. Gates for this commit: gcc test + tyck + ASan/UBSan foreground green; strict/tcc lanes + census60 in flight.
- Measured pass: builtin macro expansions in a dependent crate now name core machinery through the resolved dependency's extern prefix (`core::panicking::panic`, `core::fmt::rt::Argument::...`) instead of the current crate's `crate::` self-reference — census60 showed the routing fix made expansions real but their `crate::fmt::rt` paths unresolved in alloc (unresolved-value 37 -> 149, ubody failed 43); assert/panic/unreachable/format_args generators take the prefix from the resolved target. Census60 wins retained: call-arg 25 -> 17, method-arg 21 -> 14, branch 20 -> 12 (Arguments families collapsed). Gates: gcc test, clang strict, tcc, ASan/UBSan all green (verified by echoed exit lines). Census61 in flight.
- Measured pass: `panic!`/`unreachable!` expand directly to `<prefix>::panicking::panic("explicit panic"/literal)` or `panic_fmt(format_args!(...))` with core's panic_2021 semantics — the edition macros are macros-2.0 `pub macro` items the dependency artifact does not index, leaving 48 "macro is not in scope" failures (43 alloc bodies unexpanded/skipped). Fixture test_body_expand positive_chain threshold 11 -> 10 rules expansions (panic_2021 no longer via rules). Census61 recorded prior pass: 357 -> 244 (unresolved-value 149 -> 36). Census63: ubody failed 43 -> 0, typed 23,244 -> 23,276, error nodes 244 -> 250 (+6 from newly-typed bodies). Gates: gcc test foreground, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified).
- Measured pass: unresolved sibling body-local fn references take the first same-source match instead of bailing on ambiguity (task.rs defines the waker vtable fn set twice, for Wake and LocalWake, with identical signatures — the fallback declined all eight). Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census65 in flight.
- Measured pass: branch joins accept two different fn items (partition-strategy selection in sort decays to a common signature; left item kept leniently), and coercion goes element-wise through tuples (`(&[T], &[], &[])` vs `(&[T], &[U], &[T])` — empty-array literals unsize against slice elements). Census65 recorded the prior pass: 250 -> 242, unresolved-value 36 -> 29. Also instrumented: let-init and index error sites now print debug pairs/spans under CM_TYCK_DEBUG. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census66/67dbg in flight.
- Measured pass: index expressions deref Cow-like ADTs without an `index` method through their user `deref` before giving up (`&Cow<'_, str>[..]` / `&Cow<'_, [T]>[range]` — 19 nodes, the whole index-on-unsupported family per census67dbg samples). Census66 recorded the prior pass: 242 -> 232. Also: let-init errors now print their initializer span under CM_TYCK_DEBUG. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census68dbg in flight.
- Measured pass: method selection is return-type guided when the call site has an expected type — an args-compatible candidate whose return cannot coerce to the expectation is skipped (first args-compatible kept as fallback). Fixes bstr's macro-generated `let other: &[u8] = (&**other).as_ref();` picking `AsRef<str>` (20 let-init nodes). Census68dbg recorded the prior pass: 232 -> 212 (index family fully collapsed). Repro: minicore `Wrapp` with `AsR<str>` + `AsR<[u8]>`, miniuser `pick_asr` — 42/42. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census69 in flight.
- Measured pass: the return-type probe is judge-only — its coercion bindings are rolled back even on accept (persisting them prematurely forced generics against the expectation and later argument checks failed: census69 showed let-init 30 -> 7 but method-arg 14 -> 44 on the previous commit; net 212 -> 209). Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census70 in flight.
- Measured pass (M9-05 opened): the probe now prints a MIR-readiness census after tyck — bodies classified as mir-ready vs blocked (not-typed / closure / asm / offset_of / unsupported / skipped), the entry frontier for ubody->MIR lowering per plan's census-loop template. Census70 recorded the prior pass: 209 -> 179 error nodes (method-arg back to 14, let-init held at 7). Local repro: 41/42 mir-ready (1 closure). Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census71 (first full MIR frontier) in flight.
- Measured pass: the probe's MIR census gained an expression-kind histogram across mir-ready bodies (`mir-exprs kind=count ...`) — the ubody->MIR implementation priority list by construct frequency. Census71 recorded the first full frontier: 23,100/24,942 bodies mir-ready (92.6%); blocked: not_typed 1,616, closure 170, asm 53, offset_of 3. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census72 (full histogram) in flight.
- Measured pass (M9-05): first ubody->MIR lowering walker (src/mir/ulower.c, cm_mir_ulower_all) — executes the lowering's control-flow skeleton over every fully typed ubody with a v1 vocabulary (block/let/literal/path/call/method-call/field/index/unary/ref/binary/assign/cast/flow/if/loop/while/tuple/array/repeat/struct) and classifies each body lowered vs first-blocked (match/try/range/let-condition/for/closure/asm/offset-of remain named blockers); probe prints `mir bodies/lowered/blocked` + class histogram. Census72 recorded expr priorities (path 114k, block 44k, literal 36k, call 32k, method-call 19k, binary 18k over mir-ready bodies). Local repro: 40/42 lowered. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census73 (first full walk) in flight.
- Measured pass (M9-05): the walker descends match arms (scrutinee/guards/bodies), try, range, for, and let-conditions as counted `needs-*` classes instead of blocking — blockers now name only plan-less constructs (closure, asm, offset-of, exotic let patterns, unsupported). Census73 recorded the first walk: 21,647/23,326 clean; blocked match 869, try 316, range 208, closure 151, for 61, asm 53, let-pattern 19. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census74 in flight.
- Measured pass (M9-05): construction metrics in the walker (statements the build would emit, extra basic blocks for ifs) — the first sizing of real u-MIR emission. Census74 recorded the descending walker: 23,081/23,326 (99.0%) walk clean; hard blockers 245 (closure 171, asm 53, let-pattern 19, offset-of 2); counted needs: match 883, try 334, range 272, for 76. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census75 in flight.
- Measured pass (M9-05): first real u-MIR construction — arena-owned CmUMirBody per typed body (locals as tyck CmTyId vec with return slot 0, basic blocks of assign statements carrying originating CmUExprId + type, return/goto/switch-bool terminators; if lowers to real block structure). Unmapped kinds emit CM_UMIR_RVALUE_OPAQUE keeping their type, so emission grows class-by-class. Census75 recorded the sizing: 149,791 statements / 10,514 blocks over 23,081 constructible bodies. Local repro: 42/42 built, 84 statements, 45 blocks. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census76 in flight.
- Measured pass (M9-05): opaque u-MIR statements are tallied by originating expression kind (`umir-opaque opaque-<kind>=count`) — the real-emission priority list. Census76 recorded total construction: 23,326/23,326 typed bodies build u-MIR, 147,277 statements, 37,729 blocks. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census77 (full opaque histogram) in flight.
- Measured pass (M9-05): the mechanical emission tier — method-call, ref, cast, assign/assign-op, field/tuple-field, tuple/array/struct aggregates, and return (real terminator) now emit dedicated u-MIR rvalues instead of opaque assignments (census77 priorities: method-call 14,098, ref 1,989, cast 1,781, assign 1,324, field 841, tuple 684, tuple-field 641, assign-op 640, struct 517, array 392, return 370). Local repro: opaque residue collapsed to loop/match/closure/index. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census78 in flight.
- Measured pass (M9-05): the structural emission tier — loop/while lower to real header/body/exit block structure with back-edge gotos and switch-bool conditions; match lowers to a SWITCH dispatch terminator over per-arm blocks rejoining at a join block; index emits a place-read rvalue; qualified-path reads and range/array-repeat aggregates emit directly. Census78 recorded the mechanical tier: 229,496 statements / 37,792 blocks, residue match 892, try 609, index 514, qualified-path 476, while 176, closure 166, range 118, loop 105, for 86, repeat 84. Local repro: opaque residue = 1 closure. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census79 in flight.
- Measured pass (M9-05): the desugar emission tier — break/continue target the innermost loop's exit/header through a loop-context stack; `?` lowers to a switch-bool with an early-return arm and a TRY_UNWRAP ok-value; `for` lowers to header/body/exit blocks with an ITER_NEXT element and some/none dispatch. Census79 recorded the structural tier: 254,568 statements / 44,428 blocks; residue try 664, closure 180, for 92, asm 55, break 49. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census80 in flight.
- Measured pass (M9-05): the marker emission tier — closure values, retained inline-asm, and offset_of emit dedicated marker rvalues (CLOSURE/ASM/OFFSET_OF) carrying their expressions for S10/S11 handling at C emission. Census80 recorded the desugar tier: 260,137 statements / 46,280 blocks; residue was exactly closure 180 + asm 55 + offset-of 2. Local repro: opaque histogram empty. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census81 (expected-empty histogram) in flight.
- MILESTONE (M9-05): census81 confirms the u-MIR opaque histogram is EMPTY — every one of the 23,326 fully typed bodies emits complete u-MIR (260,137 statements, 46,280 blocks, 0 blocked). Expression coverage for ubody->MIR is total; remaining M9-05 items are the S7 const evaluator and the 1,616 partial-tyck bodies. plan.md status table updated. Next frontier: M9-06 C emission from u-MIR toward the `#![no_core]` hello-exit e2e gate.
- Measured pass (M9-06 opened): dry C emission over u-MIR (src/codegen/umir_c.c, cm_umir_c_emit_dry) — every statement classified renderable-as-C or counted in a `cemit-*`/`ctype-*` gap class (v1 C vocabulary: scalars, unit, pointers; literal/local/binary/unary/ref/cast/assign rvalues). Probe prints `cemit bodies/emitted/statements/rendered` + gap histogram. Local repro: 22/42 bodies fully renderable, 66/106 statements. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census82 (first full gap ranking) in flight.
- Measured pass (M9-06): the naming tier — every concrete nominal/structural type (ADT, tuple, ref/ptr, array, fn, slice/str as ptr+len) classifies C-nameable through mangled typedefs; call/method-call render via mangled instance symbols, field/index/aggregate via layout member names, try/iter via discriminant reads. Generic/projection positions become informational `needs-mono-*` classes (they vanish after instance collection). Census82 baseline recorded: 2,499/23,326 bodies, gaps ctype-adt 63,252 / ctype-fn 32,234 / needs-mono 16,840 / calls 16,495. Local repro: 38/42 bodies, 102/106 statements. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census83 in flight.
- MILESTONE (M9-06 dry census saturated): census83 — 16,640/23,326 bodies and 243,055/260,137 statements (93.4%) classify fully C-renderable; residue is informational needs-mono 16,840 (vanishes at instance collection) plus closure 180, asm 55, offset-of 2, dyn 5. The dry-census phase is done; next is actual C text emission from u-MIR, the monomorphization collector, and the `#![no_core]` hello-exit e2e gate.
- Measured pass (M9-06): u-MIR statements record their operand locals in evaluation order (binary/unary/call/method-call/ref/cast/assign/field/tuple-field/index; fixed 4-slot array with overflow count for long argument lists) — the input real C text emission renders from. Local repro unchanged (42/42, 38 renderable). Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census84 in flight.
- Measured pass (M9-06): first real C text from u-MIR — cm_umir_c_render_body renders one body as a C function (uniform long long local slots, labelled blocks with goto/if/return terminators, integer/bool literals, binary/unary rvalues; other rvalues as placeholder comments). The probe renders a 200-body sample to /tmp/cmrustc-crender-sample.c and gcc-syntax-checks it (CMRUSTC_CC selects the compiler). Local repro: 42 bodies rendered, 25 complete, compile=1. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census85 (full-tree sample compile) in flight.
- Measured pass (M9-06): u-MIR locals bound to ubody locals — one slot per body local after the return slot (typed from tyck local_types), `let` BINDING patterns copy the initializer into the bound slot, and PATH reads of body locals render as real `_l<n>` reads (item paths remain placeholders until symbol rendering). Census85 recorded the renderer's first full sample: 200 bodies, 74,467 bytes, gcc compile=1. Local repro: 42 rendered, compile=1 with 361 local references. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census86 in flight.
- Measured pass (M9-06): tyck persists the chosen method definition per METHOD_CALL expression (CmTyckBody.method_targets); the C renderer emits calls as `{ long long cm_u_<crate>_<index>(); _d = sym(args); }` for CALL (callee PATH resolution) and METHOD_CALL (persisted target), keeping each sample self-contained for the syntax check. Census86 recorded the local-binding sample: 200 bodies, 79,495 bytes, compile=1. Local repro: 51 rendered call symbols, compile=1, tyck tests ok. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified). Census87 in flight.
- Correction (M9-06): commit 38a20b9f was pushed on a RED ASan lane — LeakSanitizer: the new CmTyckBody.method_targets table was never freed in cm_tyck_set_destroy (10 leaked objects); the commit chain printed the echoed exit line but did not gate on it. Fixed: method_targets freed with the other per-body tables; this commit's ASan gate is the foreground rerun gated by its echoed line. Census87 recorded: 200 rendered bodies with call symbols, 86,971 bytes, gcc compile=1.
- MILESTONE (M9-06 first executable gate GREEN): tests/codegen/run_umir_echo.sh — the probe's `--emit-umir-c` renders the G3 blanket-echo `#![no_core]` fixture to a C translation unit (def-named `long long cm_u_<crate>_<index>(long long p...)` bodies with parameter prologues, block/if values propagating to the return slot, `#[no_mangle]` ABI wrappers with <stdint.h> types), gcc links it with the fixture's C harness, and the binary passes all four checks. Callees resolve from the callee expression's FN_DEF tyck type (any callee shape) or tyck's persisted method target. Census88: 200-body full-tree sample 101,075 bytes, compile=1. Gates on the final tree: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified; commit gated on them).
- Measured pass (M9-06): the u-MIR echo gate is wired into `make test` (probe built as a prerequisite; the script skips when the compiler or probe is unavailable) and runs in both compiler lanes — gcc and tcc both compile and execute the emitted C successfully. plan.md M9-06 row updated to "first executable gate green". Gates on this tree: gcc test (echo gate ok), clang strict, tcc (echo gate ok under tcc), ASan/UBSan all green (echoed exit lines verified; commit gated).
- MILESTONE (M9-06): all 19 G3 executable fixtures pass end-to-end through u-MIR (tests/codegen/run_umir_fixtures.sh, now a required `make test` gate in both compiler lanes — gcc and tinycc each compile and run all 19). Renderer growth this cycle: full binary/unary operator table with ABI-width operand casts (exact wrapping, signedness, comparisons), aggregates as function-scope slot arrays with declared-order field mapping through HIR field names (pointer-to-slots leniency), FIELD/tuple-field reads by slot index, aggregate element operands recorded in u-MIR. Fixture census: 13/19 -> 16/19 (operators) -> 19/19 (aggregates). Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified; commit gated). Census89 in flight.
- Measured pass (M9-06): enums and `match` — variant constructors (unit/tuple/struct) build slot arrays with the discriminant in slot 0; match lowers to a SWITCH terminator carrying per-arm targets and pattern discriminants (path/tuple-struct/struct patterns resolve to their variant index; binding/wildcard arms become the default), payload bindings read slots (RVALUE_SLOT); the C renderer emits `switch` on slot 0. Fixture tests/codegen/fixtures/enum-match-probe (Point/Square/Rect) added; the required fixture gate reads 20/20 in both compiler lanes. Also: the 200-body sample now includes <stdint.h>, and unary rendering is balanced by construction. Gates: gcc test (20/20), clang strict, tcc (20/20), ASan/UBSan all green (echoed exit lines verified; commit gated).
- Measured pass (M9-06): place-aware assignment (locals write their bound slot; field/index/deref targets emit STORE_FIELD/STORE_INDEX/STORE_DEREF; `op=` reads, combines, stores), references as slot addresses with loads through `*` and through reference layers for field/slot/store access, a NONE terminator with seal-if-unterminated so `break`/`continue` inside branches survive, and the emitter cases a match-rewrite splice had deleted (break/continue/try/for/closure/asm/offset_of) restored. New fixtures: loop-sum-probe (while/loop/break), ref-probe (&mut aggregate mutation); generic-dispatch-probe authored but parked (`*-harness.c.pending`) until instance dispatch — its trait-bound call resolves to the declaration symbol. Fixture scripts run binaries under `timeout 10`. Census90 recorded: enum renderer full-tree sample 102,251 bytes compile=1. Fixture gate: 22/22 in both gcc and tcc lanes. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified; commit gated).
- MILESTONE (M9-06): generic instance collection and trait dispatch — `cm_umir_c_render_program` renders one translation unit from a worklist: every non-generic complete body is a root; each call to a generic callee registers a (definition, substitution) instance (arguments from the callee's substituted FN_DEF type over `[impl params…, fn params…]`) rendered once under that substitution with an `_i<n>` symbol suffix; trait-method declaration targets resolve to the impl method whose self type matches the substituted receiver; type decisions (ABI widths, field maps, reference depth) see substituted types. generic-dispatch-probe (`apply<T: Scale>` over u32 and u8 impls) re-enabled; the fixture gate reads 23/23 in both gcc and tcc lanes. Census91 recorded: 106,134-byte full-tree sample, compile=1. Gates: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified; commit gated).
- Measured pass (M9-06): closures — frames render as one `long long _l[N]` array; each closure expression lowers to its own u-MIR body whose first `env_count` locals alias the enclosing frame (captures by construction), rendered as `cm_u_closure_<body>_<expr>(long long *env, params…)` with its own return slot and temporaries; a closure value is its frame base and CLOSURE-typed callees call the static symbol; program instances key on (definition, closure expression). Fixture closure-probe (capturing and direct closures) added — 24/24 in both gcc and tcc lanes. `Fn` traits need core and wait for the core-linked gate; tests/codegen/core-fixtures/ + run_umir_core.sh (measurement, --with-core) authored to open it. Census92/93 recorded: full-tree samples 106,134 / 78,817 bytes, compile=1. Gates on the final tree: gcc test, clang strict, tcc, ASan/UBSan all green (echoed exit lines verified; commit gated).
