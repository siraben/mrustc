/* ordinary /* nested */ comment */
alpha r#fn _ 'a 'static 'x' '\n' b'z'
0 1u8 0b101u16 0o77usize 0xff_u32 1.25f32 2e3f64 1..2
"string" b"bytes" c"C string" r#"raw " string"# br##"raw # bytes"##
( ) { } [ ] , ; : :: . .. ..= ... @ # $ ? ~
= == => ! != < <= << <<= <- > >= >> >>= + += - -= -> * *= / /=
% %= ^ ^= & && &= | || |=
as break const continue crate else enum extern false fn for if impl in let
loop match mod move mut pub ref return self static struct super trait true
type unsafe use where while abstract become box do final macro override priv
typeof unsized virtual yield
